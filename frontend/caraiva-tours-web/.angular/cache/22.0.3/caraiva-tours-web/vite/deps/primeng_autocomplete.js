import {
  e
} from "./chunk-5QBQTHLM.js";
import {
  ChevronDown,
  Motion,
  MotionModule,
  zindexutils
} from "./chunk-YC7CJDAA.js";
import "./chunk-7MELLMVS.js";
import "./chunk-MSLT2XYH.js";
import {
  BaseInput,
  Times
} from "./chunk-ZZ5Z72QQ.js";
import "./chunk-BGLX7RGU.js";
import {
  Ripple,
  Spinner
} from "./chunk-IKJBIBEI.js";
import "./chunk-QZPWBGZN.js";
import {
  AutoFocus,
  ConnectedOverlayScrollHandler
} from "./chunk-JJBFR52L.js";
import {
  InputText
} from "./chunk-5BS7KGIU.js";
import {
  NG_VALUE_ACCESSOR
} from "./chunk-HMLX62JK.js";
import "./chunk-V7NQNLRP.js";
import {
  BaseComponent,
  PARENT_INSTANCE
} from "./chunk-3KTBS3JL.js";
import {
  Bind,
  BindModule
} from "./chunk-RBNGSEBV.js";
import {
  BaseStyle
} from "./chunk-ZQICQ6EM.js";
import {
  OverlayService,
  SharedModule,
  TranslationKeys
} from "./chunk-HLLH2L5P.js";
import "./chunk-VOEPLAYN.js";
import {
  C2 as C,
  Ht,
  Pt,
  R,
  W,
  Y,
  Z2 as Z,
  b,
  d,
  et,
  ft,
  jt,
  l,
  oe,
  p,
  s2 as s,
  wt,
  z,
  z2
} from "./chunk-RB5ZFJAO.js";
import {
  NgTemplateOutlet,
  isPlatformBrowser
} from "./chunk-KX2I6PVZ.js";
import "./chunk-4ZHAZINZ.js";
import {
  CoreIcon,
  ICON_TEMPLATE
} from "./chunk-Y5JDOCLT.js";
import "./chunk-EZXZ43RR.js";
import {
  ChangeDetectionStrategy,
  Component,
  ContentChild,
  HostListener,
  Injectable,
  Input,
  NgModule,
  Output,
  ViewChild,
  ViewEncapsulation,
  booleanAttribute,
  contentChild,
  input,
  model,
  numberAttribute,
  output,
  setClassMetadata,
  viewChild,
  ɵɵHostDirectivesFeature,
  ɵɵInheritDefinitionFeature,
  ɵɵProvidersFeature,
  ɵɵadvance,
  ɵɵattribute,
  ɵɵclassMap,
  ɵɵconditional,
  ɵɵconditionalCreate,
  ɵɵcontentQuerySignal,
  ɵɵdefineComponent,
  ɵɵdefineNgModule,
  ɵɵdomElement,
  ɵɵelement,
  ɵɵelementContainer,
  ɵɵelementEnd,
  ɵɵelementStart,
  ɵɵgetCurrentView,
  ɵɵgetInheritedFactory,
  ɵɵlistener,
  ɵɵnextContext,
  ɵɵprojection,
  ɵɵprojectionDef,
  ɵɵproperty,
  ɵɵpureFunction0,
  ɵɵpureFunction1,
  ɵɵpureFunction3,
  ɵɵqueryAdvance,
  ɵɵreference,
  ɵɵrepeater,
  ɵɵrepeaterCreate,
  ɵɵrepeaterTrackByIndex,
  ɵɵsanitizeUrl,
  ɵɵstyleMap,
  ɵɵstyleProp,
  ɵɵtemplate,
  ɵɵtemplateRefExtractor,
  ɵɵtext,
  ɵɵtextInterpolate,
  ɵɵtextInterpolate1,
  ɵɵviewQuerySignal
} from "./chunk-N3JMXNJE.js";
import {
  InjectionToken,
  computed,
  effect,
  forwardRef,
  inject,
  signal,
  untracked,
  ɵɵdefineInjectable,
  ɵɵdefineInjector,
  ɵɵnamespaceSVG,
  ɵɵresetView,
  ɵɵrestoreView
} from "./chunk-QAPLPPA7.js";
import "./chunk-RSS3ODKE.js";
import {
  __spreadProps,
  __spreadValues
} from "./chunk-GOMI4DH3.js";

// node_modules/@primeicons/angular/fesm2022/primeicons-angular-times-circle.mjs
var _forTrack0 = ($index, $item) => $item[1]["key"] || $index;
function TimesCircle_For_1_Case_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵnamespaceSVG();
    ɵɵdomElement(0, "path");
  }
  if (rf & 2) {
    const node_r1 = ɵɵnextContext().$implicit;
    ɵɵattribute("d", node_r1[1]["d"])("fill", node_r1[1]["fill"])("fill-opacity", node_r1[1]["fillOpacity"])("fill-rule", node_r1[1]["fillRule"])("clip-rule", node_r1[1]["clipRule"])("stroke", node_r1[1]["stroke"])("stroke-width", node_r1[1]["strokeWidth"])("stroke-opacity", node_r1[1]["strokeOpacity"])("opacity", node_r1[1]["opacity"]);
  }
}
function TimesCircle_For_1_Case_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵnamespaceSVG();
    ɵɵdomElement(0, "circle");
  }
  if (rf & 2) {
    const node_r1 = ɵɵnextContext().$implicit;
    ɵɵattribute("cx", node_r1[1]["cx"])("cy", node_r1[1]["cy"])("r", node_r1[1]["r"])("fill", node_r1[1]["fill"])("fill-opacity", node_r1[1]["fillOpacity"])("opacity", node_r1[1]["opacity"]);
  }
}
function TimesCircle_For_1_Case_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵnamespaceSVG();
    ɵɵdomElement(0, "rect");
  }
  if (rf & 2) {
    const node_r1 = ɵɵnextContext().$implicit;
    ɵɵattribute("x", node_r1[1]["x"])("y", node_r1[1]["y"])("width", node_r1[1]["width"])("height", node_r1[1]["height"])("rx", node_r1[1]["rx"])("ry", node_r1[1]["ry"])("fill", node_r1[1]["fill"])("fill-opacity", node_r1[1]["fillOpacity"])("opacity", node_r1[1]["opacity"]);
  }
}
function TimesCircle_For_1_Case_3_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵnamespaceSVG();
    ɵɵdomElement(0, "line");
  }
  if (rf & 2) {
    const node_r1 = ɵɵnextContext().$implicit;
    ɵɵattribute("x1", node_r1[1]["x1"])("y1", node_r1[1]["y1"])("x2", node_r1[1]["x2"])("y2", node_r1[1]["y2"])("stroke", node_r1[1]["stroke"])("stroke-opacity", node_r1[1]["strokeOpacity"])("opacity", node_r1[1]["opacity"]);
  }
}
function TimesCircle_For_1_Case_4_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵnamespaceSVG();
    ɵɵdomElement(0, "polyline");
  }
  if (rf & 2) {
    const node_r1 = ɵɵnextContext().$implicit;
    ɵɵattribute("points", node_r1[1]["points"])("fill", node_r1[1]["fill"])("fill-opacity", node_r1[1]["fillOpacity"])("opacity", node_r1[1]["opacity"]);
  }
}
function TimesCircle_For_1_Case_5_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵnamespaceSVG();
    ɵɵdomElement(0, "polygon");
  }
  if (rf & 2) {
    const node_r1 = ɵɵnextContext().$implicit;
    ɵɵattribute("points", node_r1[1]["points"])("fill", node_r1[1]["fill"])("fill-opacity", node_r1[1]["fillOpacity"])("opacity", node_r1[1]["opacity"]);
  }
}
function TimesCircle_For_1_Case_6_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵnamespaceSVG();
    ɵɵdomElement(0, "ellipse");
  }
  if (rf & 2) {
    const node_r1 = ɵɵnextContext().$implicit;
    ɵɵattribute("cx", node_r1[1]["cx"])("cy", node_r1[1]["cy"])("rx", node_r1[1]["rx"])("ry", node_r1[1]["ry"])("fill", node_r1[1]["fill"])("fill-opacity", node_r1[1]["fillOpacity"])("opacity", node_r1[1]["opacity"]);
  }
}
function TimesCircle_For_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵconditionalCreate(0, TimesCircle_For_1_Case_0_Template, 1, 9, ":svg:path")(1, TimesCircle_For_1_Case_1_Template, 1, 6, ":svg:circle")(2, TimesCircle_For_1_Case_2_Template, 1, 9, ":svg:rect")(3, TimesCircle_For_1_Case_3_Template, 1, 7, ":svg:line")(4, TimesCircle_For_1_Case_4_Template, 1, 4, ":svg:polyline")(5, TimesCircle_For_1_Case_5_Template, 1, 4, ":svg:polygon")(6, TimesCircle_For_1_Case_6_Template, 1, 7, ":svg:ellipse");
  }
  if (rf & 2) {
    let tmp_10_0;
    const node_r1 = ctx.$implicit;
    ɵɵconditional((tmp_10_0 = node_r1[0]) === "path" ? 0 : tmp_10_0 === "circle" ? 1 : tmp_10_0 === "rect" ? 2 : tmp_10_0 === "line" ? 3 : tmp_10_0 === "polyline" ? 4 : tmp_10_0 === "polygon" ? 5 : tmp_10_0 === "ellipse" ? 6 : -1);
  }
}
var TimesCircle = class _TimesCircle extends CoreIcon {
  constructor() {
    super();
    this._icon = e;
  }
  static ɵfac = function TimesCircle_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _TimesCircle)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _TimesCircle,
    selectors: [["svg", "data-p-icon", "times-circle"]],
    features: [ɵɵInheritDefinitionFeature],
    decls: 2,
    vars: 0,
    template: function TimesCircle_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵrepeaterCreate(0, TimesCircle_For_1_Template, 7, 1, null, null, _forTrack0);
      }
      if (rf & 2) {
        ɵɵrepeater(ctx.iconNodes());
      }
    },
    encapsulation: 2,
    changeDetection: 1
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(TimesCircle, [{
    type: Component,
    args: [{
      selector: 'svg[data-p-icon="times-circle"]',
      standalone: true,
      template: ICON_TEMPLATE
    }]
  }], () => [], null);
})();

// node_modules/@primeuix/styles/dist/chip/index.mjs
var style = "\n    .p-chip {\n        display: inline-flex;\n        align-items: center;\n        background: dt('chip.background');\n        color: dt('chip.color');\n        border-radius: dt('chip.border.radius');\n        padding-block: dt('chip.padding.y');\n        padding-inline: dt('chip.padding.x');\n        gap: dt('chip.gap');\n    }\n    \n    .p-chip.p-focus {\n        background: dt('chip.focus.background');\n    }\n\n    .p-chip-icon {\n        color: dt('chip.icon.color');\n        font-size: dt('chip.icon.size');\n        width: dt('chip.icon.size');\n        height: dt('chip.icon.size');\n        flex-shrink: 0;\n    }\n\n    .p-chip-image {\n        border-radius: 50%;\n        width: dt('chip.image.width');\n        height: dt('chip.image.height');\n        margin-inline-start: calc(-1 * dt('chip.padding.y'));\n        flex-shrink: 0;\n    }\n\n    .p-chip-label {\n        font-weight: dt('chip.label.font.weight');\n        font-size: dt('chip.label.font.size');\n    }\n\n    .p-chip:has(.p-chip-remove-icon) {\n        padding-inline-end: dt('chip.padding.y');\n    }\n\n    .p-chip:has(.p-chip-image) {\n        padding-block-start: calc(dt('chip.padding.y') / 2);\n        padding-block-end: calc(dt('chip.padding.y') / 2);\n    }\n\n    .p-chip-remove-icon {\n        cursor: pointer;\n        font-size: dt('chip.remove.icon.size');\n        width: dt('chip.remove.icon.size');\n        height: dt('chip.remove.icon.size');\n        color: dt('chip.remove.icon.color');\n        border-radius: 50%;\n        transition:\n            outline-color dt('chip.transition.duration'),\n            box-shadow dt('chip.transition.duration');\n        outline-color: transparent;\n    }\n\n    .p-chip-remove-icon:focus-visible {\n        box-shadow: dt('chip.remove.icon.focus.ring.shadow');\n        outline: dt('chip.remove.icon.focus.ring.width') dt('chip.remove.icon.focus.ring.style') dt('chip.remove.icon.focus.ring.color');\n        outline-offset: dt('chip.remove.icon.focus.ring.offset');\n    }\n";

// node_modules/primeng/fesm2022/primeng-chip.mjs
var _c0 = ["removeicon"];
var _c1 = ["*"];
function Chip_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "img", 2);
    ɵɵlistener("error", function Chip_Conditional_1_Template_img_error_0_listener($event) {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.imageError($event));
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵclassMap(ctx_r1.cx("image"));
    ɵɵproperty("pBind", ctx_r1.ptm("image"))("src", ctx_r1._image(), ɵɵsanitizeUrl)("alt", ctx_r1._alt());
  }
}
function Chip_Conditional_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "span", 3);
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵclassMap(ctx_r1.cn(ctx_r1.cx("icon"), ctx_r1._icon()));
    ɵɵproperty("pBind", ctx_r1.ptm("icon"));
  }
}
function Chip_Conditional_3_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 3);
    ɵɵtext(1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵclassMap(ctx_r1.cx("label"));
    ɵɵproperty("pBind", ctx_r1.ptm("label"));
    ɵɵadvance();
    ɵɵtextInterpolate(ctx_r1._label());
  }
}
function Chip_Conditional_4_Conditional_0_Conditional_0_Template(rf, ctx) {
  if (rf & 1) {
    const _r3 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "span", 6);
    ɵɵlistener("click", function Chip_Conditional_4_Conditional_0_Conditional_0_Template_span_click_0_listener($event) {
      ɵɵrestoreView(_r3);
      const ctx_r1 = ɵɵnextContext(3);
      return ɵɵresetView(ctx_r1.close($event));
    })("keydown", function Chip_Conditional_4_Conditional_0_Conditional_0_Template_span_keydown_0_listener($event) {
      ɵɵrestoreView(_r3);
      const ctx_r1 = ɵɵnextContext(3);
      return ɵɵresetView(ctx_r1.onKeydown($event));
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(3);
    ɵɵclassMap(ctx_r1.cn(ctx_r1.cx("removeIcon"), ctx_r1._removeIcon()));
    ɵɵproperty("pBind", ctx_r1.ptm("removeIcon"));
    ɵɵattribute("tabindex", ctx_r1.removeIconTabindex())("aria-label", ctx_r1.removeAriaLabel());
  }
}
function Chip_Conditional_4_Conditional_0_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r4 = ɵɵgetCurrentView();
    ɵɵnamespaceSVG();
    ɵɵelementStart(0, "svg", 7);
    ɵɵlistener("click", function Chip_Conditional_4_Conditional_0_Conditional_1_Template_svg_click_0_listener($event) {
      ɵɵrestoreView(_r4);
      const ctx_r1 = ɵɵnextContext(3);
      return ɵɵresetView(ctx_r1.close($event));
    })("keydown", function Chip_Conditional_4_Conditional_0_Conditional_1_Template_svg_keydown_0_listener($event) {
      ɵɵrestoreView(_r4);
      const ctx_r1 = ɵɵnextContext(3);
      return ɵɵresetView(ctx_r1.onKeydown($event));
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(3);
    ɵɵclassMap(ctx_r1.cx("removeIcon"));
    ɵɵproperty("pBind", ctx_r1.ptm("removeIcon"));
    ɵɵattribute("tabindex", ctx_r1.removeIconTabindex())("aria-label", ctx_r1.removeAriaLabel());
  }
}
function Chip_Conditional_4_Conditional_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵconditionalCreate(0, Chip_Conditional_4_Conditional_0_Conditional_0_Template, 1, 5, "span", 4)(1, Chip_Conditional_4_Conditional_0_Conditional_1_Template, 1, 5, ":svg:svg", 5);
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵconditional(ctx_r1._removeIcon() ? 0 : 1);
  }
}
function Chip_Conditional_4_Conditional_1_1_ng_template_0_Template(rf, ctx) {
}
function Chip_Conditional_4_Conditional_1_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵtemplate(0, Chip_Conditional_4_Conditional_1_1_ng_template_0_Template, 0, 0, "ng-template");
  }
}
function Chip_Conditional_4_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r5 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "span", 6);
    ɵɵlistener("click", function Chip_Conditional_4_Conditional_1_Template_span_click_0_listener($event) {
      ɵɵrestoreView(_r5);
      const ctx_r1 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r1.close($event));
    })("keydown", function Chip_Conditional_4_Conditional_1_Template_span_keydown_0_listener($event) {
      ɵɵrestoreView(_r5);
      const ctx_r1 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r1.onKeydown($event));
    });
    ɵɵtemplate(1, Chip_Conditional_4_Conditional_1_1_Template, 1, 0, null, 8);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵclassMap(ctx_r1.cx("removeIcon"));
    ɵɵproperty("pBind", ctx_r1.ptm("removeIcon"));
    ɵɵattribute("tabindex", ctx_r1.removeIconTabindex())("aria-label", ctx_r1.removeAriaLabel());
    ɵɵadvance();
    ɵɵproperty("ngTemplateOutlet", ctx_r1.removeIconTemplate());
  }
}
function Chip_Conditional_4_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵconditionalCreate(0, Chip_Conditional_4_Conditional_0_Template, 2, 1)(1, Chip_Conditional_4_Conditional_1_Template, 2, 6, "span", 4);
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵconditional(!ctx_r1.removeIconTemplate() ? 0 : 1);
  }
}
var inlineStyles = {
  root: ({
    instance
  }) => ({
    display: !instance.visible() && "none"
  })
};
var classes = {
  root: ({
    instance
  }) => ["p-chip p-component", {
    "p-disabled": instance.disabled()
  }],
  image: "p-chip-image",
  icon: "p-chip-icon",
  label: "p-chip-label",
  removeIcon: "p-chip-remove-icon"
};
var ChipStyle = class _ChipStyle extends BaseStyle {
  name = "chip";
  style = style;
  classes = classes;
  inlineStyles = inlineStyles;
  static ɵfac = /* @__PURE__ */ (() => {
    let ɵChipStyle_BaseFactory;
    return function ChipStyle_Factory(__ngFactoryType__) {
      return (ɵChipStyle_BaseFactory || (ɵChipStyle_BaseFactory = ɵɵgetInheritedFactory(_ChipStyle)))(__ngFactoryType__ || _ChipStyle);
    };
  })();
  static ɵprov = ɵɵdefineInjectable({
    token: _ChipStyle,
    factory: _ChipStyle.ɵfac
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(ChipStyle, [{
    type: Injectable
  }], null, null);
})();
var ChipClasses;
(function(ChipClasses2) {
  ChipClasses2["root"] = "p-chip";
  ChipClasses2["image"] = "p-chip-image";
  ChipClasses2["icon"] = "p-chip-icon";
  ChipClasses2["label"] = "p-chip-label";
  ChipClasses2["removeIcon"] = "p-chip-remove-icon";
})(ChipClasses || (ChipClasses = {}));
var CHIP_INSTANCE = new InjectionToken("CHIP_INSTANCE");
var Chip = class _Chip extends BaseComponent {
  componentName = "Chip";
  $pcChip = inject(CHIP_INSTANCE, {
    optional: true,
    skipSelf: true
  }) ?? void 0;
  bindDirectiveInstance = inject(Bind, {
    self: true
  });
  onAfterViewChecked() {
    this.bindDirectiveInstance.setAttrs(this.ptms(["host", "root"]));
  }
  /**
   * Defines the text to display.
   * @group Props
   */
  label = input(
    ...ngDevMode ? [void 0, {
      debugName: "label"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  /**
   * Defines the icon to display.
   * @group Props
   */
  icon = input(
    ...ngDevMode ? [void 0, {
      debugName: "icon"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  /**
   * Defines the image to display.
   * @group Props
   */
  image = input(
    ...ngDevMode ? [void 0, {
      debugName: "image"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  /**
   * Alt attribute of the image.
   * @group Props
   */
  alt = input(
    ...ngDevMode ? [void 0, {
      debugName: "alt"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  /**
   * When present, it specifies that the element should be disabled.
   * @group Props
   */
  disabled = input(false, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "disabled"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    transform: booleanAttribute
  }));
  /**
   * Whether to display a remove icon.
   * @group Props
   */
  removable = input(false, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "removable"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    transform: booleanAttribute
  }));
  /**
   * Icon of the remove element.
   * @group Props
   */
  removeIcon = input(
    ...ngDevMode ? [void 0, {
      debugName: "removeIcon"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  /**
   * Used to pass all properties of the chipProps to the Chip component.
   * @group Props
   */
  chipProps = input(
    ...ngDevMode ? [void 0, {
      debugName: "chipProps"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  /**
   * Callback to invoke when a chip is removed.
   * @param {MouseEvent} event - Mouse event.
   * @group Emits
   */
  onRemove = output();
  /**
   * This event is triggered if an error occurs while loading an image file.
   * @param {Event} event - Browser event.
   * @group Emits
   */
  onImageError = output();
  /**
   * Custom remove icon template.
   * @group Templates
   */
  removeIconTemplate = contentChild("removeicon", __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "removeIconTemplate"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    descendants: false
  }));
  visible = signal(
    true,
    ...ngDevMode ? [{
      debugName: "visible"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  _componentStyle = inject(ChipStyle);
  _label = computed(
    () => this.chipProps()?.label ?? this.label(),
    ...ngDevMode ? [{
      debugName: "_label"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  _icon = computed(
    () => this.chipProps()?.icon ?? this.icon(),
    ...ngDevMode ? [{
      debugName: "_icon"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  _image = computed(
    () => this.chipProps()?.image ?? this.image(),
    ...ngDevMode ? [{
      debugName: "_image"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  _alt = computed(
    () => this.chipProps()?.alt ?? this.alt(),
    ...ngDevMode ? [{
      debugName: "_alt"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  _removable = computed(
    () => this.chipProps()?.removable ?? this.removable(),
    ...ngDevMode ? [{
      debugName: "_removable"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  _removeIcon = computed(
    () => this.chipProps()?.removeIcon ?? this.removeIcon(),
    ...ngDevMode ? [{
      debugName: "_removeIcon"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  removeAriaLabel = computed(
    () => this.translate(TranslationKeys.ARIA, "removeLabel"),
    ...ngDevMode ? [{
      debugName: "removeAriaLabel"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  removeIconTabindex = computed(
    () => this.disabled() ? -1 : 0,
    ...ngDevMode ? [{
      debugName: "removeIconTabindex"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  dataP = computed(
    () => this.cn({
      removable: this._removable()
    }),
    ...ngDevMode ? [{
      debugName: "dataP"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  close(event) {
    this.visible.set(false);
    this.onRemove.emit(event);
  }
  onKeydown(event) {
    if (event.key === "Enter" || event.key === "Backspace") {
      this.close(event);
    }
  }
  imageError(event) {
    this.onImageError.emit(event);
  }
  static ɵfac = /* @__PURE__ */ (() => {
    let ɵChip_BaseFactory;
    return function Chip_Factory(__ngFactoryType__) {
      return (ɵChip_BaseFactory || (ɵChip_BaseFactory = ɵɵgetInheritedFactory(_Chip)))(__ngFactoryType__ || _Chip);
    };
  })();
  static ɵcmp = ɵɵdefineComponent({
    type: _Chip,
    selectors: [["p-chip"]],
    contentQueries: function Chip_ContentQueries(rf, ctx, dirIndex) {
      if (rf & 1) {
        ɵɵcontentQuerySignal(dirIndex, ctx.removeIconTemplate, _c0, 4);
      }
      if (rf & 2) {
        ɵɵqueryAdvance();
      }
    },
    hostVars: 6,
    hostBindings: function Chip_HostBindings(rf, ctx) {
      if (rf & 2) {
        ɵɵattribute("aria-label", ctx._label())("data-p", ctx.dataP());
        ɵɵstyleMap(ctx.sx("root"));
        ɵɵclassMap(ctx.cx("root"));
      }
    },
    inputs: {
      label: [1, "label"],
      icon: [1, "icon"],
      image: [1, "image"],
      alt: [1, "alt"],
      disabled: [1, "disabled"],
      removable: [1, "removable"],
      removeIcon: [1, "removeIcon"],
      chipProps: [1, "chipProps"]
    },
    outputs: {
      onRemove: "onRemove",
      onImageError: "onImageError"
    },
    features: [ɵɵProvidersFeature([ChipStyle, {
      provide: CHIP_INSTANCE,
      useExisting: _Chip
    }, {
      provide: PARENT_INSTANCE,
      useExisting: _Chip
    }]), ɵɵHostDirectivesFeature([Bind]), ɵɵInheritDefinitionFeature],
    ngContentSelectors: _c1,
    decls: 5,
    vars: 3,
    consts: [[3, "pBind", "class", "src", "alt"], [3, "pBind", "class"], [3, "error", "pBind", "src", "alt"], [3, "pBind"], ["role", "button", 3, "pBind", "class"], ["data-p-icon", "times-circle", "role", "button", 3, "pBind", "class"], ["role", "button", 3, "click", "keydown", "pBind"], ["data-p-icon", "times-circle", "role", "button", 3, "click", "keydown", "pBind"], [4, "ngTemplateOutlet"]],
    template: function Chip_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵprojectionDef();
        ɵɵprojection(0);
        ɵɵconditionalCreate(1, Chip_Conditional_1_Template, 1, 5, "img", 0)(2, Chip_Conditional_2_Template, 1, 3, "span", 1);
        ɵɵconditionalCreate(3, Chip_Conditional_3_Template, 2, 4, "div", 1);
        ɵɵconditionalCreate(4, Chip_Conditional_4_Template, 2, 1);
      }
      if (rf & 2) {
        ɵɵadvance();
        ɵɵconditional(ctx._image() ? 1 : ctx._icon() ? 2 : -1);
        ɵɵadvance(2);
        ɵɵconditional(ctx._label() ? 3 : -1);
        ɵɵadvance();
        ɵɵconditional(ctx._removable() ? 4 : -1);
      }
    },
    dependencies: [NgTemplateOutlet, SharedModule, Bind, TimesCircle],
    encapsulation: 2
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(Chip, [{
    type: Component,
    args: [{
      selector: "p-chip",
      standalone: true,
      imports: [NgTemplateOutlet, SharedModule, Bind, TimesCircle],
      template: `
        <ng-content />
        @if (_image()) {
            <img [pBind]="ptm('image')" [class]="cx('image')" [src]="_image()" (error)="imageError($event)" [alt]="_alt()" />
        } @else if (_icon()) {
            <span [pBind]="ptm('icon')" [class]="cn(cx('icon'), _icon())"></span>
        }
        @if (_label()) {
            <div [pBind]="ptm('label')" [class]="cx('label')">{{ _label() }}</div>
        }
        @if (_removable()) {
            @if (!removeIconTemplate()) {
                @if (_removeIcon()) {
                    <span
                        [pBind]="ptm('removeIcon')"
                        [class]="cn(cx('removeIcon'), _removeIcon())"
                        (click)="close($event)"
                        (keydown)="onKeydown($event)"
                        [attr.tabindex]="removeIconTabindex()"
                        [attr.aria-label]="removeAriaLabel()"
                        role="button"
                    ></span>
                } @else {
                    <svg
                        [pBind]="ptm('removeIcon')"
                        data-p-icon="times-circle"
                        [class]="cx('removeIcon')"
                        (click)="close($event)"
                        (keydown)="onKeydown($event)"
                        [attr.tabindex]="removeIconTabindex()"
                        [attr.aria-label]="removeAriaLabel()"
                        role="button"
                    />
                }
            } @else {
                <span [pBind]="ptm('removeIcon')" [attr.tabindex]="removeIconTabindex()" [class]="cx('removeIcon')" (click)="close($event)" (keydown)="onKeydown($event)" [attr.aria-label]="removeAriaLabel()" role="button">
                    <ng-template *ngTemplateOutlet="removeIconTemplate()"></ng-template>
                </span>
            }
        }
    `,
      changeDetection: ChangeDetectionStrategy.OnPush,
      encapsulation: ViewEncapsulation.None,
      providers: [ChipStyle, {
        provide: CHIP_INSTANCE,
        useExisting: Chip
      }, {
        provide: PARENT_INSTANCE,
        useExisting: Chip
      }],
      host: {
        "[class]": "cx('root')",
        "[style]": "sx('root')",
        "[attr.aria-label]": "_label()",
        "[attr.data-p]": "dataP()"
      },
      hostDirectives: [Bind]
    }]
  }], null, {
    label: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "label",
        required: false
      }]
    }],
    icon: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "icon",
        required: false
      }]
    }],
    image: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "image",
        required: false
      }]
    }],
    alt: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "alt",
        required: false
      }]
    }],
    disabled: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "disabled",
        required: false
      }]
    }],
    removable: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "removable",
        required: false
      }]
    }],
    removeIcon: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "removeIcon",
        required: false
      }]
    }],
    chipProps: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "chipProps",
        required: false
      }]
    }],
    onRemove: [{
      type: Output,
      args: ["onRemove"]
    }],
    onImageError: [{
      type: Output,
      args: ["onImageError"]
    }],
    removeIconTemplate: [{
      type: ContentChild,
      args: ["removeicon", __spreadProps(__spreadValues({}, {
        descendants: false
      }), {
        isSignal: true
      })]
    }]
  });
})();
var ChipModule = class _ChipModule {
  static ɵfac = function ChipModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _ChipModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _ChipModule,
    imports: [Chip, SharedModule],
    exports: [Chip, SharedModule]
  });
  static ɵinj = ɵɵdefineInjector({
    imports: [Chip, SharedModule, SharedModule]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(ChipModule, [{
    type: NgModule,
    args: [{
      imports: [Chip, SharedModule],
      exports: [Chip, SharedModule]
    }]
  }], null, null);
})();

// node_modules/primeng/fesm2022/primeng-overlay.mjs
var _c02 = ["content"];
var _c12 = ["overlay"];
var _c2 = ["*", "*"];
var _c3 = () => ({
  mode: null
});
var _c4 = (a0) => ({
  $implicit: a0
});
var _c5 = (a0) => ({
  mode: a0
});
function Overlay_Conditional_0_ng_container_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0);
  }
}
function Overlay_Conditional_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵprojection(0);
    ɵɵtemplate(1, Overlay_Conditional_0_ng_container_1_Template, 1, 0, "ng-container", 2);
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵproperty("ngTemplateOutlet", ctx_r0.contentTemplate())("ngTemplateOutletContext", ɵɵpureFunction1(3, _c4, ɵɵpureFunction0(2, _c3)));
  }
}
function Overlay_Conditional_1_Conditional_0_ng_container_6_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0);
  }
}
function Overlay_Conditional_1_Conditional_0_Template(rf, ctx) {
  if (rf & 1) {
    const _r2 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div", 4, 0);
    ɵɵlistener("click", function Overlay_Conditional_1_Conditional_0_Template_div_click_0_listener() {
      ɵɵrestoreView(_r2);
      const ctx_r0 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r0.onOverlayClick());
    });
    ɵɵelementStart(2, "p-motion", 5);
    ɵɵlistener("onBeforeEnter", function Overlay_Conditional_1_Conditional_0_Template_p_motion_onBeforeEnter_2_listener($event) {
      ɵɵrestoreView(_r2);
      const ctx_r0 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r0.onOverlayBeforeEnter($event));
    })("onEnter", function Overlay_Conditional_1_Conditional_0_Template_p_motion_onEnter_2_listener($event) {
      ɵɵrestoreView(_r2);
      const ctx_r0 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r0.onOverlayEnter($event));
    })("onAfterEnter", function Overlay_Conditional_1_Conditional_0_Template_p_motion_onAfterEnter_2_listener($event) {
      ɵɵrestoreView(_r2);
      const ctx_r0 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r0.onOverlayAfterEnter($event));
    })("onBeforeLeave", function Overlay_Conditional_1_Conditional_0_Template_p_motion_onBeforeLeave_2_listener($event) {
      ɵɵrestoreView(_r2);
      const ctx_r0 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r0.onOverlayBeforeLeave($event));
    })("onLeave", function Overlay_Conditional_1_Conditional_0_Template_p_motion_onLeave_2_listener($event) {
      ɵɵrestoreView(_r2);
      const ctx_r0 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r0.onOverlayLeave($event));
    })("onAfterLeave", function Overlay_Conditional_1_Conditional_0_Template_p_motion_onAfterLeave_2_listener($event) {
      ɵɵrestoreView(_r2);
      const ctx_r0 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r0.onOverlayAfterLeave($event));
    });
    ɵɵelementStart(3, "div", 4, 1);
    ɵɵlistener("click", function Overlay_Conditional_1_Conditional_0_Template_div_click_3_listener($event) {
      ɵɵrestoreView(_r2);
      const ctx_r0 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r0.onOverlayContentClick($event));
    });
    ɵɵprojection(5, 1);
    ɵɵtemplate(6, Overlay_Conditional_1_Conditional_0_ng_container_6_Template, 1, 0, "ng-container", 2);
    ɵɵelementEnd()()();
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext(2);
    ɵɵstyleMap(ctx_r0.sx("root"));
    ɵɵclassMap(ctx_r0.cn(ctx_r0.cx("root"), ctx_r0.mergedStyleClass()));
    ɵɵproperty("pBind", ctx_r0.ptm("root"));
    ɵɵadvance(2);
    ɵɵproperty("visible", ctx_r0.visible())("appear", true)("options", ctx_r0.computedMotionOptions());
    ɵɵadvance();
    ɵɵstyleMap(ctx_r0.sx("content"));
    ɵɵclassMap(ctx_r0.cn(ctx_r0.cx("content"), ctx_r0.mergedContentStyleClass()));
    ɵɵproperty("pBind", ctx_r0.ptm("content"));
    ɵɵadvance(3);
    ɵɵproperty("ngTemplateOutlet", ctx_r0.contentTemplate())("ngTemplateOutletContext", ɵɵpureFunction1(17, _c4, ɵɵpureFunction1(15, _c5, ctx_r0.overlayMode())));
  }
}
function Overlay_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵconditionalCreate(0, Overlay_Conditional_1_Conditional_0_Template, 7, 19, "div", 3);
  }
  if (rf & 2) {
    const ctx_r0 = ɵɵnextContext();
    ɵɵconditional(ctx_r0.modalVisible() ? 0 : -1);
  }
}
var inlineStyles2 = {
  root: ({
    instance
  }) => {
    const modal = instance.modal();
    const responsiveStyle = modal ? instance.$overlayResponsiveOptions()?.style : instance.$overlayOptions()?.style;
    return __spreadValues(__spreadValues({
      position: "absolute",
      top: "0"
    }, responsiveStyle), instance.style());
  },
  content: ({
    instance
  }) => {
    const modal = instance.modal();
    const responsiveContentStyle = modal ? instance.$overlayResponsiveOptions()?.contentStyle : instance.$overlayOptions()?.contentStyle;
    return __spreadValues(__spreadValues({}, responsiveContentStyle), instance.contentStyle());
  }
};
var style2 = (
  /*css*/
  `
.p-overlay-modal {
    display: flex;
    align-items: center;
    justify-content: center;
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
}

.p-overlay-content {
    transform-origin: inherit;
    will-change: transform;
}

/* Github Issue #18560 */
.p-component-overlay.p-component {
    position: relative;
}

.p-overlay-modal > .p-overlay-content {
    z-index: 1;
    width: 90%;
}

/* Position */
/* top */
.p-overlay-top {
    align-items: flex-start;
}
.p-overlay-top-start {
    align-items: flex-start;
    justify-content: flex-start;
}
.p-overlay-top-end {
    align-items: flex-start;
    justify-content: flex-end;
}

/* bottom */
.p-overlay-bottom {
    align-items: flex-end;
}
.p-overlay-bottom-start {
    align-items: flex-end;
    justify-content: flex-start;
}
.p-overlay-bottom-end {
    align-items: flex-end;
    justify-content: flex-end;
}

/* left */
.p-overlay-left {
    justify-content: flex-start;
}
.p-overlay-left-start {
    justify-content: flex-start;
    align-items: flex-start;
}
.p-overlay-left-end {
    justify-content: flex-start;
    align-items: flex-end;
}

/* right */
.p-overlay-right {
    justify-content: flex-end;
}
.p-overlay-right-start {
    justify-content: flex-end;
    align-items: flex-start;
}
.p-overlay-right-end {
    justify-content: flex-end;
    align-items: flex-end;
}

.p-overlay-content ~ .p-overlay-content {
    display: none;
}
`
);
var classes2 = {
  host: "p-overlay-host",
  root: ({
    instance
  }) => {
    const modal = instance.modal();
    const dir = instance.overlayResponsiveDirection();
    return ["p-overlay p-component", {
      "p-overlay-modal p-overlay-mask p-overlay-mask-enter-active": modal,
      "p-overlay-center": modal && dir === "center",
      "p-overlay-top": modal && dir === "top",
      "p-overlay-top-start": modal && dir === "top-start",
      "p-overlay-top-end": modal && dir === "top-end",
      "p-overlay-bottom": modal && dir === "bottom",
      "p-overlay-bottom-start": modal && dir === "bottom-start",
      "p-overlay-bottom-end": modal && dir === "bottom-end",
      "p-overlay-left": modal && dir === "left",
      "p-overlay-left-start": modal && dir === "left-start",
      "p-overlay-left-end": modal && dir === "left-end",
      "p-overlay-right": modal && dir === "right",
      "p-overlay-right-start": modal && dir === "right-start",
      "p-overlay-right-end": modal && dir === "right-end"
    }];
  },
  content: "p-overlay-content"
};
var OverlayStyle = class _OverlayStyle extends BaseStyle {
  name = "overlay";
  style = style2;
  classes = classes2;
  inlineStyles = inlineStyles2;
  static ɵfac = /* @__PURE__ */ (() => {
    let ɵOverlayStyle_BaseFactory;
    return function OverlayStyle_Factory(__ngFactoryType__) {
      return (ɵOverlayStyle_BaseFactory || (ɵOverlayStyle_BaseFactory = ɵɵgetInheritedFactory(_OverlayStyle)))(__ngFactoryType__ || _OverlayStyle);
    };
  })();
  static ɵprov = ɵɵdefineInjectable({
    token: _OverlayStyle,
    factory: _OverlayStyle.ɵfac
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(OverlayStyle, [{
    type: Injectable
  }], null, null);
})();
var OVERLAY_INSTANCE = new InjectionToken("OVERLAY_INSTANCE");
var Overlay = class _Overlay extends BaseComponent {
  componentName = "Overlay";
  $pcOverlay = inject(OVERLAY_INSTANCE, {
    optional: true,
    skipSelf: true
  }) ?? void 0;
  hostName = input(
    "",
    ...ngDevMode ? [{
      debugName: "hostName"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  /**
   * The visible property is an input that determines the visibility of the component.
   * @defaultValue false
   * @group Props
   */
  visible = model(
    false,
    ...ngDevMode ? [{
      debugName: "visible"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  /**
   * The mode property is an input that determines the overlay mode type or string.
   * @defaultValue null
   * @group Props
   */
  mode = input(
    ...ngDevMode ? [void 0, {
      debugName: "mode"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  /**
   * The style property is an input that determines the style object for the component.
   * @defaultValue null
   * @group Props
   */
  style = input(
    ...ngDevMode ? [void 0, {
      debugName: "style"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  /**
   * The styleClass property is an input that determines the CSS class(es) for the component.
   * @defaultValue null
   * @group Props
   */
  styleClass = input(
    ...ngDevMode ? [void 0, {
      debugName: "styleClass"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  /**
   * The contentStyle property is an input that determines the style object for the content of the component.
   * @defaultValue null
   * @group Props
   */
  contentStyle = input(
    ...ngDevMode ? [void 0, {
      debugName: "contentStyle"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  /**
   * The contentStyleClass property is an input that determines the CSS class(es) for the content of the component.
   * @defaultValue null
   * @group Props
   */
  contentStyleClass = input(
    ...ngDevMode ? [void 0, {
      debugName: "contentStyleClass"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  /**
   * The target property is an input that specifies the target element or selector for the component.
   * @defaultValue null
   * @group Props
   */
  target = input(
    ...ngDevMode ? [void 0, {
      debugName: "target"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  /**
   * The autoZIndex determines whether to automatically manage layering. Its default value is 'false'.
   * @defaultValue false
   * @group Props
   */
  autoZIndex = input(
    ...ngDevMode ? [void 0, {
      debugName: "autoZIndex"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  /**
   * The baseZIndex is base zIndex value to use in layering.
   * @defaultValue null
   * @group Props
   */
  baseZIndex = input(
    ...ngDevMode ? [void 0, {
      debugName: "baseZIndex"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  /**
   * The listener property is an input that specifies the listener object for the component.
   * @defaultValue null
   * @group Props
   */
  listener = input(
    ...ngDevMode ? [void 0, {
      debugName: "listener"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  /**
   * It is the option used to determine in which mode it should appear according to the given media or breakpoint.
   * @defaultValue null
   * @group Props
   */
  responsive = input(
    ...ngDevMode ? [void 0, {
      debugName: "responsive"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  /**
   * The options property is an input that specifies the overlay options for the component.
   * @defaultValue null
   * @group Props
   */
  options = input(
    ...ngDevMode ? [void 0, {
      debugName: "options"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  /**
   * Target element to attach the overlay, valid values are "body" or a local ng-template variable of another element (note: use binding with brackets for template variables, e.g. [appendTo]="mydiv" for a div element having #mydiv as variable name).
   * @defaultValue 'self'
   * @group Props
   */
  appendTo = input(
    void 0,
    ...ngDevMode ? [{
      debugName: "appendTo"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  /**
   * Specifies whether the overlay should be rendered inline within the current component's template.
   * @defaultValue false
   * @group Props
   */
  inline = input(
    false,
    ...ngDevMode ? [{
      debugName: "inline"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  /**
   * The motion options.
   * @group Props
   */
  motionOptions = input(
    void 0,
    ...ngDevMode ? [{
      debugName: "motionOptions"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  /**
   * Callback to invoke before the overlay is shown.
   * @param {OverlayOnBeforeShowEvent} event - Custom overlay before show event.
   * @group Emits
   */
  onBeforeShow = output();
  /**
   * Callback to invoke when the overlay is shown.
   * @param {OverlayOnShowEvent} event - Custom overlay show event.
   * @group Emits
   */
  onShow = output();
  /**
   * Callback to invoke before the overlay is hidden.
   * @param {OverlayOnBeforeHideEvent} event - Custom overlay before hide event.
   * @group Emits
   */
  onBeforeHide = output();
  /**
   * Callback to invoke when the overlay is hidden
   * @param {OverlayOnHideEvent} event - Custom hide event.
   * @group Emits
   */
  onHide = output();
  /**
   * Callback to invoke when the animation is started.
   * @param {AnimationEvent} event - Animation event.
   * @group Emits
   * @deprecated since v21.0.0. Use onOverlayBeforeEnter and onOverlayBeforeLeave instead.
   */
  onAnimationStart = output();
  /**
   * Callback to invoke when the animation is done.
   * @param {AnimationEvent} event - Animation event.
   * @group Emits
   * @deprecated since v21.0.0. Use onOverlayAfterEnter and onOverlayAfterLeave instead.
   */
  onAnimationDone = output();
  /**
   * Callback to invoke before the overlay enters.
   * @param {MotionEvent} event - Event before enter.
   * @group Emits
   */
  onBeforeEnter = output();
  /**
   * Callback to invoke when the overlay enters.
   * @param {MotionEvent} event - Event on enter.
   * @group Emits
   */
  onEnter = output();
  /**
   * Callback to invoke after the overlay has entered.
   * @param {MotionEvent} event - Event after enter.
   * @group Emits
   */
  onAfterEnter = output();
  /**
   * Callback to invoke before the overlay leaves.
   * @param {MotionEvent} event - Event before leave.
   * @group Emits
   */
  onBeforeLeave = output();
  /**
   * Callback to invoke when the overlay leaves.
   * @param {MotionEvent} event - Event on leave.
   * @group Emits
   */
  onLeave = output();
  /**
   * Callback to invoke after the overlay has left.
   * @param {MotionEvent} event - Event after leave.
   * @group Emits
   */
  onAfterLeave = output();
  overlayViewChild = viewChild(
    "overlay",
    ...ngDevMode ? [{
      debugName: "overlayViewChild"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  contentViewChild = viewChild(
    "content",
    ...ngDevMode ? [{
      debugName: "contentViewChild"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  /**
   * Content template of the component.
   * @param {OverlayContentTemplateContext} context - content context.
   * @see {@link OverlayContentTemplateContext}
   * @group Templates
   */
  contentTemplate = contentChild("content", __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "contentTemplate"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    descendants: false
  }));
  hostAttrSelector = input(
    ...ngDevMode ? [void 0, {
      debugName: "hostAttrSelector"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  $appendTo = computed(
    () => this.appendTo() || this.config.overlayAppendTo(),
    ...ngDevMode ? [{
      debugName: "$appendTo"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  $overlayOptions = computed(
    () => {
      return __spreadValues(__spreadValues({}, this.config?.overlayOptions), this.options());
    },
    ...ngDevMode ? [{
      debugName: "$overlayOptions"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  $overlayResponsiveOptions = computed(
    () => {
      return __spreadValues(__spreadValues({}, this.$overlayOptions()?.responsive), this.responsive());
    },
    ...ngDevMode ? [{
      debugName: "$overlayResponsiveOptions"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  overlayResponsiveDirection = computed(
    () => this.$overlayResponsiveOptions()?.direction || "center",
    ...ngDevMode ? [{
      debugName: "overlayResponsiveDirection"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  $mode = computed(
    () => this.mode() || this.$overlayOptions()?.mode,
    ...ngDevMode ? [{
      debugName: "$mode"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  mergedStyleClass = computed(
    () => this.cn(this.styleClass(), this.modal() ? this.$overlayResponsiveOptions()?.styleClass : this.$overlayOptions()?.styleClass),
    ...ngDevMode ? [{
      debugName: "mergedStyleClass"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  mergedContentStyleClass = computed(
    () => this.cn(this.contentStyleClass(), this.modal() ? this.$overlayResponsiveOptions()?.contentStyleClass : this.$overlayOptions()?.contentStyleClass),
    ...ngDevMode ? [{
      debugName: "mergedContentStyleClass"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  $target = computed(
    () => {
      const value = this.target() || this.$overlayOptions()?.target;
      return value === void 0 ? "@prev" : value;
    },
    ...ngDevMode ? [{
      debugName: "$target"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  $autoZIndex = computed(
    () => {
      const value = this.autoZIndex() || this.$overlayOptions()?.autoZIndex;
      return value === void 0 ? true : value;
    },
    ...ngDevMode ? [{
      debugName: "$autoZIndex"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  $baseZIndex = computed(
    () => {
      const value = this.baseZIndex() || this.$overlayOptions()?.baseZIndex;
      return value === void 0 ? 0 : value;
    },
    ...ngDevMode ? [{
      debugName: "$baseZIndex"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  $listener = computed(
    () => this.listener() || this.$overlayOptions()?.listener,
    ...ngDevMode ? [{
      debugName: "$listener"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  modal = computed(
    () => {
      if (isPlatformBrowser(this.platformId)) {
        return this.$mode() === "modal" || this.$overlayResponsiveOptions() && this.document.defaultView?.matchMedia(this.$overlayResponsiveOptions().media?.replace("@media", "") || `(max-width: ${this.$overlayResponsiveOptions().breakpoint})`).matches;
      }
      return void 0;
    },
    ...ngDevMode ? [{
      debugName: "modal"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  overlayMode = computed(
    () => this.$mode() || (this.modal() ? "modal" : "overlay"),
    ...ngDevMode ? [{
      debugName: "overlayMode"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  overlayEl = computed(
    () => this.overlayViewChild()?.nativeElement,
    ...ngDevMode ? [{
      debugName: "overlayEl"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  contentEl = computed(
    () => this.contentViewChild()?.nativeElement,
    ...ngDevMode ? [{
      debugName: "contentEl"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  targetEl = computed(
    () => Z(this.$target(), this.el?.nativeElement),
    ...ngDevMode ? [{
      debugName: "targetEl"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  computedMotionOptions = computed(
    () => {
      return __spreadValues(__spreadValues({}, this.ptm("motion")), this.motionOptions() || this.$overlayOptions()?.motionOptions);
    },
    ...ngDevMode ? [{
      debugName: "computedMotionOptions"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  modalVisible = signal(
    false,
    ...ngDevMode ? [{
      debugName: "modalVisible"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  isOverlayClicked = false;
  isOverlayContentClicked = false;
  scrollHandler;
  documentClickListener;
  documentResizeListener;
  _componentStyle = inject(OverlayStyle);
  bindDirectiveInstance = inject(Bind, {
    self: true
  });
  documentKeyboardListener;
  parentDragSubscription = null;
  transformOptions = {
    default: "scaleY(0.8)",
    center: "scale(0.7)",
    top: "translate3d(0px, -100%, 0px)",
    "top-start": "translate3d(0px, -100%, 0px)",
    "top-end": "translate3d(0px, -100%, 0px)",
    bottom: "translate3d(0px, 100%, 0px)",
    "bottom-start": "translate3d(0px, 100%, 0px)",
    "bottom-end": "translate3d(0px, 100%, 0px)",
    left: "translate3d(-100%, 0px, 0px)",
    "left-start": "translate3d(-100%, 0px, 0px)",
    "left-end": "translate3d(-100%, 0px, 0px)",
    right: "translate3d(100%, 0px, 0px)",
    "right-start": "translate3d(100%, 0px, 0px)",
    "right-end": "translate3d(100%, 0px, 0px)"
  };
  overlayService = inject(OverlayService);
  constructor() {
    super();
    effect(() => {
      const v = this.visible();
      if (v && !this.modalVisible()) {
        this.modalVisible.set(true);
      }
    });
  }
  onAfterViewChecked() {
    this.bindDirectiveInstance.setAttrs(this.ptm("host"));
  }
  show(overlay, isFocus = false) {
    this.onVisibleChange(true);
    this.handleEvents("onShow", {
      overlay: overlay || this.overlayEl(),
      target: this.targetEl(),
      mode: this.overlayMode()
    });
    isFocus && Ht(this.targetEl());
    this.modal() && R(this.document?.body, "p-overflow-hidden");
  }
  hide(overlay, isFocus = false) {
    if (!this.visible()) {
      return;
    } else {
      this.onVisibleChange(false);
      this.handleEvents("onHide", {
        overlay: overlay || this.overlayEl(),
        target: this.targetEl(),
        mode: this.overlayMode()
      });
      isFocus && Ht(this.targetEl());
      this.modal() && W(this.document?.body, "p-overflow-hidden");
    }
  }
  onVisibleChange(visible) {
    this.visible.set(visible);
  }
  onOverlayClick() {
    this.isOverlayClicked = true;
  }
  onOverlayContentClick(event) {
    this.overlayService.add({
      originalEvent: event,
      target: this.targetEl()
    });
    this.isOverlayContentClicked = true;
  }
  container = signal(
    void 0,
    ...ngDevMode ? [{
      debugName: "container"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  onOverlayBeforeEnter(event) {
    this.handleEvents("onBeforeShow", {
      overlay: this.overlayEl(),
      target: this.targetEl(),
      mode: this.overlayMode()
    });
    this.container.set(this.overlayEl() || event.element);
    this.show(this.overlayEl(), true);
    this.hostAttrSelector() && this.overlayEl() && this.overlayEl().setAttribute(this.hostAttrSelector(), "");
    this.appendOverlay();
    this.alignOverlay();
    this.bindParentDragListener();
    this.setZIndex();
    this.handleEvents("onBeforeEnter", event);
  }
  onOverlayEnter(event) {
    this.handleEvents("onEnter", event);
  }
  onOverlayAfterEnter(event) {
    this.bindListeners();
    this.handleEvents("onAfterEnter", event);
  }
  onOverlayBeforeLeave(event) {
    this.handleEvents("onBeforeHide", {
      overlay: this.overlayEl(),
      target: this.targetEl(),
      mode: this.overlayMode()
    });
    this.handleEvents("onBeforeLeave", event);
  }
  onOverlayLeave(event) {
    this.handleEvents("onLeave", event);
  }
  onOverlayAfterLeave(event) {
    this.hide(this.overlayEl(), true);
    this.container.set(null);
    this.unbindListeners();
    this.appendOverlay();
    zindexutils.clear(this.overlayEl());
    this.modalVisible.set(false);
    this.cd.markForCheck();
    this.handleEvents("onAfterLeave", event);
  }
  handleEvents(name, params) {
    this[name].emit(params);
    const opts = this.options();
    opts && opts[name] && opts[name](params);
    this.config?.overlayOptions && (this.config?.overlayOptions)[name] && (this.config?.overlayOptions)[name](params);
  }
  setZIndex() {
    if (this.$autoZIndex()) {
      zindexutils.set(this.overlayMode(), this.overlayEl(), this.$baseZIndex() + this.config?.zIndex[this.overlayMode()]);
    }
  }
  appendOverlay() {
    if (this.$appendTo() && this.$appendTo() !== "self") {
      if (this.$appendTo() === "body") {
        wt(this.document.body, this.overlayEl());
      } else {
        wt(this.$appendTo(), this.overlayEl());
      }
    }
  }
  alignOverlay() {
    if (!this.modal()) {
      if (this.overlayEl() && this.targetEl()) {
        this.overlayEl().style.minWidth = C(this.targetEl()) + "px";
        if (this.$appendTo() === "self") {
          Y(this.overlayEl(), this.targetEl());
        } else {
          z2(this.overlayEl(), this.targetEl());
        }
      }
    }
  }
  bindListeners() {
    this.bindScrollListener();
    this.bindDocumentClickListener();
    this.bindDocumentResizeListener();
    this.bindDocumentKeyboardListener();
  }
  unbindListeners() {
    this.unbindScrollListener();
    this.unbindDocumentClickListener();
    this.unbindDocumentResizeListener();
    this.unbindDocumentKeyboardListener();
    this.unbindParentDragListener();
  }
  bindParentDragListener() {
    if (!this.parentDragSubscription && this.$appendTo() !== "self" && this.targetEl) {
      this.parentDragSubscription = this.overlayService.parentDragObservable.subscribe((container) => {
        if (container.contains(this.targetEl())) {
          this.hide(this.overlayEl(), true);
        }
      });
    }
  }
  unbindParentDragListener() {
    if (this.parentDragSubscription) {
      this.parentDragSubscription.unsubscribe();
      this.parentDragSubscription = null;
    }
  }
  bindScrollListener() {
    if (!this.scrollHandler) {
      this.scrollHandler = new ConnectedOverlayScrollHandler(this.targetEl(), (event) => {
        const valid = this.$listener() ? this.$listener()(event, {
          type: "scroll",
          mode: this.overlayMode(),
          valid: true
        }) : true;
        valid && this.hide(event, true);
      });
    }
    this.scrollHandler.bindScrollListener();
  }
  unbindScrollListener() {
    if (this.scrollHandler) {
      this.scrollHandler.unbindScrollListener();
    }
  }
  bindDocumentClickListener() {
    if (!this.documentClickListener) {
      this.documentClickListener = this.renderer.listen(this.document, "click", (event) => {
        const isTargetClicked = this.targetEl() && (this.targetEl().isSameNode(event.target) || !this.isOverlayClicked && this.targetEl().contains(event.target));
        const isOutsideClicked = !isTargetClicked && !this.isOverlayContentClicked;
        const valid = this.$listener() ? this.$listener()(event, {
          type: "outside",
          mode: this.overlayMode(),
          valid: event.which !== 3 && isOutsideClicked
        }) : isOutsideClicked;
        valid && this.hide(event);
        this.isOverlayClicked = this.isOverlayContentClicked = false;
      });
    }
  }
  unbindDocumentClickListener() {
    if (this.documentClickListener) {
      this.documentClickListener();
      this.documentClickListener = null;
    }
  }
  bindDocumentResizeListener() {
    if (!this.documentResizeListener) {
      this.documentResizeListener = this.renderer.listen(this.document.defaultView, "resize", (event) => {
        const valid = this.$listener() ? this.$listener()(event, {
          type: "resize",
          mode: this.overlayMode(),
          valid: !oe()
        }) : !oe();
        valid && this.hide(event, true);
      });
    }
  }
  unbindDocumentResizeListener() {
    if (this.documentResizeListener) {
      this.documentResizeListener();
      this.documentResizeListener = null;
    }
  }
  bindDocumentKeyboardListener() {
    if (this.documentKeyboardListener) {
      return;
    }
    this.documentKeyboardListener = this.renderer.listen(this.document.defaultView, "keydown", (event) => {
      if (this.$overlayOptions().hideOnEscape === false || event.code !== "Escape") {
        return;
      }
      const valid = this.$listener() ? this.$listener()(event, {
        type: "keydown",
        mode: this.overlayMode(),
        valid: !oe()
      }) : !oe();
      if (valid) {
        this.hide(event, true);
      }
    });
  }
  unbindDocumentKeyboardListener() {
    if (this.documentKeyboardListener) {
      this.documentKeyboardListener();
      this.documentKeyboardListener = null;
    }
  }
  onDestroy() {
    this.hide(this.overlayEl(), true);
    if (this.overlayEl() && this.$appendTo() !== "self") {
      this.renderer.appendChild(this.el.nativeElement, this.overlayEl());
      zindexutils.clear(this.overlayEl());
    }
    if (this.scrollHandler) {
      this.scrollHandler.destroy();
      this.scrollHandler = null;
    }
    this.unbindListeners();
  }
  static ɵfac = function Overlay_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _Overlay)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _Overlay,
    selectors: [["p-overlay"]],
    contentQueries: function Overlay_ContentQueries(rf, ctx, dirIndex) {
      if (rf & 1) {
        ɵɵcontentQuerySignal(dirIndex, ctx.contentTemplate, _c02, 4);
      }
      if (rf & 2) {
        ɵɵqueryAdvance();
      }
    },
    viewQuery: function Overlay_Query(rf, ctx) {
      if (rf & 1) {
        ɵɵviewQuerySignal(ctx.overlayViewChild, _c12, 5)(ctx.contentViewChild, _c02, 5);
      }
      if (rf & 2) {
        ɵɵqueryAdvance(2);
      }
    },
    inputs: {
      hostName: [1, "hostName"],
      visible: [1, "visible"],
      mode: [1, "mode"],
      style: [1, "style"],
      styleClass: [1, "styleClass"],
      contentStyle: [1, "contentStyle"],
      contentStyleClass: [1, "contentStyleClass"],
      target: [1, "target"],
      autoZIndex: [1, "autoZIndex"],
      baseZIndex: [1, "baseZIndex"],
      listener: [1, "listener"],
      responsive: [1, "responsive"],
      options: [1, "options"],
      appendTo: [1, "appendTo"],
      inline: [1, "inline"],
      motionOptions: [1, "motionOptions"],
      hostAttrSelector: [1, "hostAttrSelector"]
    },
    outputs: {
      visible: "visibleChange",
      onBeforeShow: "onBeforeShow",
      onShow: "onShow",
      onBeforeHide: "onBeforeHide",
      onHide: "onHide",
      onAnimationStart: "onAnimationStart",
      onAnimationDone: "onAnimationDone",
      onBeforeEnter: "onBeforeEnter",
      onEnter: "onEnter",
      onAfterEnter: "onAfterEnter",
      onBeforeLeave: "onBeforeLeave",
      onLeave: "onLeave",
      onAfterLeave: "onAfterLeave"
    },
    features: [ɵɵProvidersFeature([OverlayStyle, {
      provide: OVERLAY_INSTANCE,
      useExisting: _Overlay
    }, {
      provide: PARENT_INSTANCE,
      useExisting: _Overlay
    }]), ɵɵHostDirectivesFeature([Bind]), ɵɵInheritDefinitionFeature],
    ngContentSelectors: _c2,
    decls: 2,
    vars: 1,
    consts: [["overlay", ""], ["content", ""], [4, "ngTemplateOutlet", "ngTemplateOutletContext"], [3, "class", "style", "pBind"], [3, "click", "pBind"], ["name", "p-anchored-overlay", 3, "onBeforeEnter", "onEnter", "onAfterEnter", "onBeforeLeave", "onLeave", "onAfterLeave", "visible", "appear", "options"]],
    template: function Overlay_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵprojectionDef(_c2);
        ɵɵconditionalCreate(0, Overlay_Conditional_0_Template, 2, 5)(1, Overlay_Conditional_1_Template, 1, 1);
      }
      if (rf & 2) {
        ɵɵconditional(ctx.inline() ? 0 : 1);
      }
    },
    dependencies: [NgTemplateOutlet, SharedModule, Bind, MotionModule, Motion],
    encapsulation: 2
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(Overlay, [{
    type: Component,
    args: [{
      selector: "p-overlay",
      standalone: true,
      imports: [NgTemplateOutlet, SharedModule, Bind, MotionModule],
      hostDirectives: [Bind],
      template: `
        @if (inline()) {
            <ng-content />
            <ng-container *ngTemplateOutlet="contentTemplate(); context: { $implicit: { mode: null } }" />
        } @else {
            @if (modalVisible()) {
                <div #overlay [class]="cn(cx('root'), mergedStyleClass())" [style]="sx('root')" [pBind]="ptm('root')" (click)="onOverlayClick()">
                    <p-motion
                        [visible]="visible()"
                        name="p-anchored-overlay"
                        [appear]="true"
                        [options]="computedMotionOptions()"
                        (onBeforeEnter)="onOverlayBeforeEnter($event)"
                        (onEnter)="onOverlayEnter($event)"
                        (onAfterEnter)="onOverlayAfterEnter($event)"
                        (onBeforeLeave)="onOverlayBeforeLeave($event)"
                        (onLeave)="onOverlayLeave($event)"
                        (onAfterLeave)="onOverlayAfterLeave($event)"
                    >
                        <div #content [class]="cn(cx('content'), mergedContentStyleClass())" [style]="sx('content')" [pBind]="ptm('content')" (click)="onOverlayContentClick($event)">
                            <ng-content />
                            <ng-container *ngTemplateOutlet="contentTemplate(); context: { $implicit: { mode: overlayMode() } }" />
                        </div>
                    </p-motion>
                </div>
            }
        }
    `,
      changeDetection: ChangeDetectionStrategy.OnPush,
      encapsulation: ViewEncapsulation.None,
      providers: [OverlayStyle, {
        provide: OVERLAY_INSTANCE,
        useExisting: Overlay
      }, {
        provide: PARENT_INSTANCE,
        useExisting: Overlay
      }]
    }]
  }], () => [], {
    hostName: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "hostName",
        required: false
      }]
    }],
    visible: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "visible",
        required: false
      }]
    }, {
      type: Output,
      args: ["visibleChange"]
    }],
    mode: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "mode",
        required: false
      }]
    }],
    style: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "style",
        required: false
      }]
    }],
    styleClass: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "styleClass",
        required: false
      }]
    }],
    contentStyle: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "contentStyle",
        required: false
      }]
    }],
    contentStyleClass: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "contentStyleClass",
        required: false
      }]
    }],
    target: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "target",
        required: false
      }]
    }],
    autoZIndex: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "autoZIndex",
        required: false
      }]
    }],
    baseZIndex: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "baseZIndex",
        required: false
      }]
    }],
    listener: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "listener",
        required: false
      }]
    }],
    responsive: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "responsive",
        required: false
      }]
    }],
    options: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "options",
        required: false
      }]
    }],
    appendTo: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "appendTo",
        required: false
      }]
    }],
    inline: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "inline",
        required: false
      }]
    }],
    motionOptions: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "motionOptions",
        required: false
      }]
    }],
    onBeforeShow: [{
      type: Output,
      args: ["onBeforeShow"]
    }],
    onShow: [{
      type: Output,
      args: ["onShow"]
    }],
    onBeforeHide: [{
      type: Output,
      args: ["onBeforeHide"]
    }],
    onHide: [{
      type: Output,
      args: ["onHide"]
    }],
    onAnimationStart: [{
      type: Output,
      args: ["onAnimationStart"]
    }],
    onAnimationDone: [{
      type: Output,
      args: ["onAnimationDone"]
    }],
    onBeforeEnter: [{
      type: Output,
      args: ["onBeforeEnter"]
    }],
    onEnter: [{
      type: Output,
      args: ["onEnter"]
    }],
    onAfterEnter: [{
      type: Output,
      args: ["onAfterEnter"]
    }],
    onBeforeLeave: [{
      type: Output,
      args: ["onBeforeLeave"]
    }],
    onLeave: [{
      type: Output,
      args: ["onLeave"]
    }],
    onAfterLeave: [{
      type: Output,
      args: ["onAfterLeave"]
    }],
    overlayViewChild: [{
      type: ViewChild,
      args: ["overlay", {
        isSignal: true
      }]
    }],
    contentViewChild: [{
      type: ViewChild,
      args: ["content", {
        isSignal: true
      }]
    }],
    contentTemplate: [{
      type: ContentChild,
      args: ["content", __spreadProps(__spreadValues({}, {
        descendants: false
      }), {
        isSignal: true
      })]
    }],
    hostAttrSelector: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "hostAttrSelector",
        required: false
      }]
    }]
  });
})();
var OverlayModule = class _OverlayModule {
  static ɵfac = function OverlayModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _OverlayModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _OverlayModule,
    imports: [Overlay, SharedModule],
    exports: [Overlay, SharedModule]
  });
  static ɵinj = ɵɵdefineInjector({
    imports: [Overlay, SharedModule, SharedModule]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(OverlayModule, [{
    type: NgModule,
    args: [{
      imports: [Overlay, SharedModule],
      exports: [Overlay, SharedModule]
    }]
  }], null, null);
})();

// node_modules/primeng/fesm2022/primeng-scroller.mjs
var _c03 = ["content"];
var _c13 = ["item"];
var _c22 = ["loader"];
var _c32 = ["loadericon"];
var _c42 = ["element"];
var _c52 = ["*"];
function _forTrack02($index, $item) {
  return this._trackBy() ? this._trackBy()($index, $item) : $index;
}
function Scroller_Conditional_0_Conditional_2_ng_container_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0);
  }
}
function Scroller_Conditional_0_Conditional_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵtemplate(0, Scroller_Conditional_0_Conditional_2_ng_container_0_Template, 1, 0, "ng-container", 6);
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵproperty("ngTemplateOutlet", ctx_r1.contentTemplate())("ngTemplateOutletContext", ctx_r1.getContentTemplateContext());
  }
}
function Scroller_Conditional_0_Conditional_3_For_3_ng_container_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0);
  }
}
function Scroller_Conditional_0_Conditional_3_For_3_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵtemplate(0, Scroller_Conditional_0_Conditional_3_For_3_ng_container_0_Template, 1, 0, "ng-container", 6);
  }
  if (rf & 2) {
    const item_r3 = ctx.$implicit;
    const ɵ$index_12_r4 = ctx.$index;
    const ctx_r1 = ɵɵnextContext(3);
    ɵɵproperty("ngTemplateOutlet", ctx_r1.itemTemplate())("ngTemplateOutletContext", ctx_r1.getItemTemplateContext(item_r3, ɵ$index_12_r4));
  }
}
function Scroller_Conditional_0_Conditional_3_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 7, 1);
    ɵɵrepeaterCreate(2, Scroller_Conditional_0_Conditional_3_For_3_Template, 1, 2, "ng-container", null, _forTrack02, true);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵstyleMap(ctx_r1.contentStyle);
    ɵɵclassMap(ctx_r1.cn(ctx_r1.cx("content"), ctx_r1.contentStyleClass()));
    ɵɵproperty("pBind", ctx_r1.ptm("content"));
    ɵɵadvance(2);
    ɵɵrepeater(ctx_r1.loadedItems);
  }
}
function Scroller_Conditional_0_Conditional_4_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "div", 7);
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵstyleMap(ctx_r1.spacerStyle);
    ɵɵclassMap(ctx_r1.cx("spacer"));
    ɵɵproperty("pBind", ctx_r1.ptm("spacer"));
  }
}
function Scroller_Conditional_0_Conditional_5_Conditional_1_For_1_ng_container_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0);
  }
}
function Scroller_Conditional_0_Conditional_5_Conditional_1_For_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵtemplate(0, Scroller_Conditional_0_Conditional_5_Conditional_1_For_1_ng_container_0_Template, 1, 0, "ng-container", 6);
  }
  if (rf & 2) {
    const ɵ$index_24_r5 = ctx.$index;
    const ctx_r1 = ɵɵnextContext(4);
    ɵɵproperty("ngTemplateOutlet", ctx_r1.loaderTemplate())("ngTemplateOutletContext", ctx_r1.getLoaderTemplateContext(ɵ$index_24_r5));
  }
}
function Scroller_Conditional_0_Conditional_5_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵrepeaterCreate(0, Scroller_Conditional_0_Conditional_5_Conditional_1_For_1_Template, 1, 2, "ng-container", null, ɵɵrepeaterTrackByIndex);
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(3);
    ɵɵrepeater(ctx_r1.loaderArr);
  }
}
function Scroller_Conditional_0_Conditional_5_Conditional_2_Conditional_0_ng_container_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0);
  }
}
function Scroller_Conditional_0_Conditional_5_Conditional_2_Conditional_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵtemplate(0, Scroller_Conditional_0_Conditional_5_Conditional_2_Conditional_0_ng_container_0_Template, 1, 0, "ng-container", 6);
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(4);
    ɵɵproperty("ngTemplateOutlet", ctx_r1.loaderIconTemplate())("ngTemplateOutletContext", ctx_r1.loaderIconContext);
  }
}
function Scroller_Conditional_0_Conditional_5_Conditional_2_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵnamespaceSVG();
    ɵɵelement(0, "svg", 9);
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(4);
    ɵɵclassMap(ctx_r1.cx("loadingIcon"));
    ɵɵproperty("spin", true)("pBind", ctx_r1.ptm("loadingIcon"));
  }
}
function Scroller_Conditional_0_Conditional_5_Conditional_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵconditionalCreate(0, Scroller_Conditional_0_Conditional_5_Conditional_2_Conditional_0_Template, 1, 2, "ng-container")(1, Scroller_Conditional_0_Conditional_5_Conditional_2_Conditional_1_Template, 1, 4, ":svg:svg", 8);
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(3);
    ɵɵconditional(ctx_r1.loaderIconTemplate() ? 0 : 1);
  }
}
function Scroller_Conditional_0_Conditional_5_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 7);
    ɵɵconditionalCreate(1, Scroller_Conditional_0_Conditional_5_Conditional_1_Template, 2, 0)(2, Scroller_Conditional_0_Conditional_5_Conditional_2_Template, 2, 1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵclassMap(ctx_r1.cx("loader"));
    ɵɵproperty("pBind", ctx_r1.ptm("loader"));
    ɵɵadvance();
    ɵɵconditional(ctx_r1.loaderTemplate() ? 1 : 2);
  }
}
function Scroller_Conditional_0_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "div", 3, 0);
    ɵɵlistener("scroll", function Scroller_Conditional_0_Template_div_scroll_0_listener($event) {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.onContainerScroll($event));
    });
    ɵɵconditionalCreate(2, Scroller_Conditional_0_Conditional_2_Template, 1, 2, "ng-container")(3, Scroller_Conditional_0_Conditional_3_Template, 4, 5, "div", 4);
    ɵɵconditionalCreate(4, Scroller_Conditional_0_Conditional_4_Template, 1, 5, "div", 4);
    ɵɵconditionalCreate(5, Scroller_Conditional_0_Conditional_5_Template, 3, 4, "div", 5);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵstyleMap(ctx_r1._style());
    ɵɵclassMap(ctx_r1.cn(ctx_r1.cx("root"), ctx_r1._styleClass()));
    ɵɵproperty("pBind", ctx_r1.ptm("root"));
    ɵɵattribute("id", ctx_r1._id())("tabindex", ctx_r1._tabindex());
    ɵɵadvance(2);
    ɵɵconditional(ctx_r1.contentTemplate() ? 2 : 3);
    ɵɵadvance(2);
    ɵɵconditional(ctx_r1._showSpacer() ? 4 : -1);
    ɵɵadvance();
    ɵɵconditional(!ctx_r1._loaderDisabled() && ctx_r1._showLoader() && ctx_r1.d_loading ? 5 : -1);
  }
}
function Scroller_Conditional_1_Conditional_1_ng_container_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0);
  }
}
function Scroller_Conditional_1_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵtemplate(0, Scroller_Conditional_1_Conditional_1_ng_container_0_Template, 1, 0, "ng-container", 6);
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵproperty("ngTemplateOutlet", ctx_r1.contentTemplate())("ngTemplateOutletContext", ctx_r1.getDisabledContentTemplateContext());
  }
}
function Scroller_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵprojection(0);
    ɵɵconditionalCreate(1, Scroller_Conditional_1_Conditional_1_Template, 1, 2, "ng-container");
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵadvance();
    ɵɵconditional(ctx_r1.contentTemplate() ? 1 : -1);
  }
}
var css = (
  /*css*/
  `
.p-virtualscroller {
    position: relative;
    overflow: auto;
    contain: strict;
    transform: translateZ(0);
    will-change: scroll-position;
    outline: 0 none;
}

.p-virtualscroller-content {
    position: absolute;
    top: 0;
    left: 0;
    min-height: 100%;
    min-width: 100%;
    will-change: transform;
}

.p-virtualscroller-spacer {
    position: absolute;
    top: 0;
    left: 0;
    height: 1px;
    width: 1px;
    transform-origin: 0 0;
    pointer-events: none;
}

.p-virtualscroller-loader {
    position: sticky;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    background: dt('virtualscroller.loader.mask.background');
    color: dt('virtualscroller.loader.mask.color');
}

.p-virtualscroller-loader-mask {
    display: flex;
    align-items: center;
    justify-content: center;
}

.p-virtualscroller-loading-icon {
    font-size: dt('virtualscroller.loader.icon.size');
    width: dt('virtualscroller.loader.icon.size');
    height: dt('virtualscroller.loader.icon.size');
}

.p-virtualscroller-horizontal > .p-virtualscroller-content {
    display: flex;
}

.p-virtualscroller-inline .p-virtualscroller-content {
    position: static;
}
`
);
var classes3 = {
  root: ({
    instance
  }) => ["p-virtualscroller", {
    "p-virtualscroller-inline": instance.inline(),
    "p-virtualscroller-both p-both-scroll": instance.both(),
    "p-virtualscroller-horizontal p-horizontal-scroll": instance.horizontal()
  }],
  content: "p-virtualscroller-content",
  spacer: "p-virtualscroller-spacer",
  loader: ({
    instance
  }) => ["p-virtualscroller-loader", {
    "p-virtualscroller-loader-mask": !instance.loaderTemplate()
  }],
  loadingIcon: "p-virtualscroller-loading-icon"
};
var ScrollerStyle = class _ScrollerStyle extends BaseStyle {
  name = "virtualscroller";
  css = css;
  classes = classes3;
  static ɵfac = /* @__PURE__ */ (() => {
    let ɵScrollerStyle_BaseFactory;
    return function ScrollerStyle_Factory(__ngFactoryType__) {
      return (ɵScrollerStyle_BaseFactory || (ɵScrollerStyle_BaseFactory = ɵɵgetInheritedFactory(_ScrollerStyle)))(__ngFactoryType__ || _ScrollerStyle);
    };
  })();
  static ɵprov = ɵɵdefineInjectable({
    token: _ScrollerStyle,
    factory: _ScrollerStyle.ɵfac
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(ScrollerStyle, [{
    type: Injectable
  }], null, null);
})();
var ScrollerClasses;
(function(ScrollerClasses2) {
  ScrollerClasses2["root"] = "p-virtualscroller";
  ScrollerClasses2["content"] = "p-virtualscroller-content";
  ScrollerClasses2["spacer"] = "p-virtualscroller-spacer";
  ScrollerClasses2["loader"] = "p-virtualscroller-loader";
  ScrollerClasses2["loadingIcon"] = "p-virtualscroller-loading-icon";
})(ScrollerClasses || (ScrollerClasses = {}));
var SCROLLER_INSTANCE = new InjectionToken("SCROLLER_INSTANCE");
var Scroller = class _Scroller extends BaseComponent {
  componentName = "VirtualScroller";
  bindDirectiveInstance = inject(Bind, {
    self: true
  });
  $pcScroller = inject(SCROLLER_INSTANCE, {
    optional: true,
    skipSelf: true
  }) ?? void 0;
  hostName = input(
    "",
    ...ngDevMode ? [{
      debugName: "hostName"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  /**
   * Unique identifier of the element.
   * @group Props
   */
  id = input(
    ...ngDevMode ? [void 0, {
      debugName: "id"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  /**
   * Inline style of the component.
   * @group Props
   */
  style = input(
    ...ngDevMode ? [void 0, {
      debugName: "style"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  /**
   * Style class of the element.
   * @group Props
   */
  styleClass = input(
    ...ngDevMode ? [void 0, {
      debugName: "styleClass"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  /**
   * Index of the element in tabbing order.
   * @group Props
   */
  tabindex = input(
    0,
    ...ngDevMode ? [{
      debugName: "tabindex"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  /**
   * An array of objects to display.
   * @group Props
   */
  items = input(
    ...ngDevMode ? [void 0, {
      debugName: "items"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  /**
   * The height/width of item according to orientation.
   * @group Props
   */
  itemSize = input(
    0,
    ...ngDevMode ? [{
      debugName: "itemSize"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  /**
   * Height of the scroll viewport.
   * @group Props
   */
  scrollHeight = input(
    ...ngDevMode ? [void 0, {
      debugName: "scrollHeight"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  /**
   * Width of the scroll viewport.
   * @group Props
   */
  scrollWidth = input(
    ...ngDevMode ? [void 0, {
      debugName: "scrollWidth"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  /**
   * The orientation of scrollbar.
   * @group Props
   */
  orientation = input(
    "vertical",
    ...ngDevMode ? [{
      debugName: "orientation"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  /**
   * Used to specify how many items to load in each load method in lazy mode.
   * @group Props
   */
  step = input(
    0,
    ...ngDevMode ? [{
      debugName: "step"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  /**
   * Delay in scroll before new data is loaded.
   * @group Props
   */
  delay = input(
    0,
    ...ngDevMode ? [{
      debugName: "delay"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  /**
   * Delay after window's resize finishes.
   * @group Props
   */
  resizeDelay = input(
    10,
    ...ngDevMode ? [{
      debugName: "resizeDelay"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  /**
   * Used to append each loaded item to top without removing any items from the DOM. Using very large data may cause the browser to crash.
   * @group Props
   */
  appendOnly = input(
    false,
    ...ngDevMode ? [{
      debugName: "appendOnly"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  /**
   * Specifies whether the scroller should be displayed inline or not.
   * @group Props
   */
  inline = input(
    false,
    ...ngDevMode ? [{
      debugName: "inline"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  /**
   * Defines if data is loaded and interacted with in lazy manner.
   * @group Props
   */
  lazy = input(
    false,
    ...ngDevMode ? [{
      debugName: "lazy"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  /**
   * If disabled, the scroller feature is eliminated and the content is displayed directly.
   * @group Props
   */
  disabled = input(
    false,
    ...ngDevMode ? [{
      debugName: "disabled"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  /**
   * Used to implement a custom loader instead of using the loader feature in the scroller.
   * @group Props
   */
  loaderDisabled = input(
    false,
    ...ngDevMode ? [{
      debugName: "loaderDisabled"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  /**
   * Columns to display.
   * @group Props
   */
  columns = input(
    ...ngDevMode ? [void 0, {
      debugName: "columns"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  /**
   * Used to implement a custom spacer instead of using the spacer feature in the scroller.
   * @group Props
   */
  showSpacer = input(
    true,
    ...ngDevMode ? [{
      debugName: "showSpacer"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  /**
   * Defines whether to show loader.
   * @group Props
   */
  showLoader = input(
    false,
    ...ngDevMode ? [{
      debugName: "showLoader"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  /**
   * Determines how many additional elements to add to the DOM outside of the view. According to the scrolls made up and down, extra items are added in a certain algorithm in the form of multiples of this number. Default value is half the number of items shown in the view.
   * @group Props
   */
  numToleratedItems = input(
    ...ngDevMode ? [void 0, {
      debugName: "numToleratedItems"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  /**
   * Defines whether the data is loaded.
   * @group Props
   */
  loading = input(
    ...ngDevMode ? [void 0, {
      debugName: "loading"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  /**
   * Defines whether to dynamically change the height or width of scrollable container.
   * @group Props
   */
  autoSize = input(
    false,
    ...ngDevMode ? [{
      debugName: "autoSize"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  /**
   * Function to optimize the dom operations by delegating to ngForTrackBy, default algoritm checks for object identity.
   * @group Props
   */
  trackBy = input(
    ...ngDevMode ? [void 0, {
      debugName: "trackBy"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  /**
   * Defines whether to use the scroller feature. The properties of scroller component can be used like an object in it.
   * @group Props
   */
  options = input(
    ...ngDevMode ? [void 0, {
      debugName: "options"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  // Computed: Options merge with individual inputs
  _id = computed(
    () => this.options()?.id ?? this.id(),
    ...ngDevMode ? [{
      debugName: "_id"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  _style = computed(
    () => this.options()?.style ?? this.style(),
    ...ngDevMode ? [{
      debugName: "_style"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  _styleClass = computed(
    () => this.options()?.styleClass ?? this.styleClass(),
    ...ngDevMode ? [{
      debugName: "_styleClass"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  _tabindex = computed(
    () => this.options()?.tabindex ?? this.tabindex(),
    ...ngDevMode ? [{
      debugName: "_tabindex"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  _items = computed(
    () => this.options()?.items ?? this.items(),
    ...ngDevMode ? [{
      debugName: "_items"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  _itemSize = computed(
    () => this.options()?.itemSize ?? this.itemSize(),
    ...ngDevMode ? [{
      debugName: "_itemSize"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  _scrollHeight = computed(
    () => this.options()?.scrollHeight ?? this.scrollHeight(),
    ...ngDevMode ? [{
      debugName: "_scrollHeight"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  _scrollWidth = computed(
    () => this.options()?.scrollWidth ?? this.scrollWidth(),
    ...ngDevMode ? [{
      debugName: "_scrollWidth"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  _orientation = computed(
    () => this.options()?.orientation ?? this.orientation(),
    ...ngDevMode ? [{
      debugName: "_orientation"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  _step = computed(
    () => this.options()?.step ?? this.step(),
    ...ngDevMode ? [{
      debugName: "_step"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  _delay = computed(
    () => this.options()?.delay ?? this.delay(),
    ...ngDevMode ? [{
      debugName: "_delay"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  _resizeDelay = computed(
    () => this.options()?.resizeDelay ?? this.resizeDelay(),
    ...ngDevMode ? [{
      debugName: "_resizeDelay"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  _appendOnly = computed(
    () => this.options()?.appendOnly ?? this.appendOnly(),
    ...ngDevMode ? [{
      debugName: "_appendOnly"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  _inline = computed(
    () => this.options()?.inline ?? this.inline(),
    ...ngDevMode ? [{
      debugName: "_inline"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  _lazy = computed(
    () => this.options()?.lazy ?? this.lazy(),
    ...ngDevMode ? [{
      debugName: "_lazy"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  _disabled = computed(
    () => this.options()?.disabled ?? this.disabled(),
    ...ngDevMode ? [{
      debugName: "_disabled"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  _loaderDisabled = computed(
    () => this.options()?.loaderDisabled ?? this.loaderDisabled(),
    ...ngDevMode ? [{
      debugName: "_loaderDisabled"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  _columns = computed(
    () => this.options()?.columns ?? this.columns(),
    ...ngDevMode ? [{
      debugName: "_columns"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  _showSpacer = computed(
    () => this.options()?.showSpacer ?? this.showSpacer(),
    ...ngDevMode ? [{
      debugName: "_showSpacer"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  _showLoader = computed(
    () => this.options()?.showLoader ?? this.showLoader(),
    ...ngDevMode ? [{
      debugName: "_showLoader"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  _numToleratedItems = computed(
    () => this.options()?.numToleratedItems ?? this.numToleratedItems(),
    ...ngDevMode ? [{
      debugName: "_numToleratedItems"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  _loading = computed(
    () => this.options()?.loading ?? this.loading(),
    ...ngDevMode ? [{
      debugName: "_loading"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  _autoSize = computed(
    () => this.options()?.autoSize ?? this.autoSize(),
    ...ngDevMode ? [{
      debugName: "_autoSize"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  _trackBy = computed(
    () => this.options()?.trackBy ?? this.trackBy(),
    ...ngDevMode ? [{
      debugName: "_trackBy"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  contentStyleClass = computed(
    () => this.options()?.contentStyleClass,
    ...ngDevMode ? [{
      debugName: "contentStyleClass"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  /**
   * Callback to invoke in lazy mode to load new data.
   * @param {ScrollerLazyLoadEvent} event - Custom lazy load event.
   * @group Emits
   */
  onLazyLoad = output();
  /**
   * Callback to invoke when scroll position changes.
   * @param {ScrollerScrollEvent} event - Custom scroll event.
   * @group Emits
   */
  onScroll = output();
  /**
   * Callback to invoke when scroll position and item's range in view changes.
   * @param {ScrollerScrollEvent} event - Custom scroll index change event.
   * @group Emits
   */
  onScrollIndexChange = output();
  elementViewChild = viewChild(
    "element",
    ...ngDevMode ? [{
      debugName: "elementViewChild"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  contentViewChild = viewChild(
    "content",
    ...ngDevMode ? [{
      debugName: "contentViewChild"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  hostHeight = signal(
    void 0,
    ...ngDevMode ? [{
      debugName: "hostHeight"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  /**
   * Content template of the component.
   * @param {ScrollerContentTemplateContext} context - content context.
   * @see {@link ScrollerContentTemplateContext}
   * @group Templates
   */
  contentTemplate = contentChild("content", __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "contentTemplate"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    descendants: false
  }));
  /**
   * Item template of the component.
   * @param {ScrollerItemTemplateContext} context - item context.
   * @see {@link ScrollerItemTemplateContext}
   * @group Templates
   */
  itemTemplate = contentChild("item", __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "itemTemplate"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    descendants: false
  }));
  /**
   * Loader template of the component.
   * @param {ScrollerLoaderTemplateContext} context - loader context.
   * @see {@link ScrollerLoaderTemplateContext}
   * @group Templates
   */
  loaderTemplate = contentChild("loader", __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "loaderTemplate"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    descendants: false
  }));
  /**
   * Loader icon template of the component.
   * @param {ScrollerLoaderIconTemplateContext} context - loader icon context.
   * @see {@link ScrollerLoaderIconTemplateContext}
   * @group Templates
   */
  loaderIconTemplate = contentChild("loadericon", __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "loaderIconTemplate"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    descendants: false
  }));
  d_loading = false;
  d_numToleratedItems;
  contentEl;
  vertical = computed(
    () => this._orientation() === "vertical",
    ...ngDevMode ? [{
      debugName: "vertical"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  horizontal = computed(
    () => this._orientation() === "horizontal",
    ...ngDevMode ? [{
      debugName: "horizontal"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  both = computed(
    () => this._orientation() === "both",
    ...ngDevMode ? [{
      debugName: "both"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  get loadedItems() {
    const items = this._items();
    if (items && !this.d_loading) {
      if (this.both()) {
        return items.slice(this._appendOnly() ? 0 : this.first.rows, this.last.rows).map((item) => {
          if (this._columns()) {
            return item;
          } else if (Array.isArray(item)) {
            return item.slice(this._appendOnly() ? 0 : this.first.cols, this.last.cols);
          } else {
            return item;
          }
        });
      } else if (this.horizontal() && this._columns()) return items;
      else return items.slice(this._appendOnly() ? 0 : this.first, this.last);
    }
    return [];
  }
  get loadedRows() {
    return this.d_loading ? this._loaderDisabled() ? this.loaderArr : [] : this.loadedItems;
  }
  get loadedColumns() {
    const columns = this._columns();
    if (columns && (this.both() || this.horizontal())) {
      return this.d_loading && this._loaderDisabled() ? this.both() ? this.loaderArr[0] : this.loaderArr : columns.slice(this.both() ? this.first.cols : this.first, this.both() ? this.last.cols : this.last);
    }
    return columns;
  }
  first = 0;
  last = 0;
  page = 0;
  isRangeChanged = false;
  numItemsInViewport = 0;
  lastScrollPos = 0;
  lazyLoadState = {};
  loaderArr = [];
  spacerStyle;
  contentStyle;
  scrollTimeout;
  resizeTimeout;
  _destroyed = false;
  initialized = false;
  windowResizeListener;
  defaultWidth;
  defaultHeight;
  defaultContentWidth;
  defaultContentHeight;
  _componentStyle = inject(ScrollerStyle);
  constructor() {
    super();
    effect(() => {
      if (this._scrollHeight() === "100%") {
        this.hostHeight.set("100%");
      }
    });
    effect(() => {
      const loading = this._loading();
      untracked(() => {
        if (this._lazy() && loading !== void 0 && loading !== this.d_loading) {
          this.d_loading = loading;
        }
      });
    });
    effect(() => {
      this._orientation();
      untracked(() => {
        this.lastScrollPos = this.both() ? {
          top: 0,
          left: 0
        } : 0;
      });
    });
    effect(() => {
      const numT = this._numToleratedItems();
      untracked(() => {
        if (numT !== void 0 && numT !== this.d_numToleratedItems) {
          this.d_numToleratedItems = numT;
        }
      });
    });
    effect(() => {
      this._itemSize();
      this._scrollHeight();
      this._scrollWidth();
      untracked(() => {
        if (this.initialized) {
          this.init();
          this.calculateAutoSize();
        }
      });
    });
    effect(() => {
      this._items();
      untracked(() => {
        if (this.initialized && !this._lazy()) {
          this.init();
        }
      });
    });
    effect(() => {
      const opts = this.options();
      untracked(() => {
        if (opts?.contentStyle !== void 0) {
          this.contentStyle = opts.contentStyle;
        }
      });
    });
  }
  onInit() {
    this.setInitialState();
  }
  onAfterViewInit() {
    Promise.resolve().then(() => {
      this.viewInit();
    });
  }
  onAfterViewChecked() {
    this.bindDirectiveInstance.setAttrs(this.ptm("host"));
    if (!this.initialized) {
      this.viewInit();
    }
  }
  onDestroy() {
    this._destroyed = true;
    this.unbindResizeListener();
    if (this.scrollTimeout) {
      clearTimeout(this.scrollTimeout);
    }
    if (this.resizeTimeout) {
      clearTimeout(this.resizeTimeout);
    }
    this.contentEl = null;
    this.initialized = false;
  }
  viewInit() {
    if (isPlatformBrowser(this.platformId) && !this.initialized) {
      if (ft(this.elementViewChild()?.nativeElement)) {
        this.setInitialState();
        this.setContentEl(this.contentEl);
        this.init();
        this.defaultWidth = jt(this.elementViewChild()?.nativeElement);
        this.defaultHeight = Pt(this.elementViewChild()?.nativeElement);
        this.defaultContentWidth = jt(this.contentEl);
        this.defaultContentHeight = Pt(this.contentEl);
        this.initialized = true;
      }
    }
  }
  init() {
    if (!this._disabled()) {
      this.bindResizeListener();
      setTimeout(() => {
        this.setSpacerSize();
        this.setSize();
        this.calculateOptions();
        this.calculateAutoSize();
        this.cd.detectChanges();
      }, 1);
    }
  }
  setContentEl(el) {
    this.contentEl = el || this.contentViewChild()?.nativeElement || et(this.elementViewChild()?.nativeElement, ".p-virtualscroller-content");
  }
  setInitialState() {
    this.first = this.both() ? {
      rows: 0,
      cols: 0
    } : 0;
    this.last = this.both() ? {
      rows: 0,
      cols: 0
    } : 0;
    this.numItemsInViewport = this.both() ? {
      rows: 0,
      cols: 0
    } : 0;
    this.lastScrollPos = this.both() ? {
      top: 0,
      left: 0
    } : 0;
    if (this.d_loading === void 0 || this.d_loading === false) {
      this.d_loading = this._loading() || false;
    }
    this.d_numToleratedItems = this._numToleratedItems();
    this.loaderArr = this.loaderArr.length > 0 ? this.loaderArr : [];
  }
  getElementRef() {
    return this.elementViewChild();
  }
  getPageByFirst(first) {
    return Math.floor(((first ?? this.first) + this.d_numToleratedItems * 4) / (this._step() || 1));
  }
  isPageChanged(first) {
    return this._step() ? this.page !== this.getPageByFirst(first ?? this.first) : true;
  }
  scrollTo(options) {
    this.elementViewChild()?.nativeElement?.scrollTo(options);
  }
  scrollToIndex(index, behavior = "auto") {
    const valid = this.both() ? index.every((i) => i > -1) : index > -1;
    if (valid) {
      const first = this.first;
      const {
        scrollTop = 0,
        scrollLeft = 0
      } = this.elementViewChild()?.nativeElement;
      const {
        numToleratedItems
      } = this.calculateNumItems();
      const contentPos = this.getContentPosition();
      const itemSize = this._itemSize();
      const calculateFirst = (_index = 0, _numT) => _index <= _numT ? 0 : _index;
      const calculateCoord = (_first, _size, _cpos) => _first * _size + _cpos;
      const scrollTo = (left = 0, top = 0) => this.scrollTo({
        left,
        top,
        behavior
      });
      let newFirst = this.both() ? {
        rows: 0,
        cols: 0
      } : 0;
      let isRangeChanged = false, isScrollChanged = false;
      if (this.both()) {
        newFirst = {
          rows: calculateFirst(index[0], numToleratedItems[0]),
          cols: calculateFirst(index[1], numToleratedItems[1])
        };
        scrollTo(calculateCoord(newFirst.cols, itemSize[1], contentPos.left), calculateCoord(newFirst.rows, itemSize[0], contentPos.top));
        isScrollChanged = this.lastScrollPos.top !== scrollTop || this.lastScrollPos.left !== scrollLeft;
        isRangeChanged = newFirst.rows !== first.rows || newFirst.cols !== first.cols;
      } else {
        newFirst = calculateFirst(index, numToleratedItems);
        this.horizontal() ? scrollTo(calculateCoord(newFirst, itemSize, contentPos.left), scrollTop) : scrollTo(scrollLeft, calculateCoord(newFirst, itemSize, contentPos.top));
        isScrollChanged = this.lastScrollPos !== (this.horizontal() ? scrollLeft : scrollTop);
        isRangeChanged = newFirst !== first;
      }
      this.isRangeChanged = isRangeChanged;
      isScrollChanged && (this.first = newFirst);
    }
  }
  scrollInView(index, to, behavior = "auto") {
    if (to) {
      const {
        first,
        viewport
      } = this.getRenderedRange();
      const scrollTo = (left = 0, top = 0) => this.scrollTo({
        left,
        top,
        behavior
      });
      const isToStart = to === "to-start";
      const isToEnd = to === "to-end";
      if (isToStart) {
        if (this.both()) {
          if (viewport.first.rows - first.rows > index[0]) {
            scrollTo(viewport.first.cols * this._itemSize()[1], (viewport.first.rows - 1) * this._itemSize()[0]);
          } else if (viewport.first.cols - first.cols > index[1]) {
            scrollTo((viewport.first.cols - 1) * this._itemSize()[1], viewport.first.rows * this._itemSize()[0]);
          }
        } else {
          if (viewport.first - first > index) {
            const pos = (viewport.first - 1) * this._itemSize();
            this.horizontal() ? scrollTo(pos, 0) : scrollTo(0, pos);
          }
        }
      } else if (isToEnd) {
        if (this.both()) {
          if (viewport.last.rows - first.rows <= index[0] + 1) {
            scrollTo(viewport.first.cols * this._itemSize()[1], (viewport.first.rows + 1) * this._itemSize()[0]);
          } else if (viewport.last.cols - first.cols <= index[1] + 1) {
            scrollTo((viewport.first.cols + 1) * this._itemSize()[1], viewport.first.rows * this._itemSize()[0]);
          }
        } else {
          if (viewport.last - first <= index + 1) {
            const pos = (viewport.first + 1) * this._itemSize();
            this.horizontal() ? scrollTo(pos, 0) : scrollTo(0, pos);
          }
        }
      }
    } else {
      this.scrollToIndex(index, behavior);
    }
  }
  getRenderedRange() {
    const calculateFirstInViewport = (_pos, _size) => _size || _pos ? Math.floor(_pos / (_size || _pos)) : 0;
    let firstInViewport = this.first;
    let lastInViewport = 0;
    const el = this.elementViewChild()?.nativeElement;
    if (el) {
      const {
        scrollTop,
        scrollLeft
      } = el;
      if (this.both()) {
        firstInViewport = {
          rows: calculateFirstInViewport(scrollTop, this._itemSize()[0]),
          cols: calculateFirstInViewport(scrollLeft, this._itemSize()[1])
        };
        lastInViewport = {
          rows: firstInViewport.rows + this.numItemsInViewport.rows,
          cols: firstInViewport.cols + this.numItemsInViewport.cols
        };
      } else {
        const scrollPos = this.horizontal() ? scrollLeft : scrollTop;
        firstInViewport = calculateFirstInViewport(scrollPos, this._itemSize());
        lastInViewport = firstInViewport + this.numItemsInViewport;
      }
    }
    return {
      first: this.first,
      last: this.last,
      viewport: {
        first: firstInViewport,
        last: lastInViewport
      }
    };
  }
  calculateNumItems() {
    const contentPos = this.getContentPosition();
    const el = this.elementViewChild()?.nativeElement;
    const contentWidth = (el ? el.offsetWidth - contentPos.left : 0) || 0;
    const contentHeight = (el ? el.offsetHeight - contentPos.top : 0) || 0;
    const calculateNumItemsInViewport = (_contentSize, _itemSize) => _itemSize || _contentSize ? Math.ceil(_contentSize / (_itemSize || _contentSize)) : 0;
    const calculateNumToleratedItems = (_numItems) => Math.ceil(_numItems / 2);
    const numItemsInViewport = this.both() ? {
      rows: calculateNumItemsInViewport(contentHeight, this._itemSize()[0]),
      cols: calculateNumItemsInViewport(contentWidth, this._itemSize()[1])
    } : calculateNumItemsInViewport(this.horizontal() ? contentWidth : contentHeight, this._itemSize());
    const numToleratedItems = this.d_numToleratedItems || (this.both() ? [calculateNumToleratedItems(numItemsInViewport.rows), calculateNumToleratedItems(numItemsInViewport.cols)] : calculateNumToleratedItems(numItemsInViewport));
    return {
      numItemsInViewport,
      numToleratedItems
    };
  }
  calculateOptions() {
    const {
      numItemsInViewport,
      numToleratedItems
    } = this.calculateNumItems();
    const calculateLast = (_first, _num, _numT, _isCols = false) => this.getLast(_first + _num + (_first < _numT ? 2 : 3) * _numT, _isCols);
    const first = this.first;
    const last = this.both() ? {
      rows: calculateLast(this.first.rows, numItemsInViewport.rows, numToleratedItems[0]),
      cols: calculateLast(this.first.cols, numItemsInViewport.cols, numToleratedItems[1], true)
    } : calculateLast(this.first, numItemsInViewport, numToleratedItems);
    this.last = last;
    this.numItemsInViewport = numItemsInViewport;
    this.d_numToleratedItems = numToleratedItems;
    if (this._showLoader()) {
      this.loaderArr = this.both() ? Array.from({
        length: numItemsInViewport.rows
      }).map(() => Array.from({
        length: numItemsInViewport.cols
      })) : Array.from({
        length: numItemsInViewport
      });
    }
    if (this._lazy()) {
      Promise.resolve().then(() => {
        this.lazyLoadState = {
          first: this._step() ? this.both() ? {
            rows: 0,
            cols: first.cols
          } : 0 : first,
          last: Math.min(this._step() ? this._step() : this.last, this._items().length)
        };
        this.handleEvents("onLazyLoad", this.lazyLoadState);
      });
    }
  }
  calculateAutoSize() {
    if (this._autoSize() && !this.d_loading) {
      Promise.resolve().then(() => {
        if (this.contentEl) {
          this.contentEl.style.minHeight = this.contentEl.style.minWidth = "auto";
          this.contentEl.style.position = "relative";
          this.elementViewChild().nativeElement.style.contain = "none";
          const [contentWidth, contentHeight] = [jt(this.contentEl), Pt(this.contentEl)];
          contentWidth !== this.defaultContentWidth && (this.elementViewChild().nativeElement.style.width = "");
          contentHeight !== this.defaultContentHeight && (this.elementViewChild().nativeElement.style.height = "");
          const [width, height] = [jt(this.elementViewChild().nativeElement), Pt(this.elementViewChild().nativeElement)];
          (this.both() || this.horizontal()) && (this.elementViewChild().nativeElement.style.width = width < this.defaultWidth ? width + "px" : this._scrollWidth() || this.defaultWidth + "px");
          (this.both() || this.vertical()) && (this.elementViewChild().nativeElement.style.height = height < this.defaultHeight ? height + "px" : this._scrollHeight() || this.defaultHeight + "px");
          this.contentEl.style.minHeight = this.contentEl.style.minWidth = "";
          this.contentEl.style.position = "";
          this.elementViewChild().nativeElement.style.contain = "";
        }
      });
    }
  }
  getLast(last = 0, isCols = false) {
    const items = this._items();
    const columns = this._columns();
    return items ? Math.min(isCols ? (columns || items[0]).length : items.length, last) : 0;
  }
  getContentPosition() {
    if (this.contentEl) {
      const style4 = getComputedStyle(this.contentEl);
      const left = parseFloat(style4.paddingLeft) + Math.max(parseFloat(style4.left) || 0, 0);
      const right = parseFloat(style4.paddingRight) + Math.max(parseFloat(style4.right) || 0, 0);
      const top = parseFloat(style4.paddingTop) + Math.max(parseFloat(style4.top) || 0, 0);
      const bottom = parseFloat(style4.paddingBottom) + Math.max(parseFloat(style4.bottom) || 0, 0);
      return {
        left,
        right,
        top,
        bottom,
        x: left + right,
        y: top + bottom
      };
    }
    return {
      left: 0,
      right: 0,
      top: 0,
      bottom: 0,
      x: 0,
      y: 0
    };
  }
  setSize() {
    const nativeElement = this.elementViewChild()?.nativeElement;
    if (nativeElement) {
      const parentElement = nativeElement.parentElement?.parentElement;
      const elementWidth = nativeElement.offsetWidth;
      const parentWidth = parentElement?.offsetWidth || 0;
      const width = this._scrollWidth() || `${elementWidth || parentWidth}px`;
      const elementHeight = nativeElement.offsetHeight;
      const parentHeight = parentElement?.offsetHeight || 0;
      const height = this._scrollHeight() || `${elementHeight || parentHeight}px`;
      const setProp = (_name, _value) => nativeElement.style[_name] = _value;
      if (this.both() || this.horizontal()) {
        setProp("height", height);
        setProp("width", width);
      } else {
        setProp("height", height);
      }
    }
  }
  setSpacerSize() {
    const items = this._items();
    if (items) {
      const contentPos = this.getContentPosition();
      const setProp = (_name, _value, _size, _cpos = 0) => this.spacerStyle = __spreadValues(__spreadValues({}, this.spacerStyle), {
        [`${_name}`]: (_value || []).length * _size + _cpos + "px"
      });
      if (this.both()) {
        setProp("height", items, this._itemSize()[0], contentPos.y);
        setProp("width", this._columns() || items[1], this._itemSize()[1], contentPos.x);
      } else {
        this.horizontal() ? setProp("width", this._columns() || items, this._itemSize(), contentPos.x) : setProp("height", items, this._itemSize(), contentPos.y);
      }
    }
  }
  setContentPosition(pos) {
    if (this.contentEl && !this._appendOnly()) {
      const first = pos ? pos.first : this.first;
      const calculateTranslateVal = (_first, _size) => _first * _size;
      const setTransform = (_x = 0, _y = 0) => this.contentStyle = __spreadValues(__spreadValues({}, this.contentStyle), {
        transform: `translate3d(${_x}px, ${_y}px, 0)`
      });
      if (this.both()) {
        setTransform(calculateTranslateVal(first.cols, this._itemSize()[1]), calculateTranslateVal(first.rows, this._itemSize()[0]));
      } else {
        const translateVal = calculateTranslateVal(first, this._itemSize());
        this.horizontal() ? setTransform(translateVal, 0) : setTransform(0, translateVal);
      }
    }
  }
  onScrollPositionChange(event) {
    const target = event.target;
    if (!target) {
      throw new Error("Event target is null");
    }
    const contentPos = this.getContentPosition();
    const calculateScrollPos = (_pos, _cpos) => _pos ? _pos > _cpos ? _pos - _cpos : _pos : 0;
    const calculateCurrentIndex = (_pos, _size) => _size || _pos ? Math.floor(_pos / (_size || _pos)) : 0;
    const calculateTriggerIndex = (_currentIndex, _first, _last, _num, _numT, _isScrollDownOrRight) => {
      return _currentIndex <= _numT ? _numT : _isScrollDownOrRight ? _last - _num - _numT : _first + _numT - 1;
    };
    const calculateFirst = (_currentIndex, _triggerIndex, _first, _last, _num, _numT, _isScrollDownOrRight) => {
      if (_currentIndex <= _numT) return 0;
      else return Math.max(0, _isScrollDownOrRight ? _currentIndex < _triggerIndex ? _first : _currentIndex - _numT : _currentIndex > _triggerIndex ? _first : _currentIndex - 2 * _numT);
    };
    const calculateLast = (_currentIndex, _first, _last, _num, _numT, _isCols = false) => {
      let lastValue = _first + _num + 2 * _numT;
      if (_currentIndex >= _numT) {
        lastValue += _numT + 1;
      }
      return this.getLast(lastValue, _isCols);
    };
    const scrollTop = calculateScrollPos(target.scrollTop, contentPos.top);
    const scrollLeft = calculateScrollPos(target.scrollLeft, contentPos.left);
    let newFirst = this.both() ? {
      rows: 0,
      cols: 0
    } : 0;
    let newLast = this.last;
    let isRangeChanged = false;
    let newScrollPos = this.lastScrollPos;
    if (this.both()) {
      const isScrollDown = this.lastScrollPos.top <= scrollTop;
      const isScrollRight = this.lastScrollPos.left <= scrollLeft;
      if (!this._appendOnly() || this._appendOnly() && (isScrollDown || isScrollRight)) {
        const currentIndex = {
          rows: calculateCurrentIndex(scrollTop, this._itemSize()[0]),
          cols: calculateCurrentIndex(scrollLeft, this._itemSize()[1])
        };
        const triggerIndex = {
          rows: calculateTriggerIndex(currentIndex.rows, this.first.rows, this.last.rows, this.numItemsInViewport.rows, this.d_numToleratedItems[0], isScrollDown),
          cols: calculateTriggerIndex(currentIndex.cols, this.first.cols, this.last.cols, this.numItemsInViewport.cols, this.d_numToleratedItems[1], isScrollRight)
        };
        newFirst = {
          rows: calculateFirst(currentIndex.rows, triggerIndex.rows, this.first.rows, this.last.rows, this.numItemsInViewport.rows, this.d_numToleratedItems[0], isScrollDown),
          cols: calculateFirst(currentIndex.cols, triggerIndex.cols, this.first.cols, this.last.cols, this.numItemsInViewport.cols, this.d_numToleratedItems[1], isScrollRight)
        };
        newLast = {
          rows: calculateLast(currentIndex.rows, newFirst.rows, this.last.rows, this.numItemsInViewport.rows, this.d_numToleratedItems[0]),
          cols: calculateLast(currentIndex.cols, newFirst.cols, this.last.cols, this.numItemsInViewport.cols, this.d_numToleratedItems[1], true)
        };
        isRangeChanged = newFirst.rows !== this.first.rows || newLast.rows !== this.last.rows || newFirst.cols !== this.first.cols || newLast.cols !== this.last.cols || this.isRangeChanged;
        newScrollPos = {
          top: scrollTop,
          left: scrollLeft
        };
      }
    } else {
      const scrollPos = this.horizontal() ? scrollLeft : scrollTop;
      const isScrollDownOrRight = this.lastScrollPos <= scrollPos;
      if (!this._appendOnly() || this._appendOnly() && isScrollDownOrRight) {
        const currentIndex = calculateCurrentIndex(scrollPos, this._itemSize());
        const triggerIndex = calculateTriggerIndex(currentIndex, this.first, this.last, this.numItemsInViewport, this.d_numToleratedItems, isScrollDownOrRight);
        newFirst = calculateFirst(currentIndex, triggerIndex, this.first, this.last, this.numItemsInViewport, this.d_numToleratedItems, isScrollDownOrRight);
        newLast = calculateLast(currentIndex, newFirst, this.last, this.numItemsInViewport, this.d_numToleratedItems);
        isRangeChanged = newFirst !== this.first || newLast !== this.last || this.isRangeChanged;
        newScrollPos = scrollPos;
      }
    }
    return {
      first: newFirst,
      last: newLast,
      isRangeChanged,
      scrollPos: newScrollPos
    };
  }
  onScrollChange(event) {
    const {
      first,
      last,
      isRangeChanged,
      scrollPos
    } = this.onScrollPositionChange(event);
    if (isRangeChanged) {
      const newState = {
        first,
        last
      };
      this.setContentPosition(newState);
      this.first = first;
      this.last = last;
      this.lastScrollPos = scrollPos;
      this.handleEvents("onScrollIndexChange", newState);
      if (this._lazy() && this.isPageChanged(first)) {
        const lazyLoadState = {
          first: this._step() ? Math.min(this.getPageByFirst(first) * this._step(), this._items().length - this._step()) : first,
          last: Math.min(this._step() ? (this.getPageByFirst(first) + 1) * this._step() : last, this._items().length)
        };
        const isLazyStateChanged = this.lazyLoadState.first !== lazyLoadState.first || this.lazyLoadState.last !== lazyLoadState.last;
        isLazyStateChanged && this.handleEvents("onLazyLoad", lazyLoadState);
        this.lazyLoadState = lazyLoadState;
      }
    }
  }
  onContainerScroll(event) {
    this.handleEvents("onScroll", {
      originalEvent: event
    });
    if (this._delay()) {
      if (this.scrollTimeout) {
        clearTimeout(this.scrollTimeout);
      }
      if (!this.d_loading && this._showLoader()) {
        const {
          isRangeChanged
        } = this.onScrollPositionChange(event);
        const changed = isRangeChanged || (this._step() ? this.isPageChanged() : false);
        if (changed) {
          this.d_loading = true;
          this.cd.detectChanges();
        }
      }
      this.scrollTimeout = setTimeout(() => {
        this.onScrollChange(event);
        if (this.d_loading && this._showLoader() && (!this._lazy() || this._loading() === void 0)) {
          this.d_loading = false;
          this.page = this.getPageByFirst();
        }
        this.cd.detectChanges();
      }, this._delay());
    } else {
      !this.d_loading && this.onScrollChange(event);
    }
  }
  bindResizeListener() {
    if (isPlatformBrowser(this.platformId)) {
      if (!this.windowResizeListener) {
        const window = this.document.defaultView;
        const event = oe() ? "orientationchange" : "resize";
        this.windowResizeListener = this.renderer.listen(window, event, this.onWindowResize.bind(this));
      }
    }
  }
  unbindResizeListener() {
    if (this.windowResizeListener) {
      this.windowResizeListener();
      this.windowResizeListener = null;
    }
  }
  onWindowResize() {
    if (this.resizeTimeout) {
      clearTimeout(this.resizeTimeout);
    }
    this.resizeTimeout = setTimeout(() => {
      if (ft(this.elementViewChild()?.nativeElement)) {
        const [width, height] = [jt(this.elementViewChild()?.nativeElement), Pt(this.elementViewChild()?.nativeElement)];
        const [isDiffWidth, isDiffHeight] = [width !== this.defaultWidth, height !== this.defaultHeight];
        const reinit = this.both() ? isDiffWidth || isDiffHeight : this.horizontal() ? isDiffWidth : this.vertical() ? isDiffHeight : false;
        if (reinit) {
          this.d_numToleratedItems = this._numToleratedItems();
          this.defaultWidth = width;
          this.defaultHeight = height;
          this.defaultContentWidth = jt(this.contentEl);
          this.defaultContentHeight = Pt(this.contentEl);
          this.init();
        }
      }
    }, this._resizeDelay());
  }
  handleEvents(name, params) {
    if (this._destroyed) return;
    const opts = this.options();
    return opts && opts[name] ? opts[name](params) : this[name].emit(params);
  }
  loaderIconContext = {
    options: {
      styleClass: "p-virtualscroller-loading-icon"
    }
  };
  getContentTemplateContext() {
    return {
      $implicit: this.loadedItems,
      options: this.getContentOptions()
    };
  }
  getItemTemplateContext(item, index) {
    return {
      $implicit: item,
      options: this.getOptions(index)
    };
  }
  getLoaderTemplateContext(index) {
    return {
      options: this.getLoaderOptions(index, this.both() && {
        numCols: this.numItemsInViewport.cols
      })
    };
  }
  getDisabledContentTemplateContext() {
    return {
      $implicit: this.items(),
      options: {
        rows: this._items(),
        columns: this.loadedColumns
      }
    };
  }
  getContentOptions() {
    return {
      contentStyleClass: `p-virtualscroller-content ${this.d_loading ? "p-virtualscroller-loading" : ""}`,
      items: this.loadedItems,
      getItemOptions: (index) => this.getOptions(index),
      loading: this.d_loading,
      getLoaderOptions: (index, options) => this.getLoaderOptions(index, options),
      itemSize: this._itemSize(),
      rows: this.loadedRows,
      columns: this.loadedColumns,
      spacerStyle: this.spacerStyle,
      contentStyle: this.contentStyle,
      vertical: this.vertical(),
      horizontal: this.horizontal(),
      both: this.both(),
      scrollTo: this.scrollTo.bind(this),
      scrollToIndex: this.scrollToIndex.bind(this),
      orientation: this._orientation(),
      scrollableElement: this.elementViewChild()?.nativeElement
    };
  }
  getOptions(renderedIndex) {
    const count = (this._items() || []).length;
    const index = this.both() ? this.first.rows + renderedIndex : this.first + renderedIndex;
    return {
      index,
      count,
      first: index === 0,
      last: index === count - 1,
      even: index % 2 === 0,
      odd: index % 2 !== 0
    };
  }
  getLoaderOptions(index, extOptions) {
    const count = this.loaderArr.length;
    return __spreadValues({
      index,
      count,
      first: index === 0,
      last: index === count - 1,
      even: index % 2 === 0,
      odd: index % 2 !== 0,
      loading: this.d_loading
    }, extOptions);
  }
  static ɵfac = function Scroller_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _Scroller)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _Scroller,
    selectors: [["p-scroller"], ["p-virtualscroller"], ["p-virtual-scroller"]],
    contentQueries: function Scroller_ContentQueries(rf, ctx, dirIndex) {
      if (rf & 1) {
        ɵɵcontentQuerySignal(dirIndex, ctx.contentTemplate, _c03, 4)(dirIndex, ctx.itemTemplate, _c13, 4)(dirIndex, ctx.loaderTemplate, _c22, 4)(dirIndex, ctx.loaderIconTemplate, _c32, 4);
      }
      if (rf & 2) {
        ɵɵqueryAdvance(4);
      }
    },
    viewQuery: function Scroller_Query(rf, ctx) {
      if (rf & 1) {
        ɵɵviewQuerySignal(ctx.elementViewChild, _c42, 5)(ctx.contentViewChild, _c03, 5);
      }
      if (rf & 2) {
        ɵɵqueryAdvance(2);
      }
    },
    hostVars: 2,
    hostBindings: function Scroller_HostBindings(rf, ctx) {
      if (rf & 2) {
        ɵɵstyleProp("height", ctx.hostHeight());
      }
    },
    inputs: {
      hostName: [1, "hostName"],
      id: [1, "id"],
      style: [1, "style"],
      styleClass: [1, "styleClass"],
      tabindex: [1, "tabindex"],
      items: [1, "items"],
      itemSize: [1, "itemSize"],
      scrollHeight: [1, "scrollHeight"],
      scrollWidth: [1, "scrollWidth"],
      orientation: [1, "orientation"],
      step: [1, "step"],
      delay: [1, "delay"],
      resizeDelay: [1, "resizeDelay"],
      appendOnly: [1, "appendOnly"],
      inline: [1, "inline"],
      lazy: [1, "lazy"],
      disabled: [1, "disabled"],
      loaderDisabled: [1, "loaderDisabled"],
      columns: [1, "columns"],
      showSpacer: [1, "showSpacer"],
      showLoader: [1, "showLoader"],
      numToleratedItems: [1, "numToleratedItems"],
      loading: [1, "loading"],
      autoSize: [1, "autoSize"],
      trackBy: [1, "trackBy"],
      options: [1, "options"]
    },
    outputs: {
      onLazyLoad: "onLazyLoad",
      onScroll: "onScroll",
      onScrollIndexChange: "onScrollIndexChange"
    },
    features: [ɵɵProvidersFeature([ScrollerStyle, {
      provide: SCROLLER_INSTANCE,
      useExisting: _Scroller
    }, {
      provide: PARENT_INSTANCE,
      useExisting: _Scroller
    }]), ɵɵHostDirectivesFeature([Bind]), ɵɵInheritDefinitionFeature],
    ngContentSelectors: _c52,
    decls: 2,
    vars: 1,
    consts: [["element", ""], ["content", ""], [3, "style", "class", "pBind"], [3, "scroll", "pBind"], [3, "class", "style", "pBind"], [3, "class", "pBind"], [4, "ngTemplateOutlet", "ngTemplateOutletContext"], [3, "pBind"], ["data-p-icon", "spinner", 3, "class", "spin", "pBind"], ["data-p-icon", "spinner", 3, "spin", "pBind"]],
    template: function Scroller_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵprojectionDef();
        ɵɵconditionalCreate(0, Scroller_Conditional_0_Template, 6, 10, "div", 2)(1, Scroller_Conditional_1_Template, 2, 1);
      }
      if (rf & 2) {
        ɵɵconditional(!ctx._disabled() ? 0 : 1);
      }
    },
    dependencies: [NgTemplateOutlet, Spinner, Bind],
    encapsulation: 2,
    changeDetection: 1
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(Scroller, [{
    type: Component,
    args: [{
      selector: "p-scroller, p-virtualscroller, p-virtual-scroller",
      imports: [NgTemplateOutlet, Spinner, Bind],
      standalone: true,
      template: `
        @if (!_disabled()) {
            <div #element [attr.id]="_id()" [attr.tabindex]="_tabindex()" [style]="_style()" [class]="cn(cx('root'), _styleClass())" (scroll)="onContainerScroll($event)" [pBind]="ptm('root')">
                @if (contentTemplate()) {
                    <ng-container *ngTemplateOutlet="contentTemplate(); context: getContentTemplateContext()"></ng-container>
                } @else {
                    <div #content [class]="cn(cx('content'), contentStyleClass())" [style]="contentStyle" [pBind]="ptm('content')">
                        @for (item of loadedItems; track _trackBy() ? _trackBy()($index, item) : $index; let index = $index) {
                            <ng-container *ngTemplateOutlet="itemTemplate(); context: getItemTemplateContext(item, index)"></ng-container>
                        }
                    </div>
                }
                @if (_showSpacer()) {
                    <div [class]="cx('spacer')" [style]="spacerStyle" [pBind]="ptm('spacer')"></div>
                }
                @if (!_loaderDisabled() && _showLoader() && d_loading) {
                    <div [class]="cx('loader')" [pBind]="ptm('loader')">
                        @if (loaderTemplate()) {
                            @for (item of loaderArr; track $index; let index = $index) {
                                <ng-container *ngTemplateOutlet="loaderTemplate(); context: getLoaderTemplateContext(index)"></ng-container>
                            }
                        } @else {
                            @if (loaderIconTemplate()) {
                                <ng-container *ngTemplateOutlet="loaderIconTemplate(); context: loaderIconContext"></ng-container>
                            } @else {
                                <svg data-p-icon="spinner" [class]="cx('loadingIcon')" [spin]="true" [pBind]="ptm('loadingIcon')" />
                            }
                        }
                    </div>
                }
            </div>
        } @else {
            <ng-content />
            @if (contentTemplate()) {
                <ng-container *ngTemplateOutlet="contentTemplate(); context: getDisabledContentTemplateContext()"></ng-container>
            }
        }
    `,
      changeDetection: ChangeDetectionStrategy.Default,
      encapsulation: ViewEncapsulation.None,
      providers: [ScrollerStyle, {
        provide: SCROLLER_INSTANCE,
        useExisting: Scroller
      }, {
        provide: PARENT_INSTANCE,
        useExisting: Scroller
      }],
      hostDirectives: [Bind],
      host: {
        "[style.height]": "hostHeight()"
      }
    }]
  }], () => [], {
    hostName: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "hostName",
        required: false
      }]
    }],
    id: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "id",
        required: false
      }]
    }],
    style: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "style",
        required: false
      }]
    }],
    styleClass: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "styleClass",
        required: false
      }]
    }],
    tabindex: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "tabindex",
        required: false
      }]
    }],
    items: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "items",
        required: false
      }]
    }],
    itemSize: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "itemSize",
        required: false
      }]
    }],
    scrollHeight: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "scrollHeight",
        required: false
      }]
    }],
    scrollWidth: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "scrollWidth",
        required: false
      }]
    }],
    orientation: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "orientation",
        required: false
      }]
    }],
    step: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "step",
        required: false
      }]
    }],
    delay: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "delay",
        required: false
      }]
    }],
    resizeDelay: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "resizeDelay",
        required: false
      }]
    }],
    appendOnly: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "appendOnly",
        required: false
      }]
    }],
    inline: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "inline",
        required: false
      }]
    }],
    lazy: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "lazy",
        required: false
      }]
    }],
    disabled: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "disabled",
        required: false
      }]
    }],
    loaderDisabled: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "loaderDisabled",
        required: false
      }]
    }],
    columns: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "columns",
        required: false
      }]
    }],
    showSpacer: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "showSpacer",
        required: false
      }]
    }],
    showLoader: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "showLoader",
        required: false
      }]
    }],
    numToleratedItems: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "numToleratedItems",
        required: false
      }]
    }],
    loading: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "loading",
        required: false
      }]
    }],
    autoSize: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "autoSize",
        required: false
      }]
    }],
    trackBy: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "trackBy",
        required: false
      }]
    }],
    options: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "options",
        required: false
      }]
    }],
    onLazyLoad: [{
      type: Output,
      args: ["onLazyLoad"]
    }],
    onScroll: [{
      type: Output,
      args: ["onScroll"]
    }],
    onScrollIndexChange: [{
      type: Output,
      args: ["onScrollIndexChange"]
    }],
    elementViewChild: [{
      type: ViewChild,
      args: ["element", {
        isSignal: true
      }]
    }],
    contentViewChild: [{
      type: ViewChild,
      args: ["content", {
        isSignal: true
      }]
    }],
    contentTemplate: [{
      type: ContentChild,
      args: ["content", __spreadProps(__spreadValues({}, {
        descendants: false
      }), {
        isSignal: true
      })]
    }],
    itemTemplate: [{
      type: ContentChild,
      args: ["item", __spreadProps(__spreadValues({}, {
        descendants: false
      }), {
        isSignal: true
      })]
    }],
    loaderTemplate: [{
      type: ContentChild,
      args: ["loader", __spreadProps(__spreadValues({}, {
        descendants: false
      }), {
        isSignal: true
      })]
    }],
    loaderIconTemplate: [{
      type: ContentChild,
      args: ["loadericon", __spreadProps(__spreadValues({}, {
        descendants: false
      }), {
        isSignal: true
      })]
    }]
  });
})();
var ScrollerModule = class _ScrollerModule {
  static ɵfac = function ScrollerModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _ScrollerModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _ScrollerModule,
    imports: [Scroller],
    exports: [Scroller]
  });
  static ɵinj = ɵɵdefineInjector({
    imports: [Scroller]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(ScrollerModule, [{
    type: NgModule,
    args: [{
      imports: [Scroller],
      exports: [Scroller]
    }]
  }], null, null);
})();

// node_modules/@primeuix/styles/dist/autocomplete/index.mjs
var style3 = "\n    .p-autocomplete {\n        display: inline-flex;\n    }\n\n    .p-autocomplete-loader {\n        position: absolute;\n        top: 50%;\n        margin-top: calc(-1 * dt('icon.size') / 2);\n        inset-inline-end: dt('autocomplete.padding.x');\n    }\n\n    .p-autocomplete:has(.p-autocomplete-dropdown) .p-autocomplete-loader {\n        inset-inline-end: calc(dt('autocomplete.dropdown.width') + dt('autocomplete.padding.x'));\n    }\n\n    .p-autocomplete:has(.p-autocomplete-dropdown) .p-autocomplete-input {\n        flex: 1 1 auto;\n        width: 1%;\n    }\n\n    .p-autocomplete:has(.p-autocomplete-dropdown) .p-autocomplete-input,\n    .p-autocomplete:has(.p-autocomplete-dropdown) .p-autocomplete-input-multiple {\n        border-start-end-radius: 0;\n        border-end-end-radius: 0;\n    }\n\n    .p-autocomplete-dropdown {\n        cursor: pointer;\n        display: inline-flex;\n        user-select: none;\n        align-items: center;\n        justify-content: center;\n        overflow: hidden;\n        position: relative;\n        width: dt('autocomplete.dropdown.width');\n        border-start-end-radius: dt('autocomplete.dropdown.border.radius');\n        border-end-end-radius: dt('autocomplete.dropdown.border.radius');\n        background: dt('autocomplete.dropdown.background');\n        border: 1px solid dt('autocomplete.dropdown.border.color');\n        border-inline-start: 0 none;\n        color: dt('autocomplete.dropdown.color');\n        transition:\n            background dt('autocomplete.transition.duration'),\n            color dt('autocomplete.transition.duration'),\n            border-color dt('autocomplete.transition.duration'),\n            outline-color dt('autocomplete.transition.duration'),\n            box-shadow dt('autocomplete.transition.duration');\n        outline-color: transparent;\n    }\n\n    .p-autocomplete-dropdown:not(:disabled):hover {\n        background: dt('autocomplete.dropdown.hover.background');\n        border-color: dt('autocomplete.dropdown.hover.border.color');\n        color: dt('autocomplete.dropdown.hover.color');\n    }\n\n    .p-autocomplete-dropdown:not(:disabled):active {\n        background: dt('autocomplete.dropdown.active.background');\n        border-color: dt('autocomplete.dropdown.active.border.color');\n        color: dt('autocomplete.dropdown.active.color');\n    }\n\n    .p-autocomplete-dropdown:focus-visible {\n        box-shadow: dt('autocomplete.dropdown.focus.ring.shadow');\n        outline: dt('autocomplete.dropdown.focus.ring.width') dt('autocomplete.dropdown.focus.ring.style') dt('autocomplete.dropdown.focus.ring.color');\n        outline-offset: dt('autocomplete.dropdown.focus.ring.offset');\n    }\n\n    .p-autocomplete-overlay {\n        position: absolute;\n        top: 0;\n        left: 0;\n        background: dt('autocomplete.overlay.background');\n        color: dt('autocomplete.overlay.color');\n        border: 1px solid dt('autocomplete.overlay.border.color');\n        border-radius: dt('autocomplete.overlay.border.radius');\n        box-shadow: dt('autocomplete.overlay.shadow');\n        min-width: 100%;\n    }\n\n    .p-autocomplete-list-container {\n        overflow: auto;\n    }\n\n    .p-autocomplete-list {\n        margin: 0;\n        list-style-type: none;\n        display: flex;\n        flex-direction: column;\n        gap: dt('autocomplete.list.gap');\n        padding: dt('autocomplete.list.padding');\n    }\n\n    .p-autocomplete-option {\n        font-weight: dt('autocomplete.option.font.weight');\n        font-size: dt('autocomplete.option.font.size');\n        cursor: pointer;\n        white-space: nowrap;\n        position: relative;\n        overflow: hidden;\n        display: flex;\n        align-items: center;\n        padding: dt('autocomplete.option.padding');\n        border: 0 none;\n        color: dt('autocomplete.option.color');\n        background: transparent;\n        transition:\n            background dt('list.option.transition.duration'),\n            color dt('list.option.transition.duration'),\n            border-color dt('list.option.transition.duration');\n        border-radius: dt('autocomplete.option.border.radius');\n    }\n\n    .p-autocomplete-option:not(.p-autocomplete-option-selected):not(.p-disabled).p-focus {\n        background: dt('autocomplete.option.focus.background');\n        color: dt('autocomplete.option.focus.color');\n    }\n\n    .p-autocomplete-option:not(.p-autocomplete-option-selected):not(.p-disabled):hover {\n        background: dt('autocomplete.option.focus.background');\n        color: dt('autocomplete.option.focus.color');\n    }\n\n    .p-autocomplete-option-selected {\n        background: dt('autocomplete.option.selected.background');\n        color: dt('autocomplete.option.selected.color');\n    }\n\n    .p-autocomplete-option-selected.p-focus {\n        background: dt('autocomplete.option.selected.focus.background');\n        color: dt('autocomplete.option.selected.focus.color');\n    }\n\n    .p-autocomplete-option-group {\n        font-weight: dt('autocomplete.option.group.font.weight');\n        font-size: dt('autocomplete.option.group.font.size');\n        margin: 0;\n        padding: dt('autocomplete.option.group.padding');\n        color: dt('autocomplete.option.group.color');\n        background: dt('autocomplete.option.group.background');\n    }\n\n    .p-autocomplete-input-multiple {\n        margin: 0;\n        list-style-type: none;\n        cursor: text;\n        overflow: hidden;\n        display: flex;\n        align-items: center;\n        flex-wrap: wrap;\n        padding: calc(dt('autocomplete.padding.y') / 2) dt('autocomplete.padding.x');\n        gap: calc(dt('autocomplete.padding.y') / 2);\n        color: dt('autocomplete.color');\n        background: dt('autocomplete.background');\n        border: 1px solid dt('autocomplete.border.color');\n        border-radius: dt('autocomplete.border.radius');\n        width: 100%;\n        transition:\n            background dt('autocomplete.transition.duration'),\n            color dt('autocomplete.transition.duration'),\n            border-color dt('autocomplete.transition.duration'),\n            outline-color dt('autocomplete.transition.duration'),\n            box-shadow dt('autocomplete.transition.duration');\n        outline-color: transparent;\n        box-shadow: dt('autocomplete.shadow');\n    }\n\n    .p-autocomplete-input-multiple.p-disabled {\n        opacity: 1;\n        background: dt('autocomplete.disabled.background');\n        color: dt('autocomplete.disabled.color');\n    }\n\n    .p-autocomplete-input-multiple:not(.p-disabled):hover {\n        border-color: dt('autocomplete.hover.border.color');\n    }\n\n    .p-autocomplete.p-focus .p-autocomplete-input-multiple:not(.p-disabled) {\n        border-color: dt('autocomplete.focus.border.color');\n        box-shadow: dt('autocomplete.focus.ring.shadow');\n        outline: dt('autocomplete.focus.ring.width') dt('autocomplete.focus.ring.style') dt('autocomplete.focus.ring.color');\n        outline-offset: dt('autocomplete.focus.ring.offset');\n    }\n\n    .p-autocomplete.p-invalid .p-autocomplete-input-multiple {\n        border-color: dt('autocomplete.invalid.border.color');\n    }\n\n    .p-variant-filled.p-autocomplete-input-multiple {\n        background: dt('autocomplete.filled.background');\n    }\n\n    .p-autocomplete-input-multiple.p-variant-filled:not(.p-disabled):hover {\n        background: dt('autocomplete.filled.hover.background');\n    }\n\n    .p-autocomplete.p-focus .p-autocomplete-input-multiple.p-variant-filled:not(.p-disabled) {\n        background: dt('autocomplete.filled.focus.background');\n    }\n\n    .p-autocomplete-chip.p-chip {\n        padding-block-start: calc(dt('autocomplete.padding.y') / 2);\n        padding-block-end: calc(dt('autocomplete.padding.y') / 2);\n        border-radius: dt('autocomplete.chip.border.radius');\n    }\n\n    .p-autocomplete-input-multiple:has(.p-autocomplete-chip) {\n        padding-inline-start: calc(dt('autocomplete.padding.y') / 2);\n        padding-inline-end: calc(dt('autocomplete.padding.y') / 2);\n    }\n\n    .p-autocomplete-chip-item.p-focus .p-autocomplete-chip {\n        background: dt('autocomplete.chip.focus.background');\n        color: dt('autocomplete.chip.focus.color');\n    }\n\n    .p-autocomplete-input-chip {\n        flex: 1 1 auto;\n        display: inline-flex;\n        padding-block-start: calc(dt('autocomplete.padding.y') / 2);\n        padding-block-end: calc(dt('autocomplete.padding.y') / 2);\n    }\n\n    .p-autocomplete-input-chip input {\n        border: 0 none;\n        outline: 0 none;\n        background: transparent;\n        margin: 0;\n        padding: 0;\n        box-shadow: none;\n        border-radius: 0;\n        width: 100%;\n        font-family: inherit;\n        font-feature-settings: inherit;\n        font-size: 1rem;\n        color: inherit;\n    }\n\n    .p-autocomplete-input-chip input::placeholder {\n        color: dt('autocomplete.placeholder.color');\n    }\n\n    .p-autocomplete.p-invalid .p-autocomplete-input-chip input::placeholder {\n        color: dt('autocomplete.invalid.placeholder.color');\n    }\n\n    .p-autocomplete-empty-message {\n        padding: dt('autocomplete.empty.message.padding');\n        font-weight: dt('autocomplete.option.font.weight');\n        font-size: dt('autocomplete.option.font.size');\n    }\n\n    .p-autocomplete-fluid {\n        display: flex;\n    }\n\n    .p-autocomplete-fluid:has(.p-autocomplete-dropdown) .p-autocomplete-input {\n        width: 1%;\n    }\n\n    .p-autocomplete:has(.p-inputtext-sm) .p-autocomplete-dropdown {\n        width: dt('autocomplete.dropdown.sm.width');\n    }\n\n    .p-autocomplete:has(.p-inputtext-sm) .p-autocomplete-dropdown .p-icon {\n        font-size: dt('form.field.sm.font.size');\n        width: dt('form.field.sm.font.size');\n        height: dt('form.field.sm.font.size');\n    }\n\n    .p-autocomplete:has(.p-inputtext-lg) .p-autocomplete-dropdown {\n        width: dt('autocomplete.dropdown.lg.width');\n    }\n\n    .p-autocomplete:has(.p-inputtext-lg) .p-autocomplete-dropdown .p-icon {\n        font-size: dt('form.field.lg.font.size');\n        width: dt('form.field.lg.font.size');\n        height: dt('form.field.lg.font.size');\n    }\n\n    .p-autocomplete-clear-icon {\n        position: absolute;\n        top: 50%;\n        margin-top: calc(-1 * dt('icon.size') / 2);\n        cursor: pointer;\n        color: dt('form.field.icon.color');\n        inset-inline-end: dt('autocomplete.padding.x');\n    }\n\n    .p-autocomplete:has(.p-autocomplete-dropdown) .p-autocomplete-clear-icon {\n        inset-inline-end: calc(dt('autocomplete.padding.x') + dt('autocomplete.dropdown.width'));\n    }\n\n    .p-autocomplete:has(.p-autocomplete-clear-icon) .p-autocomplete-input {\n        padding-inline-end: calc((dt('form.field.padding.x') * 2) + dt('icon.size'));\n    }\n\n    .p-inputgroup .p-autocomplete-dropdown {\n        border-radius: 0;\n    }\n\n    .p-inputgroup > .p-autocomplete:last-child:has(.p-autocomplete-dropdown) > .p-autocomplete-input {\n        border-start-end-radius: 0;\n        border-end-end-radius: 0;\n    }\n\n    .p-inputgroup > .p-autocomplete:last-child .p-autocomplete-dropdown {\n        border-start-end-radius: dt('autocomplete.dropdown.border.radius');\n        border-end-end-radius: dt('autocomplete.dropdown.border.radius');\n    }\n";

// node_modules/primeng/fesm2022/primeng-autocomplete.mjs
var _c04 = ["item"];
var _c14 = ["empty"];
var _c23 = ["header"];
var _c33 = ["footer"];
var _c43 = ["selecteditem"];
var _c53 = ["group"];
var _c6 = ["loader"];
var _c7 = ["removeicon"];
var _c8 = ["loadingicon"];
var _c9 = ["clearicon"];
var _c10 = ["dropdownicon"];
var _c11 = ["focusInput"];
var _c122 = ["multiIn"];
var _c132 = ["multiContainer"];
var _c142 = ["ddBtn"];
var _c15 = ["items"];
var _c16 = ["scroller"];
var _c17 = ["overlay"];
var _c18 = (a0) => ({
  i: a0
});
var _c19 = () => ({});
var _c20 = (a0, a1, a2) => ({
  option: a0,
  i: a1,
  scrollerOptions: a2
});
function _forTrack03($index, $item) {
  return this.getChipTrackKey($item);
}
function _forTrack1($index, $item) {
  return this.getOptionValue($item);
}
function AutoComplete_Conditional_0_Template(rf, ctx) {
  if (rf & 1) {
    const _r1 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "input", 16, 2);
    ɵɵlistener("input", function AutoComplete_Conditional_0_Template_input_input_0_listener($event) {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.onInput($event));
    })("keydown", function AutoComplete_Conditional_0_Template_input_keydown_0_listener($event) {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.onKeyDown($event));
    })("change", function AutoComplete_Conditional_0_Template_input_change_0_listener($event) {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.onInputChange($event));
    })("focus", function AutoComplete_Conditional_0_Template_input_focus_0_listener($event) {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.onInputFocus($event));
    })("blur", function AutoComplete_Conditional_0_Template_input_blur_0_listener($event) {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.onInputBlur($event));
    })("paste", function AutoComplete_Conditional_0_Template_input_paste_0_listener($event) {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.onInputPaste($event));
    })("keyup", function AutoComplete_Conditional_0_Template_input_keyup_0_listener($event) {
      ɵɵrestoreView(_r1);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.onInputKeyUp($event));
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵstyleMap(ctx_r1.inputStyle());
    ɵɵclassMap(ctx_r1.cn(ctx_r1.cx("pcInputText"), ctx_r1.inputStyleClass()));
    ɵɵproperty("pAutoFocus", ctx_r1.autofocus())("pt", ctx_r1.ptm("pcInputText"))("variant", ctx_r1.$variant())("invalid", ctx_r1.invalid())("pSize", ctx_r1.size())("fluid", ctx_r1.hasFluid)("pInputTextUnstyled", ctx_r1.unstyled());
    ɵɵattribute("type", ctx_r1.type())("value", ctx_r1.inputValue())("id", ctx_r1.inputId())("autocomplete", ctx_r1.autocomplete())("placeholder", ctx_r1.placeholder())("name", ctx_r1.name())("minlength", ctx_r1.minlength())("min", ctx_r1.min())("max", ctx_r1.max())("pattern", ctx_r1.$pattern())("size", ctx_r1.inputSize())("maxlength", ctx_r1.maxlength())("tabindex", ctx_r1.$tabindex())("required", ctx_r1.requiredAttr())("readonly", ctx_r1.readonlyAttr())("disabled", ctx_r1.disabledAttr())("aria-label", ctx_r1.ariaLabel())("aria-labelledby", ctx_r1.ariaLabelledBy())("aria-required", ctx_r1.required())("aria-expanded", ctx_r1.overlayVisible())("aria-controls", ctx_r1.getAriaControls())("aria-activedescendant", ctx_r1.getAriaActiveDescendant());
  }
}
function AutoComplete_Conditional_1_Conditional_0_Template(rf, ctx) {
  if (rf & 1) {
    const _r3 = ɵɵgetCurrentView();
    ɵɵnamespaceSVG();
    ɵɵelementStart(0, "svg", 19);
    ɵɵlistener("click", function AutoComplete_Conditional_1_Conditional_0_Template_svg_click_0_listener() {
      ɵɵrestoreView(_r3);
      const ctx_r1 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r1.clear());
    });
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵclassMap(ctx_r1.cx("clearIcon"));
    ɵɵproperty("pBind", ctx_r1.ptm("clearIcon"));
    ɵɵattribute("aria-hidden", true);
  }
}
function AutoComplete_Conditional_1_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r4 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "span", 20);
    ɵɵlistener("click", function AutoComplete_Conditional_1_Conditional_1_Template_span_click_0_listener() {
      ɵɵrestoreView(_r4);
      const ctx_r1 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r1.clear());
    });
    ɵɵelementContainer(1, 21);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵclassMap(ctx_r1.cx("clearIcon"));
    ɵɵproperty("pBind", ctx_r1.ptm("clearIcon"));
    ɵɵadvance();
    ɵɵproperty("ngTemplateOutlet", ctx_r1.clearIconTemplate());
  }
}
function AutoComplete_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵconditionalCreate(0, AutoComplete_Conditional_1_Conditional_0_Template, 1, 4, ":svg:svg", 17)(1, AutoComplete_Conditional_1_Conditional_1_Template, 2, 4, "span", 18);
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵconditional(!ctx_r1.clearIconTemplate() ? 0 : 1);
  }
}
function AutoComplete_Conditional_2_For_3_Conditional_3_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0, 27);
  }
  if (rf & 2) {
    const option_r8 = ɵɵnextContext().$implicit;
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵproperty("ngTemplateOutlet", ctx_r1.selectedItemTemplate())("ngTemplateOutletContext", ctx_r1.getSelectedItemContext(option_r8));
  }
}
function AutoComplete_Conditional_2_For_3_ng_template_4_Conditional_0_Template(rf, ctx) {
  if (rf & 1) {
    const _r9 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "span", 20);
    ɵɵlistener("click", function AutoComplete_Conditional_2_For_3_ng_template_4_Conditional_0_Template_span_click_0_listener($event) {
      ɵɵrestoreView(_r9);
      const ɵ$index_16_r7 = ɵɵnextContext(2).$index;
      const ctx_r1 = ɵɵnextContext(2);
      return ɵɵresetView(!ctx_r1.readonly() && !ctx_r1.$disabled() ? ctx_r1.removeOption($event, ɵ$index_16_r7) : "");
    });
    ɵɵnamespaceSVG();
    ɵɵelement(1, "svg", 29);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(4);
    ɵɵclassMap(ctx_r1.cx("chipIcon"));
    ɵɵproperty("pBind", ctx_r1.ptm("chipIcon"));
    ɵɵadvance();
    ɵɵclassMap(ctx_r1.cx("chipIcon"));
    ɵɵattribute("aria-hidden", true);
  }
}
function AutoComplete_Conditional_2_For_3_ng_template_4_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "span", 28);
    ɵɵelementContainer(1, 27);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ɵ$index_16_r7 = ɵɵnextContext(2).$index;
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵproperty("pBind", ctx_r1.ptm("chipIcon"));
    ɵɵattribute("aria-hidden", true);
    ɵɵadvance();
    ɵɵproperty("ngTemplateOutlet", ctx_r1.removeIconTemplate())("ngTemplateOutletContext", ctx_r1.getRemoveIconContext(ɵ$index_16_r7));
  }
}
function AutoComplete_Conditional_2_For_3_ng_template_4_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵconditionalCreate(0, AutoComplete_Conditional_2_For_3_ng_template_4_Conditional_0_Template, 2, 6, "span", 18)(1, AutoComplete_Conditional_2_For_3_ng_template_4_Conditional_1_Template, 2, 4, "span", 28);
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(3);
    ɵɵconditional(!ctx_r1.removeIconTemplate() ? 0 : 1);
  }
}
function AutoComplete_Conditional_2_For_3_Template(rf, ctx) {
  if (rf & 1) {
    const _r6 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "li", 24, 5)(2, "p-chip", 26);
    ɵɵlistener("onRemove", function AutoComplete_Conditional_2_For_3_Template_p_chip_onRemove_2_listener($event) {
      const ɵ$index_16_r7 = ɵɵrestoreView(_r6).$index;
      const ctx_r1 = ɵɵnextContext(2);
      return ɵɵresetView(!ctx_r1.readonly() ? ctx_r1.removeOption($event, ɵ$index_16_r7) : "");
    });
    ɵɵconditionalCreate(3, AutoComplete_Conditional_2_For_3_Conditional_3_Template, 1, 2, "ng-container", 27);
    ɵɵtemplate(4, AutoComplete_Conditional_2_For_3_ng_template_4_Template, 2, 1, "ng-template", null, 6, ɵɵtemplateRefExtractor);
    ɵɵelementEnd()();
  }
  if (rf & 2) {
    const option_r8 = ctx.$implicit;
    const ɵ$index_16_r7 = ctx.$index;
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵclassMap(ctx_r1.cx("chipItem", ɵɵpureFunction1(16, _c18, ɵ$index_16_r7)));
    ɵɵproperty("pBind", ctx_r1.ptm("chipItem"));
    ɵɵattribute("id", ctx_r1.getMultipleOptionId(ɵ$index_16_r7))("aria-label", ctx_r1.getOptionLabel(option_r8))("aria-setsize", ctx_r1.modelValue().length)("aria-posinset", ɵ$index_16_r7 + 1)("aria-selected", true);
    ɵɵadvance(2);
    ɵɵclassMap(ctx_r1.cx("pcChip"));
    ɵɵproperty("pt", ctx_r1.ptm("pcChip"))("label", ctx_r1.getChipLabel(option_r8))("disabled", ctx_r1.$disabled())("removable", true)("unstyled", ctx_r1.unstyled());
    ɵɵadvance();
    ɵɵconditional(ctx_r1.selectedItemTemplate() ? 3 : -1);
  }
}
function AutoComplete_Conditional_2_Template(rf, ctx) {
  if (rf & 1) {
    const _r5 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "ul", 22, 3);
    ɵɵlistener("focus", function AutoComplete_Conditional_2_Template_ul_focus_0_listener($event) {
      ɵɵrestoreView(_r5);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.onMultipleContainerFocus($event));
    })("blur", function AutoComplete_Conditional_2_Template_ul_blur_0_listener($event) {
      ɵɵrestoreView(_r5);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.onMultipleContainerBlur($event));
    })("keydown", function AutoComplete_Conditional_2_Template_ul_keydown_0_listener($event) {
      ɵɵrestoreView(_r5);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.onMultipleContainerKeyDown($event));
    });
    ɵɵrepeaterCreate(2, AutoComplete_Conditional_2_For_3_Template, 6, 18, "li", 23, _forTrack03, true);
    ɵɵelementStart(4, "li", 24)(5, "input", 25, 4);
    ɵɵlistener("input", function AutoComplete_Conditional_2_Template_input_input_5_listener($event) {
      ɵɵrestoreView(_r5);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.onInput($event));
    })("keydown", function AutoComplete_Conditional_2_Template_input_keydown_5_listener($event) {
      ɵɵrestoreView(_r5);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.onKeyDown($event));
    })("change", function AutoComplete_Conditional_2_Template_input_change_5_listener($event) {
      ɵɵrestoreView(_r5);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.onInputChange($event));
    })("focus", function AutoComplete_Conditional_2_Template_input_focus_5_listener($event) {
      ɵɵrestoreView(_r5);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.onInputFocus($event));
    })("blur", function AutoComplete_Conditional_2_Template_input_blur_5_listener($event) {
      ɵɵrestoreView(_r5);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.onInputBlur($event));
    })("paste", function AutoComplete_Conditional_2_Template_input_paste_5_listener($event) {
      ɵɵrestoreView(_r5);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.onInputPaste($event));
    })("keyup", function AutoComplete_Conditional_2_Template_input_keyup_5_listener($event) {
      ɵɵrestoreView(_r5);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.onInputKeyUp($event));
    });
    ɵɵelementEnd()()();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵclassMap(ctx_r1.cx("inputMultiple"));
    ɵɵproperty("pBind", ctx_r1.ptm("inputMultiple"))("tabindex", -1);
    ɵɵattribute("data-p", ctx_r1.inputMultipleDataP)("aria-orientation", "horizontal")("aria-activedescendant", ctx_r1.getMultipleAriaActiveDescendant());
    ɵɵadvance(2);
    ɵɵrepeater(ctx_r1.modelValue());
    ɵɵadvance(2);
    ɵɵclassMap(ctx_r1.cx("inputChip"));
    ɵɵproperty("pBind", ctx_r1.ptm("inputChip"));
    ɵɵadvance();
    ɵɵstyleMap(ctx_r1.inputStyle());
    ɵɵclassMap(ctx_r1.cx("pcInputText"));
    ɵɵproperty("pAutoFocus", ctx_r1.autofocus())("pBind", ctx_r1.ptm("input"));
    ɵɵattribute("type", ctx_r1.type())("id", ctx_r1.inputId())("autocomplete", ctx_r1.autocomplete())("name", ctx_r1.name())("minlength", ctx_r1.minlength())("maxlength", ctx_r1.maxlength())("size", ctx_r1.size())("min", ctx_r1.min())("max", ctx_r1.max())("pattern", ctx_r1.$pattern())("placeholder", ctx_r1.getMultiplePlaceholder())("tabindex", ctx_r1.$tabindex())("required", ctx_r1.requiredAttr())("readonly", ctx_r1.readonlyAttr())("disabled", ctx_r1.disabledAttr())("aria-label", ctx_r1.ariaLabel())("aria-labelledby", ctx_r1.ariaLabelledBy())("aria-required", ctx_r1.required())("aria-expanded", ctx_r1.overlayVisible())("aria-controls", ctx_r1.getAriaControls())("aria-activedescendant", ctx_r1.getAriaActiveDescendant());
  }
}
function AutoComplete_Conditional_3_Conditional_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵnamespaceSVG();
    ɵɵelement(0, "svg", 31);
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵclassMap(ctx_r1.cn(ctx_r1.cx("loader")));
    ɵɵproperty("pBind", ctx_r1.ptm("loader"));
    ɵɵattribute("aria-hidden", true);
  }
}
function AutoComplete_Conditional_3_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "span", 28);
    ɵɵelementContainer(1, 21);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵclassMap(ctx_r1.cx("loader"));
    ɵɵproperty("pBind", ctx_r1.ptm("loader"));
    ɵɵattribute("aria-hidden", true);
    ɵɵadvance();
    ɵɵproperty("ngTemplateOutlet", ctx_r1.loadingIconTemplate());
  }
}
function AutoComplete_Conditional_3_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵconditionalCreate(0, AutoComplete_Conditional_3_Conditional_0_Template, 1, 4, ":svg:svg", 30)(1, AutoComplete_Conditional_3_Conditional_1_Template, 2, 5, "span", 18);
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵconditional(!ctx_r1.loadingIconTemplate() ? 0 : 1);
  }
}
function AutoComplete_Conditional_4_Conditional_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelement(0, "span");
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵclassMap(ctx_r1.dropdownIcon());
    ɵɵattribute("aria-hidden", true);
  }
}
function AutoComplete_Conditional_4_Conditional_3_Conditional_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵnamespaceSVG();
    ɵɵelement(0, "svg", 34);
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(3);
    ɵɵproperty("pBind", ctx_r1.ptm("dropdown"));
  }
}
function AutoComplete_Conditional_4_Conditional_3_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0, 21);
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(3);
    ɵɵproperty("ngTemplateOutlet", ctx_r1.dropdownIconTemplate());
  }
}
function AutoComplete_Conditional_4_Conditional_3_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵconditionalCreate(0, AutoComplete_Conditional_4_Conditional_3_Conditional_0_Template, 1, 1, ":svg:svg", 34)(1, AutoComplete_Conditional_4_Conditional_3_Conditional_1_Template, 1, 1, "ng-container", 21);
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵconditional(!ctx_r1.dropdownIconTemplate() ? 0 : 1);
  }
}
function AutoComplete_Conditional_4_Template(rf, ctx) {
  if (rf & 1) {
    const _r10 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "button", 32, 7);
    ɵɵlistener("click", function AutoComplete_Conditional_4_Template_button_click_0_listener($event) {
      ɵɵrestoreView(_r10);
      const ctx_r1 = ɵɵnextContext();
      return ɵɵresetView(ctx_r1.handleDropdownClick($event));
    });
    ɵɵconditionalCreate(2, AutoComplete_Conditional_4_Conditional_2_Template, 1, 3, "span", 33)(3, AutoComplete_Conditional_4_Conditional_3_Template, 2, 1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵclassMap(ctx_r1.cx("dropdown"));
    ɵɵproperty("pBind", ctx_r1.ptm("dropdown"))("disabled", ctx_r1.$disabled());
    ɵɵattribute("aria-label", ctx_r1.dropdownAriaLabel())("tabindex", ctx_r1.tabindex());
    ɵɵadvance(2);
    ɵɵconditional(ctx_r1.dropdownIcon() ? 2 : 3);
  }
}
function AutoComplete_ng_template_7_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0, 21);
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵproperty("ngTemplateOutlet", ctx_r1.headerTemplate());
  }
}
function AutoComplete_ng_template_7_Conditional_3_ng_template_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0, 27);
  }
  if (rf & 2) {
    const items_r12 = ctx.$implicit;
    const scrollerOptions_r13 = ctx.options;
    ɵɵnextContext(2);
    const buildInItems_r14 = ɵɵreference(6);
    const ctx_r1 = ɵɵnextContext();
    ɵɵproperty("ngTemplateOutlet", buildInItems_r14)("ngTemplateOutletContext", ctx_r1.getBuildInItemsContext(items_r12, scrollerOptions_r13));
  }
}
function AutoComplete_ng_template_7_Conditional_3_Conditional_4_ng_template_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0, 27);
  }
  if (rf & 2) {
    const scrollerOptions_r15 = ctx.options;
    const ctx_r1 = ɵɵnextContext(4);
    ɵɵproperty("ngTemplateOutlet", ctx_r1.loaderTemplate())("ngTemplateOutletContext", ctx_r1.getLoaderContext(scrollerOptions_r15));
  }
}
function AutoComplete_ng_template_7_Conditional_3_Conditional_4_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵtemplate(0, AutoComplete_ng_template_7_Conditional_3_Conditional_4_ng_template_0_Template, 1, 2, "ng-template", null, 10, ɵɵtemplateRefExtractor);
  }
}
function AutoComplete_ng_template_7_Conditional_3_Template(rf, ctx) {
  if (rf & 1) {
    const _r11 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "p-scroller", 38, 9);
    ɵɵlistener("onLazyLoad", function AutoComplete_ng_template_7_Conditional_3_Template_p_scroller_onLazyLoad_0_listener($event) {
      ɵɵrestoreView(_r11);
      const ctx_r1 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r1.onLazyLoad.emit($event));
    });
    ɵɵtemplate(2, AutoComplete_ng_template_7_Conditional_3_ng_template_2_Template, 1, 2, "ng-template", null, 1, ɵɵtemplateRefExtractor);
    ɵɵconditionalCreate(4, AutoComplete_ng_template_7_Conditional_3_Conditional_4_Template, 2, 0);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵstyleMap(ctx_r1.scrollerStyle());
    ɵɵproperty("tabindex", -1)("pt", ctx_r1.ptm("virtualScroller"))("items", ctx_r1.visibleOptions())("itemSize", ctx_r1.virtualScrollItemSize())("autoSize", true)("lazy", ctx_r1.lazy())("options", ctx_r1.virtualScrollOptions());
    ɵɵadvance(4);
    ɵɵconditional(ctx_r1.loaderTemplate() ? 4 : -1);
  }
}
function AutoComplete_ng_template_7_Conditional_4_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0, 27);
  }
  if (rf & 2) {
    ɵɵnextContext();
    const buildInItems_r14 = ɵɵreference(6);
    const ctx_r1 = ɵɵnextContext();
    ɵɵproperty("ngTemplateOutlet", buildInItems_r14)("ngTemplateOutletContext", ctx_r1.getBuildInItemsContext(ctx_r1.visibleOptions(), ɵɵpureFunction0(2, _c19)));
  }
}
function AutoComplete_ng_template_7_ng_template_5_For_3_Conditional_0_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "span");
    ɵɵtext(1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const option_r16 = ɵɵnextContext(2).$implicit;
    const ctx_r1 = ɵɵnextContext(3);
    ɵɵadvance();
    ɵɵtextInterpolate(ctx_r1.getOptionGroupLabel(option_r16.optionGroup));
  }
}
function AutoComplete_ng_template_7_ng_template_5_For_3_Conditional_0_Conditional_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0, 27);
  }
  if (rf & 2) {
    const option_r16 = ɵɵnextContext(2).$implicit;
    const ctx_r1 = ɵɵnextContext(3);
    ɵɵproperty("ngTemplateOutlet", ctx_r1.groupTemplate())("ngTemplateOutletContext", ctx_r1.getGroupContext(option_r16.optionGroup));
  }
}
function AutoComplete_ng_template_7_ng_template_5_For_3_Conditional_0_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "li", 24);
    ɵɵconditionalCreate(1, AutoComplete_ng_template_7_ng_template_5_For_3_Conditional_0_Conditional_1_Template, 2, 1, "span");
    ɵɵconditionalCreate(2, AutoComplete_ng_template_7_ng_template_5_For_3_Conditional_0_Conditional_2_Template, 1, 2, "ng-container", 27);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ɵ$index_92_r17 = ɵɵnextContext().$index;
    const scrollerOptions_r18 = ɵɵnextContext().options;
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵstyleMap(ctx_r1.getItemStyle(scrollerOptions_r18));
    ɵɵclassMap(ctx_r1.cx("optionGroup"));
    ɵɵproperty("pBind", ctx_r1.ptm("optionGroup"));
    ɵɵattribute("id", ctx_r1.getOptionElementId(ɵ$index_92_r17, scrollerOptions_r18));
    ɵɵadvance();
    ɵɵconditional(!ctx_r1.groupTemplate() ? 1 : -1);
    ɵɵadvance();
    ɵɵconditional(ctx_r1.groupTemplate() ? 2 : -1);
  }
}
function AutoComplete_ng_template_7_ng_template_5_For_3_Conditional_1_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "span");
    ɵɵtext(1);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const option_r16 = ɵɵnextContext(2).$implicit;
    const ctx_r1 = ɵɵnextContext(3);
    ɵɵadvance();
    ɵɵtextInterpolate(ctx_r1.getOptionLabel(option_r16));
  }
}
function AutoComplete_ng_template_7_ng_template_5_For_3_Conditional_1_Conditional_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0, 27);
  }
  if (rf & 2) {
    const ctx_r19 = ɵɵnextContext(2);
    const option_r16 = ctx_r19.$implicit;
    const ɵ$index_92_r17 = ctx_r19.$index;
    const scrollerOptions_r18 = ɵɵnextContext().options;
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵproperty("ngTemplateOutlet", ctx_r1.itemTemplate())("ngTemplateOutletContext", ctx_r1.getItemContext(option_r16, ɵ$index_92_r17, scrollerOptions_r18));
  }
}
function AutoComplete_ng_template_7_ng_template_5_For_3_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    const _r19 = ɵɵgetCurrentView();
    ɵɵelementStart(0, "li", 42);
    ɵɵlistener("click", function AutoComplete_ng_template_7_ng_template_5_For_3_Conditional_1_Template_li_click_0_listener($event) {
      ɵɵrestoreView(_r19);
      const option_r16 = ɵɵnextContext().$implicit;
      const ctx_r1 = ɵɵnextContext(3);
      return ɵɵresetView(ctx_r1.onOptionSelect($event, option_r16));
    })("mouseenter", function AutoComplete_ng_template_7_ng_template_5_For_3_Conditional_1_Template_li_mouseenter_0_listener($event) {
      ɵɵrestoreView(_r19);
      const ɵ$index_92_r17 = ɵɵnextContext().$index;
      const scrollerOptions_r18 = ɵɵnextContext().options;
      const ctx_r1 = ɵɵnextContext(2);
      return ɵɵresetView(ctx_r1.onOptionMouseEnter($event, ctx_r1.getOptionIndex(ɵ$index_92_r17, scrollerOptions_r18)));
    });
    ɵɵconditionalCreate(1, AutoComplete_ng_template_7_ng_template_5_For_3_Conditional_1_Conditional_1_Template, 2, 1, "span")(2, AutoComplete_ng_template_7_ng_template_5_For_3_Conditional_1_Conditional_2_Template, 1, 2, "ng-container", 27);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r19 = ɵɵnextContext();
    const option_r16 = ctx_r19.$implicit;
    const ɵ$index_92_r17 = ctx_r19.$index;
    const scrollerOptions_r18 = ɵɵnextContext().options;
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵstyleMap(ctx_r1.getItemStyle(scrollerOptions_r18));
    ɵɵclassMap(ctx_r1.cx("option", ɵɵpureFunction3(14, _c20, option_r16, ɵ$index_92_r17, scrollerOptions_r18)));
    ɵɵproperty("pBind", ctx_r1.getPTOptions(option_r16, scrollerOptions_r18, ɵ$index_92_r17, "option"));
    ɵɵattribute("id", ctx_r1.getOptionElementId(ɵ$index_92_r17, scrollerOptions_r18))("aria-label", ctx_r1.getOptionLabel(option_r16))("aria-selected", ctx_r1.isSelected(option_r16))("data-p-selected", ctx_r1.isSelected(option_r16))("aria-disabled", ctx_r1.isOptionDisabled(option_r16))("data-p-focused", ctx_r1.isOptionFocused(ɵ$index_92_r17, scrollerOptions_r18))("aria-setsize", ctx_r1.ariaSetSize)("aria-posinset", ctx_r1.getAriaPosInset(ctx_r1.getOptionIndex(ɵ$index_92_r17, scrollerOptions_r18)));
    ɵɵadvance();
    ɵɵconditional(!ctx_r1.itemTemplate() ? 1 : 2);
  }
}
function AutoComplete_ng_template_7_ng_template_5_For_3_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵconditionalCreate(0, AutoComplete_ng_template_7_ng_template_5_For_3_Conditional_0_Template, 3, 8, "li", 40)(1, AutoComplete_ng_template_7_ng_template_5_For_3_Conditional_1_Template, 3, 18, "li", 41);
  }
  if (rf & 2) {
    const option_r16 = ctx.$implicit;
    const ctx_r1 = ɵɵnextContext(3);
    ɵɵconditional(ctx_r1.isOptionGroup(option_r16) ? 0 : 1);
  }
}
function AutoComplete_ng_template_7_ng_template_5_Conditional_4_Conditional_1_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵtext(0);
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(4);
    ɵɵtextInterpolate1(" ", ctx_r1.searchResultMessageText, " ");
  }
}
function AutoComplete_ng_template_7_ng_template_5_Conditional_4_Conditional_2_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0, 21);
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(4);
    ɵɵproperty("ngTemplateOutlet", ctx_r1.emptyTemplate());
  }
}
function AutoComplete_ng_template_7_ng_template_5_Conditional_4_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "li", 24);
    ɵɵconditionalCreate(1, AutoComplete_ng_template_7_ng_template_5_Conditional_4_Conditional_1_Template, 1, 1)(2, AutoComplete_ng_template_7_ng_template_5_Conditional_4_Conditional_2_Template, 1, 1, "ng-container", 21);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const scrollerOptions_r18 = ɵɵnextContext().options;
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵstyleMap(ctx_r1.getItemStyle(scrollerOptions_r18));
    ɵɵclassMap(ctx_r1.cx("emptyMessage"));
    ɵɵproperty("pBind", ctx_r1.ptm("emptyMessage"));
    ɵɵadvance();
    ɵɵconditional(!ctx_r1.emptyTemplate() ? 1 : 2);
  }
}
function AutoComplete_ng_template_7_ng_template_5_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "ul", 39, 11);
    ɵɵrepeaterCreate(2, AutoComplete_ng_template_7_ng_template_5_For_3_Template, 2, 1, null, null, _forTrack1, true);
    ɵɵconditionalCreate(4, AutoComplete_ng_template_7_ng_template_5_Conditional_4_Template, 3, 6, "li", 40);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const items_r21 = ctx.$implicit;
    const scrollerOptions_r18 = ctx.options;
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵstyleMap(scrollerOptions_r18.contentStyle);
    ɵɵclassMap(ctx_r1.cn(ctx_r1.cx("list"), scrollerOptions_r18.contentStyleClass));
    ɵɵproperty("pBind", ctx_r1.ptm("list"));
    ɵɵattribute("id", ctx_r1.$listId())("aria-label", ctx_r1.listLabel);
    ɵɵadvance(2);
    ɵɵrepeater(items_r21);
    ɵɵadvance(2);
    ɵɵconditional(ctx_r1.shouldShowEmptyMessage(items_r21) ? 4 : -1);
  }
}
function AutoComplete_ng_template_7_Conditional_7_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementContainer(0, 21);
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext(2);
    ɵɵproperty("ngTemplateOutlet", ctx_r1.footerTemplate());
  }
}
function AutoComplete_ng_template_7_Template(rf, ctx) {
  if (rf & 1) {
    ɵɵelementStart(0, "div", 28);
    ɵɵconditionalCreate(1, AutoComplete_ng_template_7_Conditional_1_Template, 1, 1, "ng-container", 21);
    ɵɵelementStart(2, "div", 35);
    ɵɵconditionalCreate(3, AutoComplete_ng_template_7_Conditional_3_Template, 5, 10, "p-scroller", 36)(4, AutoComplete_ng_template_7_Conditional_4_Template, 1, 3, "ng-container", 27);
    ɵɵelementEnd();
    ɵɵtemplate(5, AutoComplete_ng_template_7_ng_template_5_Template, 5, 8, "ng-template", null, 8, ɵɵtemplateRefExtractor);
    ɵɵconditionalCreate(7, AutoComplete_ng_template_7_Conditional_7_Template, 1, 1, "ng-container", 21);
    ɵɵelementEnd();
    ɵɵelementStart(8, "span", 37);
    ɵɵtext(9);
    ɵɵelementEnd();
  }
  if (rf & 2) {
    const ctx_r1 = ɵɵnextContext();
    ɵɵstyleMap(ctx_r1.panelStyle());
    ɵɵclassMap(ctx_r1.cn(ctx_r1.cx("overlay"), ctx_r1.panelStyleClass()));
    ɵɵproperty("pBind", ctx_r1.ptm("overlay"));
    ɵɵadvance();
    ɵɵconditional(ctx_r1.headerTemplate() ? 1 : -1);
    ɵɵadvance();
    ɵɵclassMap(ctx_r1.cx("listContainer"));
    ɵɵstyleProp("max-height", ctx_r1.getListContainerMaxHeight());
    ɵɵproperty("pBind", ctx_r1.ptm("listContainer"))("tabindex", -1);
    ɵɵadvance();
    ɵɵconditional(ctx_r1.virtualScroll() ? 3 : 4);
    ɵɵadvance(4);
    ɵɵconditional(ctx_r1.footerTemplate() ? 7 : -1);
    ɵɵadvance(2);
    ɵɵtextInterpolate1(" ", ctx_r1.selectedMessageText, " ");
  }
}
var inlineStyles3 = {
  root: {
    position: "relative"
  }
};
var classes4 = {
  root: ({
    instance
  }) => ["p-autocomplete p-component p-inputwrapper", {
    "p-invalid": instance.invalid(),
    "p-focus": instance.focused(),
    "p-inputwrapper-filled": instance.$filled(),
    "p-inputwrapper-focus": instance.focused() && !instance.$disabled() || instance.autofocus() || instance.overlayVisible(),
    "p-autocomplete-open": instance.overlayVisible(),
    "p-autocomplete-fluid": instance.hasFluid
  }],
  pcInputText: "p-autocomplete-input",
  inputMultiple: ({
    instance
  }) => ["p-autocomplete-input-multiple", {
    "p-disabled": instance.$disabled(),
    "p-variant-filled": instance.$variant() === "filled"
  }],
  clearIcon: "p-autocomplete-clear-icon",
  chipItem: ({
    instance,
    i
  }) => ["p-autocomplete-chip-item", {
    "p-focus": instance.focusedMultipleOptionIndex() === i
  }],
  pcChip: "p-autocomplete-chip",
  chipIcon: "p-autocomplete-chip-icon",
  inputChip: "p-autocomplete-input-chip",
  loader: "p-autocomplete-loader",
  dropdown: "p-autocomplete-dropdown",
  overlay: ({
    instance
  }) => ["p-autocomplete-overlay p-component-overlay p-component", {
    "p-input-filled": instance.$variant() === "filled",
    "p-ripple-disabled": instance.config.ripple() === false
  }],
  listContainer: "p-autocomplete-list-container",
  list: "p-autocomplete-list",
  optionGroup: "p-autocomplete-option-group",
  option: ({
    instance,
    option,
    i,
    scrollerOptions
  }) => ({
    "p-autocomplete-option": true,
    "p-autocomplete-option-selected": instance.isSelected(option),
    "p-focus": instance.focusedOptionIndex() === instance.getOptionIndex(i, scrollerOptions),
    "p-disabled": instance.isOptionDisabled(option)
  }),
  emptyMessage: "p-autocomplete-empty-message"
};
var AutoCompleteStyle = class _AutoCompleteStyle extends BaseStyle {
  name = "autocomplete";
  style = style3;
  classes = classes4;
  inlineStyles = inlineStyles3;
  static ɵfac = /* @__PURE__ */ (() => {
    let ɵAutoCompleteStyle_BaseFactory;
    return function AutoCompleteStyle_Factory(__ngFactoryType__) {
      return (ɵAutoCompleteStyle_BaseFactory || (ɵAutoCompleteStyle_BaseFactory = ɵɵgetInheritedFactory(_AutoCompleteStyle)))(__ngFactoryType__ || _AutoCompleteStyle);
    };
  })();
  static ɵprov = ɵɵdefineInjectable({
    token: _AutoCompleteStyle,
    factory: _AutoCompleteStyle.ɵfac
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(AutoCompleteStyle, [{
    type: Injectable
  }], null, null);
})();
var AutoCompleteClasses;
(function(AutoCompleteClasses2) {
  AutoCompleteClasses2["root"] = "p-autocomplete";
  AutoCompleteClasses2["pcInputText"] = "p-autocomplete-input";
  AutoCompleteClasses2["inputMultiple"] = "p-autocomplete-input-multiple";
  AutoCompleteClasses2["chipItem"] = "p-autocomplete-chip-item";
  AutoCompleteClasses2["pcChip"] = "p-autocomplete-chip";
  AutoCompleteClasses2["chipIcon"] = "p-autocomplete-chip-icon";
  AutoCompleteClasses2["inputChip"] = "p-autocomplete-input-chip";
  AutoCompleteClasses2["loader"] = "p-autocomplete-loader";
  AutoCompleteClasses2["dropdown"] = "p-autocomplete-dropdown";
  AutoCompleteClasses2["panel"] = "p-autocomplete-overlay";
  AutoCompleteClasses2["list"] = "p-autocomplete-list";
  AutoCompleteClasses2["optionGroup"] = "p-autocomplete-option-group";
  AutoCompleteClasses2["option"] = "p-autocomplete-option";
  AutoCompleteClasses2["emptyMessage"] = "p-autocomplete-empty-message";
  AutoCompleteClasses2["clearIcon"] = "p-autocomplete-clear-icon";
})(AutoCompleteClasses || (AutoCompleteClasses = {}));
var AUTOCOMPLETE_INSTANCE = new InjectionToken("AUTOCOMPLETE_INSTANCE");
var AUTOCOMPLETE_VALUE_ACCESSOR = {
  provide: NG_VALUE_ACCESSOR,
  useExisting: forwardRef(() => AutoComplete),
  multi: true
};
var AutoComplete = class _AutoComplete extends BaseInput {
  componentName = "AutoComplete";
  $pcAutoComplete = inject(AUTOCOMPLETE_INSTANCE, {
    optional: true,
    skipSelf: true
  }) ?? void 0;
  bindDirectiveInstance = inject(Bind, {
    self: true
  });
  /**
   * Minimum number of characters to initiate a search.
   * @group Props
   */
  minQueryLength = input(
    ...ngDevMode ? [void 0, {
      debugName: "minQueryLength"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  /**
   * Delay between keystrokes to wait before sending a query.
   * @group Props
   */
  delay = input(300, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "delay"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    transform: numberAttribute
  }));
  /**
   * Inline style of the overlay panel element.
   * @group Props
   */
  panelStyle = input(
    ...ngDevMode ? [void 0, {
      debugName: "panelStyle"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  /**
   * Style class of the overlay panel element.
   * @group Props
   */
  panelStyleClass = input(
    ...ngDevMode ? [void 0, {
      debugName: "panelStyleClass"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  /**
   * Inline style of the input field.
   * @group Props
   */
  inputStyle = input(
    ...ngDevMode ? [void 0, {
      debugName: "inputStyle"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  /**
   * Identifier of the focus input to match a label defined for the component.
   * @group Props
   */
  inputId = input(
    ...ngDevMode ? [void 0, {
      debugName: "inputId"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  /**
   * Inline style of the input field.
   * @group Props
   */
  inputStyleClass = input(
    ...ngDevMode ? [void 0, {
      debugName: "inputStyleClass"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  /**
   * Hint text for the input field.
   * @group Props
   */
  placeholder = input(
    ...ngDevMode ? [void 0, {
      debugName: "placeholder"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  /**
   * When present, it specifies that the input cannot be typed.
   * @group Props
   */
  readonly = input(false, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "readonly"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    transform: booleanAttribute
  }));
  /**
   * Maximum height of the suggestions panel.
   * @group Props
   */
  scrollHeight = input(
    "200px",
    ...ngDevMode ? [{
      debugName: "scrollHeight"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  /**
   * Defines if data is loaded and interacted with in lazy manner.
   * @group Props
   */
  lazy = input(false, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "lazy"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    transform: booleanAttribute
  }));
  /**
   * Whether the data should be loaded on demand during scroll.
   * @group Props
   */
  virtualScroll = input(false, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "virtualScroll"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    transform: booleanAttribute
  }));
  /**
   * Height of an item in the list for VirtualScrolling.
   * @group Props
   */
  virtualScrollItemSize = input(
    ...ngDevMode ? [void 0, {
      debugName: "virtualScrollItemSize"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  /**
   * Whether to use the scroller feature. The properties of scroller component can be used like an object in it.
   * @group Props
   */
  virtualScrollOptions = input(
    ...ngDevMode ? [void 0, {
      debugName: "virtualScrollOptions"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  /**
   * When enabled, highlights the first item in the list by default.
   * @group Props
   */
  autoHighlight = input(false, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "autoHighlight"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    transform: booleanAttribute
  }));
  /**
   * When present, autocomplete clears the manual input if it does not match of the suggestions to force only accepting values from the suggestions.
   * @group Props
   */
  forceSelection = input(false, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "forceSelection"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    transform: booleanAttribute
  }));
  /**
   * Type of the input, defaults to "text".
   * @group Props
   */
  type = input(
    "text",
    ...ngDevMode ? [{
      debugName: "type"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  /**
   * Whether to automatically manage layering.
   * @group Props
   */
  autoZIndex = input(true, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "autoZIndex"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    transform: booleanAttribute
  }));
  /**
   * Base zIndex value to use in layering.
   * @group Props
   */
  baseZIndex = input(0, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "baseZIndex"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    transform: numberAttribute
  }));
  /**
   * Defines a string that labels the input for accessibility.
   * @group Props
   */
  ariaLabel = input(
    ...ngDevMode ? [void 0, {
      debugName: "ariaLabel"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  /**
   * Defines a string that labels the dropdown button for accessibility.
   * @group Props
   */
  dropdownAriaLabel = input(
    ...ngDevMode ? [void 0, {
      debugName: "dropdownAriaLabel"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  /**
   * Specifies one or more IDs in the DOM that labels the input field.
   * @group Props
   */
  ariaLabelledBy = input(
    ...ngDevMode ? [void 0, {
      debugName: "ariaLabelledBy"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  /**
   * Icon class of the dropdown icon.
   * @group Props
   */
  dropdownIcon = input(
    ...ngDevMode ? [void 0, {
      debugName: "dropdownIcon"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  /**
   * Ensures uniqueness of selected items on multiple mode.
   * @group Props
   */
  unique = input(true, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "unique"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    transform: booleanAttribute
  }));
  /**
   * Whether to display options as grouped when nested options are provided.
   * @group Props
   */
  group = input(false, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "group"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    transform: booleanAttribute
  }));
  /**
   * Whether to run a query when input receives focus.
   * @group Props
   */
  completeOnFocus = input(false, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "completeOnFocus"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    transform: booleanAttribute
  }));
  /**
   * When enabled, a clear icon is displayed to clear the value.
   * @group Props
   */
  showClear = input(false, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "showClear"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    transform: booleanAttribute
  }));
  /**
   * Displays a button next to the input field when enabled.
   * @group Props
   */
  dropdown = input(false, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "dropdown"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    transform: booleanAttribute
  }));
  /**
   * Whether to show the empty message or not.
   * @group Props
   */
  showEmptyMessage = input(true, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "showEmptyMessage"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    transform: booleanAttribute
  }));
  /**
   * Specifies the behavior dropdown button. Default "blank" mode sends an empty string and "current" mode sends the input value.
   * @group Props
   */
  dropdownMode = input(
    "blank",
    ...ngDevMode ? [{
      debugName: "dropdownMode"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  /**
   * Specifies if multiple values can be selected.
   * @deprecated Use InputTags component instead for chips/tags input functionality.
   * @group Props
   */
  multiple = input(false, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "multiple"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    transform: booleanAttribute
  }));
  /**
   * When enabled, the input value is added to the selected items on tab key press when multiple is true and typeahead is false.
   * @deprecated Use InputTags component instead for chips/tags input functionality.
   * @group Props
   */
  addOnTab = input(false, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "addOnTab"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    transform: booleanAttribute
  }));
  /**
   * Index of the element in tabbing order.
   * @group Props
   */
  tabindex = input(
    ...ngDevMode ? [void 0, {
      debugName: "tabindex"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  /**
   * A property to uniquely identify a value in options.
   * @group Props
   */
  dataKey = input(
    ...ngDevMode ? [void 0, {
      debugName: "dataKey"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  /**
   * Text to display when there is no data. Defaults to global value in i18n translation configuration.
   * @group Props
   */
  emptyMessage = input(
    ...ngDevMode ? [void 0, {
      debugName: "emptyMessage"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  /**
   * When present, it specifies that the component should automatically get focus on load.
   * @group Props
   */
  autofocus = input(false, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "autofocus"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    transform: booleanAttribute
  }));
  /**
   * Used to define a string that autocomplete attribute the current element.
   * @group Props
   */
  autocomplete = input(
    "off",
    ...ngDevMode ? [{
      debugName: "autocomplete"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  /**
   * Name of the options field of an option group.
   * @group Props
   */
  optionGroupChildren = input(
    "items",
    ...ngDevMode ? [{
      debugName: "optionGroupChildren"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  /**
   * Name of the label field of an option group.
   * @group Props
   */
  optionGroupLabel = input(
    "label",
    ...ngDevMode ? [{
      debugName: "optionGroupLabel"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  /**
   * Options for the overlay element.
   * @group Props
   */
  overlayOptions = input(
    ...ngDevMode ? [void 0, {
      debugName: "overlayOptions"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  /**
   * An array of suggestions to display.
   * @group Props
   */
  suggestions = input(
    [],
    ...ngDevMode ? [{
      debugName: "suggestions"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  /**
   * Property name or getter function to use as the label of an option.
   * @group Props
   */
  optionLabel = input(
    ...ngDevMode ? [void 0, {
      debugName: "optionLabel"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  /**
   * Property name or getter function to use as the value of an option.
   * @group Props
   */
  optionValue = input(
    ...ngDevMode ? [void 0, {
      debugName: "optionValue"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  /**
   * Unique identifier of the component.
   * @group Props
   */
  id = input(
    ...ngDevMode ? [void 0, {
      debugName: "id"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  /**
   * Text to display when the search is active. Defaults to global value in i18n translation configuration.
   * @group Props
   * @defaultValue '{0} results are available'
   */
  searchMessage = input(
    ...ngDevMode ? [void 0, {
      debugName: "searchMessage"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  /**
   * Text to display when filtering does not return any results. Defaults to global value in i18n translation configuration.
   * @group Props
   * @defaultValue 'No selected item'
   */
  emptySelectionMessage = input(
    ...ngDevMode ? [void 0, {
      debugName: "emptySelectionMessage"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  /**
   * Text to be displayed in hidden accessible field when options are selected. Defaults to global value in i18n translation configuration.
   * @group Props
   * @defaultValue '{0} items selected'
   */
  selectionMessage = input(
    ...ngDevMode ? [void 0, {
      debugName: "selectionMessage"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  /**
   * Whether to focus on the first visible or selected element when the overlay panel is shown.
   * @group Props
   */
  autoOptionFocus = input(false, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "autoOptionFocus"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    transform: booleanAttribute
  }));
  /**
   * When enabled, the focused option is selected.
   * @group Props
   */
  selectOnFocus = input(false, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "selectOnFocus"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    transform: booleanAttribute
  }));
  /**
   * Locale to use in searching. The default locale is the host environment's current locale.
   * @group Props
   */
  searchLocale = input(
    ...ngDevMode ? [void 0, {
      debugName: "searchLocale"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  /**
   * Property name or getter function to use as the disabled flag of an option, defaults to false when not defined.
   * @group Props
   */
  optionDisabled = input(
    ...ngDevMode ? [void 0, {
      debugName: "optionDisabled"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  /**
   * When enabled, the hovered option will be focused.
   * @group Props
   */
  focusOnHover = input(true, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "focusOnHover"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    transform: booleanAttribute
  }));
  /**
   * Whether typeahead is active or not.
   * @defaultValue true
   * @group Props
   */
  typeahead = input(true, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "typeahead"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    transform: booleanAttribute
  }));
  /**
   * Whether to add an item on blur event if the input has value and typeahead is false with multiple mode.
   * @deprecated Use InputTags component instead for chips/tags input functionality.
   * @defaultValue false
   * @group Props
   */
  addOnBlur = input(false, __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "addOnBlur"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    transform: booleanAttribute
  }));
  /**
   * Separator char to add item when typeahead is false and multiple mode is enabled.
   * @deprecated Use InputTags component instead for chips/tags input functionality.
   * @group Props
   */
  separator = input(
    ...ngDevMode ? [void 0, {
      debugName: "separator"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  /**
   * Target element to attach the overlay, valid values are "body" or a local ng-template variable of another element (note: use binding with brackets for template variables, e.g. [appendTo]="mydiv" for a div element having #mydiv as variable name).
   * @defaultValue 'self'
   * @group Props
   */
  appendTo = input(
    void 0,
    ...ngDevMode ? [{
      debugName: "appendTo"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  /**
   * The motion options.
   * @group Props
   */
  motionOptions = input(
    void 0,
    ...ngDevMode ? [{
      debugName: "motionOptions"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  /**
   * Callback to invoke to search for suggestions.
   * @param {AutoCompleteCompleteEvent} event - Custom complete event.
   * @group Emits
   */
  completeMethod = output();
  /**
   * Callback to invoke when a suggestion is selected.
   * @param {AutoCompleteSelectEvent} event - custom select event.
   * @group Emits
   */
  onSelect = output();
  /**
   * Callback to invoke when a selected value is removed.
   * @param {AutoCompleteUnselectEvent} event - custom unselect event.
   * @group Emits
   */
  onUnselect = output();
  /**
   * Callback to invoke when an item is added via addOnBlur or separator features.
   * @param {AutoCompleteAddEvent} event - Custom add event.
   * @group Emits
   */
  onAdd = output();
  /**
   * Callback to invoke when the component receives focus.
   * @param {Event} event - Browser event.
   * @group Emits
   */
  onFocus = output();
  /**
   * Callback to invoke when the component loses focus.
   * @param {Event} event - Browser event.
   * @group Emits
   */
  onBlur = output();
  /**
   * Callback to invoke to when dropdown button is clicked.
   * @param {AutoCompleteDropdownClickEvent} event - custom dropdown click event.
   * @group Emits
   */
  onDropdownClick = output();
  /**
   * Callback to invoke when clear button is clicked.
   * @group Emits
   */
  onClear = output();
  /**
   * Callback to invoke on input key down.
   * @param {KeyboardEvent} event - Keyboard event.
   * @group Emits
   */
  onInputKeydown = output();
  /**
   * Callback to invoke on input key up.
   * @param {KeyboardEvent} event - Keyboard event.
   * @group Emits
   */
  onKeyUp = output();
  /**
   * Callback to invoke on overlay is shown.
   * @group Emits
   */
  onShow = output();
  /**
   * Callback to invoke on overlay is hidden.
   * @group Emits
   */
  onHide = output();
  /**
   * Callback to invoke on lazy load data.
   * @param {AutoCompleteLazyLoadEvent} event - Lazy load event.
   * @group Emits
   */
  onLazyLoad = output();
  inputEL = viewChild(
    "focusInput",
    ...ngDevMode ? [{
      debugName: "inputEL"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  multiInputEl = viewChild(
    "multiIn",
    ...ngDevMode ? [{
      debugName: "multiInputEl"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  multiContainerEL = viewChild(
    "multiContainer",
    ...ngDevMode ? [{
      debugName: "multiContainerEL"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  dropdownButton = viewChild(
    "ddBtn",
    ...ngDevMode ? [{
      debugName: "dropdownButton"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  itemsViewChild = viewChild(
    "items",
    ...ngDevMode ? [{
      debugName: "itemsViewChild"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  scroller = viewChild(
    "scroller",
    ...ngDevMode ? [{
      debugName: "scroller"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  overlayViewChild = viewChild(
    "overlay",
    ...ngDevMode ? [{
      debugName: "overlayViewChild"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  itemsWrapper;
  /**
   * Custom item template.
   * @group Templates
   */
  itemTemplate = contentChild("item", __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "itemTemplate"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    descendants: false
  }));
  /**
   * Custom empty message template.
   * @group Templates
   */
  emptyTemplate = contentChild("empty", __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "emptyTemplate"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    descendants: false
  }));
  /**
   * Custom header template.
   * @group Templates
   */
  headerTemplate = contentChild("header", __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "headerTemplate"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    descendants: false
  }));
  /**
   * Custom footer template.
   * @group Templates
   */
  footerTemplate = contentChild("footer", __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "footerTemplate"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    descendants: false
  }));
  /**
   * Custom selected item template.
   * @group Templates
   */
  selectedItemTemplate = contentChild("selecteditem", __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "selectedItemTemplate"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    descendants: false
  }));
  /**
   * Custom group template.
   * @group Templates
   */
  groupTemplate = contentChild("group", __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "groupTemplate"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    descendants: false
  }));
  /**
   * Custom loader template.
   * @group Templates
   */
  loaderTemplate = contentChild("loader", __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "loaderTemplate"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    descendants: false
  }));
  /**
   * Custom remove icon template.
   * @group Templates
   */
  removeIconTemplate = contentChild("removeicon", __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "removeIconTemplate"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    descendants: false
  }));
  /**
   * Custom loading icon template.
   * @group Templates
   */
  loadingIconTemplate = contentChild("loadingicon", __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "loadingIconTemplate"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    descendants: false
  }));
  /**
   * Custom clear icon template.
   * @group Templates
   */
  clearIconTemplate = contentChild("clearicon", __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "clearIconTemplate"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    descendants: false
  }));
  /**
   * Custom dropdown icon template.
   * @group Templates
   */
  dropdownIconTemplate = contentChild("dropdownicon", __spreadProps(__spreadValues({}, ngDevMode ? {
    debugName: "dropdownIconTemplate"
  } : (
    /* istanbul ignore next */
    {}
  )), {
    descendants: false
  }));
  onHostClick(event) {
    this.onContainerClick(event);
  }
  value;
  timeout = null;
  overlayVisible = signal(
    false,
    ...ngDevMode ? [{
      debugName: "overlayVisible"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  suggestionsUpdated;
  highlightOption;
  highlightOptionChanged;
  focused = signal(
    false,
    ...ngDevMode ? [{
      debugName: "focused"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  loading = signal(
    false,
    ...ngDevMode ? [{
      debugName: "loading"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  scrollHandler;
  listId;
  searchTimeout = null;
  dirty = false;
  focusedMultipleOptionIndex = signal(
    -1,
    ...ngDevMode ? [{
      debugName: "focusedMultipleOptionIndex"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  focusedOptionIndex = signal(
    -1,
    ...ngDevMode ? [{
      debugName: "focusedOptionIndex"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  _componentStyle = inject(AutoCompleteStyle);
  overlayService = inject(OverlayService);
  _internalId = s("pn_id_");
  $id = computed(
    () => this.id() || this._internalId,
    ...ngDevMode ? [{
      debugName: "$id"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  $appendTo = computed(
    () => this.appendTo() || this.config.overlayAppendTo(),
    ...ngDevMode ? [{
      debugName: "$appendTo"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  get $overlayTarget() {
    return this.el?.nativeElement?.closest('[data-pc-name="inputtags"]') ? "@grandparent" : "@parent";
  }
  visibleOptions = computed(
    () => {
      return this.group() ? this.flatOptions(this.suggestions()) : this.suggestions() || [];
    },
    ...ngDevMode ? [{
      debugName: "visibleOptions"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  inputValue = computed(
    () => {
      const modelValue = this.modelValue();
      const selectedOption = this.optionValueSelected ? (this.suggestions() || []).find((option) => b(option, modelValue, this.equalityKey())) : modelValue;
      if (l(modelValue)) {
        if (typeof modelValue === "object" || this.optionValueSelected) {
          const label = this.getOptionLabel(selectedOption);
          return label != null ? label : modelValue;
        } else {
          return modelValue;
        }
      } else {
        return "";
      }
    },
    ...ngDevMode ? [{
      debugName: "inputValue"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  $showClear = computed(
    () => this.$filled() && !this.$disabled() && this.showClear() && !this.loading(),
    ...ngDevMode ? [{
      debugName: "$showClear"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  scrollerStyle = computed(
    () => ({
      height: this.scrollHeight()
    }),
    ...ngDevMode ? [{
      debugName: "scrollerStyle"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  $tabindex = computed(
    () => !this.$disabled() ? this.tabindex() : -1,
    ...ngDevMode ? [{
      debugName: "$tabindex"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  requiredAttr = computed(
    () => this.required() ? "" : void 0,
    ...ngDevMode ? [{
      debugName: "requiredAttr"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  readonlyAttr = computed(
    () => this.readonly() ? "" : void 0,
    ...ngDevMode ? [{
      debugName: "readonlyAttr"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  disabledAttr = computed(
    () => this.$disabled() ? "" : void 0,
    ...ngDevMode ? [{
      debugName: "disabledAttr"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  $listId = computed(
    () => this.$id() + "_list",
    ...ngDevMode ? [{
      debugName: "$listId"
    }] : (
      /* istanbul ignore next */
      []
    )
  );
  get focusedMultipleOptionId() {
    return this.focusedMultipleOptionIndex() !== -1 ? `${this.$id()}_multiple_option_${this.focusedMultipleOptionIndex()}` : null;
  }
  get focusedOptionId() {
    return this.focusedOptionIndex() !== -1 ? `${this.$id()}_${this.focusedOptionIndex()}` : null;
  }
  get searchResultMessageText() {
    return l(this.visibleOptions()) && this.overlayVisible() ? this.searchMessageText.replaceAll("{0}", this.visibleOptions().length) : this.emptySearchMessageText;
  }
  get searchMessageText() {
    return this.searchMessage() || this.config.translation.searchMessage || "";
  }
  get emptySearchMessageText() {
    return this.emptyMessage() || this.config.translation.emptySearchMessage || "";
  }
  get selectionMessageText() {
    return this.selectionMessage() || this.config.translation.selectionMessage || "";
  }
  get emptySelectionMessageText() {
    return this.emptySelectionMessage() || this.config.translation.emptySelectionMessage || "";
  }
  get selectedMessageText() {
    return this.hasSelectedOption() ? this.selectionMessageText.replaceAll("{0}", this.multiple() ? this.modelValue()?.length : "1") : this.emptySelectionMessageText;
  }
  get ariaSetSize() {
    return this.visibleOptions().filter((option) => !this.isOptionGroup(option)).length;
  }
  get listLabel() {
    return this.translate(TranslationKeys.ARIA, "listLabel");
  }
  get virtualScrollerDisabled() {
    return !this.virtualScroll();
  }
  get optionValueSelected() {
    return typeof this.modelValue() === "string" && this.optionValue();
  }
  chipItemClass(index) {
    return this._componentStyle.classes.chipItem({
      instance: this,
      i: index
    });
  }
  constructor() {
    super();
    effect(() => {
      const value = this.suggestions();
      if (value) {
        this.handleSuggestionsChange();
      }
    });
  }
  onAfterViewChecked() {
    this.bindDirectiveInstance.setAttrs(this.ptms(["host", "root"]));
    if (this.suggestionsUpdated && this.overlayViewChild()) {
      setTimeout(() => {
        if (this.overlayViewChild()) {
          this.overlayViewChild().alignOverlay();
        }
      }, 1);
      this.suggestionsUpdated = false;
    }
  }
  handleSuggestionsChange() {
    if (this.loading()) {
      this.suggestions()?.length > 0 || this.showEmptyMessage() || !!this.emptyTemplate() ? this.show() : this.hide();
      const focusedOptionIndex = this.overlayVisible() && this.autoOptionFocus() ? this.findFirstFocusedOptionIndex() : -1;
      this.focusedOptionIndex.set(focusedOptionIndex);
      this.suggestionsUpdated = true;
      this.loading.set(false);
    }
  }
  flatOptions(options) {
    return (options || []).reduce((result, option, index) => {
      result.push({
        optionGroup: option,
        group: true,
        index
      });
      const optionGroupChildren = this.getOptionGroupChildren(option);
      optionGroupChildren && optionGroupChildren.forEach((o) => result.push(o));
      return result;
    }, []);
  }
  isOptionGroup(option) {
    return this.optionGroupLabel() && option.optionGroup && option.group;
  }
  findFirstOptionIndex() {
    return this.visibleOptions().findIndex((option) => this.isValidOption(option));
  }
  findLastOptionIndex() {
    return z(this.visibleOptions(), (option) => this.isValidOption(option));
  }
  findFirstFocusedOptionIndex() {
    const selectedIndex = this.findSelectedOptionIndex();
    return selectedIndex < 0 ? this.findFirstOptionIndex() : selectedIndex;
  }
  findLastFocusedOptionIndex() {
    const selectedIndex = this.findSelectedOptionIndex();
    return selectedIndex < 0 ? this.findLastOptionIndex() : selectedIndex;
  }
  findSelectedOptionIndex() {
    return this.hasSelectedOption() ? this.visibleOptions().findIndex((option) => this.isValidSelectedOption(option)) : -1;
  }
  findNextOptionIndex(index) {
    const matchedOptionIndex = index < this.visibleOptions().length - 1 ? this.visibleOptions().slice(index + 1).findIndex((option) => this.isValidOption(option)) : -1;
    return matchedOptionIndex > -1 ? matchedOptionIndex + index + 1 : index;
  }
  findPrevOptionIndex(index) {
    const matchedOptionIndex = index > 0 ? z(this.visibleOptions().slice(0, index), (option) => this.isValidOption(option)) : -1;
    return matchedOptionIndex > -1 ? matchedOptionIndex : index;
  }
  isValidSelectedOption(option) {
    return this.isValidOption(option) && this.isSelected(option);
  }
  isValidOption(option) {
    return option && !(this.isOptionDisabled(option) || this.isOptionGroup(option));
  }
  isOptionDisabled(option) {
    return this.optionDisabled() ? d(option, this.optionDisabled()) : false;
  }
  isSelected(option) {
    if (this.multiple()) {
      return this.unique() ? this.modelValue()?.some((model2) => b(model2, option, this.equalityKey())) : false;
    }
    return b(this.modelValue(), option, this.equalityKey());
  }
  isOptionMatched(option, value) {
    return this.isValidOption(option) && this.getOptionLabel(option).toLocaleLowerCase(this.searchLocale()) === value.toLocaleLowerCase(this.searchLocale());
  }
  isInputClicked(event) {
    return event.target === this.inputEL()?.nativeElement;
  }
  isDropdownClicked(event) {
    return this.dropdownButton()?.nativeElement ? event.target === this.dropdownButton().nativeElement || this.dropdownButton().nativeElement.contains(event.target) : false;
  }
  equalityKey() {
    return this.optionValue() ? void 0 : this.dataKey();
  }
  onContainerClick(event) {
    if (this.$disabled() || this.loading() || this.isInputClicked(event) || this.isDropdownClicked(event)) {
      return;
    }
    if (!this.overlayViewChild() || !this.overlayViewChild().overlayViewChild()?.nativeElement.contains(event.target)) {
      Ht(this.inputEL()?.nativeElement);
    }
  }
  handleDropdownClick(event) {
    let query = void 0;
    if (this.overlayVisible()) {
      this.hide(true);
    } else {
      Ht(this.inputEL()?.nativeElement);
      query = this.inputEL()?.nativeElement?.value;
      if (this.dropdownMode() === "blank") this.search(event, "", "dropdown");
      else if (this.dropdownMode() === "current") this.search(event, query, "dropdown");
    }
    this.onDropdownClick.emit({
      originalEvent: event,
      query
    });
  }
  onInput(event) {
    if (this.typeahead()) {
      const _minLength = this.minQueryLength() || 1;
      if (this.searchTimeout) {
        clearTimeout(this.searchTimeout);
      }
      let query = event.target.value;
      const maxLen = this.maxlength();
      if (maxLen != null) {
        query = query.split("").slice(0, maxLen).join("");
      }
      if (!this.multiple() && !this.forceSelection()) {
        this.updateModel(query);
      }
      if (query.length === 0 && !this.multiple()) {
        this.onClear.emit();
        setTimeout(() => {
          this.hide();
        }, this.delay() / 2);
      } else {
        if (query.length >= _minLength) {
          this.focusedOptionIndex.set(-1);
          this.searchTimeout = setTimeout(() => {
            this.search(event, query, "input");
          }, this.delay());
        } else {
          this.hide();
        }
      }
    }
  }
  onInputChange(event) {
    this.updateInputWithForceSelection(event);
  }
  onInputFocus(event) {
    if (this.$disabled()) {
      return;
    }
    if (!this.dirty && this.completeOnFocus()) {
      this.search(event, event.target.value, "focus");
    }
    this.dirty = true;
    this.focused.set(true);
    const focusedOptionIndex = this.focusedOptionIndex() !== -1 ? this.focusedOptionIndex() : this.overlayVisible() && this.autoOptionFocus() ? this.findFirstFocusedOptionIndex() : -1;
    this.focusedOptionIndex.set(focusedOptionIndex);
    this.overlayVisible() && this.scrollInView(this.focusedOptionIndex());
    this.onFocus.emit(event);
  }
  onMultipleContainerFocus(event) {
    if (this.$disabled()) {
      return;
    }
    this.focused.set(true);
  }
  onMultipleContainerBlur(event) {
    this.focusedMultipleOptionIndex.set(-1);
    this.focused.set(false);
  }
  onMultipleContainerKeyDown(event) {
    if (this.$disabled()) {
      event.preventDefault();
      return;
    }
    switch (event.code) {
      case "ArrowLeft":
        this.onArrowLeftKeyOnMultiple(event);
        break;
      case "ArrowRight":
        this.onArrowRightKeyOnMultiple(event);
        break;
      case "Backspace":
        this.onBackspaceKeyOnMultiple(event);
        break;
      default:
        break;
    }
  }
  onInputBlur(event) {
    this.dirty = false;
    this.focused.set(false);
    this.focusedOptionIndex.set(-1);
    if (this.addOnBlur() && this.multiple() && !this.typeahead()) {
      const inputValue = (this.multiInputEl()?.nativeElement?.value || event.target.value || "").trim();
      if (inputValue && !this.isSelected(inputValue)) {
        this.updateModel([...this.modelValue() || [], inputValue]);
        this.onAdd.emit({
          originalEvent: event,
          value: inputValue
        });
        if (this.multiInputEl()?.nativeElement) {
          this.multiInputEl().nativeElement.value = "";
        } else {
          event.target.value = "";
        }
      }
    }
    this.onModelTouched();
    this.onBlur.emit(event);
  }
  onInputPaste(event) {
    if (this.separator() && this.multiple() && !this.typeahead()) {
      const pastedData = event.clipboardData?.getData("Text");
      if (pastedData) {
        const values = pastedData.split(this.separator());
        const newValues = [...this.modelValue() || []];
        values.forEach((value) => {
          const trimmedValue = value.trim();
          if (trimmedValue && !this.isSelected(trimmedValue)) {
            newValues.push(trimmedValue);
          }
        });
        if (newValues.length > (this.modelValue() || []).length) {
          const addedValues = newValues.slice((this.modelValue() || []).length);
          this.updateModel(newValues);
          addedValues.forEach((addedValue) => {
            this.onAdd.emit({
              originalEvent: event,
              value: addedValue
            });
          });
          if (this.multiInputEl()?.nativeElement) {
            this.multiInputEl().nativeElement.value = "";
          } else {
            event.target.value = "";
          }
          event.preventDefault();
        }
      }
    } else {
      this.onKeyDown(event);
    }
  }
  onInputKeyUp(event) {
    this.onKeyUp.emit(event);
  }
  onKeyDown(event) {
    if (this.$disabled()) {
      event.preventDefault();
      return;
    }
    this.onInputKeydown.emit(event);
    switch (event.code) {
      case "ArrowDown":
        this.onArrowDownKey(event);
        break;
      case "ArrowUp":
        this.onArrowUpKey(event);
        break;
      case "ArrowLeft":
        this.onArrowLeftKey(event);
        break;
      case "ArrowRight":
        this.onArrowRightKey(event);
        break;
      case "Home":
        this.onHomeKey(event);
        break;
      case "End":
        this.onEndKey(event);
        break;
      case "PageDown":
        this.onPageDownKey(event);
        break;
      case "PageUp":
        this.onPageUpKey(event);
        break;
      case "Enter":
      case "NumpadEnter":
        this.onEnterKey(event);
        break;
      case "Escape":
        this.onEscapeKey(event);
        if (this.overlayVisible()) {
          event.stopPropagation();
        }
        break;
      case "Tab":
        this.onTabKey(event);
        break;
      case "Backspace":
        this.onBackspaceKey(event);
        break;
      case "ShiftLeft":
      case "ShiftRight":
        break;
      default:
        this.handleSeparatorKey(event);
        break;
    }
  }
  handleSeparatorKey(event) {
    const separator = this.separator();
    if (separator && this.multiple() && !this.typeahead()) {
      if (separator === event.key || typeof separator === "string" && event.key === separator || separator instanceof RegExp && event.key.match(separator)) {
        const inputValue = (this.multiInputEl()?.nativeElement?.value || event.target.value || "").trim();
        if (inputValue && !this.isSelected(inputValue)) {
          this.updateModel([...this.modelValue() || [], inputValue]);
          this.onAdd.emit({
            originalEvent: event,
            value: inputValue
          });
          if (this.multiInputEl()?.nativeElement) {
            this.multiInputEl().nativeElement.value = "";
          } else {
            event.target.value = "";
          }
          event.preventDefault();
        }
      }
    }
  }
  onArrowDownKey(event) {
    if (!this.overlayVisible()) {
      return;
    }
    const optionIndex = this.focusedOptionIndex() !== -1 ? this.findNextOptionIndex(this.focusedOptionIndex()) : this.findFirstFocusedOptionIndex();
    this.changeFocusedOptionIndex(event, optionIndex);
    event.preventDefault();
    event.stopPropagation();
  }
  onArrowUpKey(event) {
    if (!this.overlayVisible()) {
      return;
    }
    if (event.altKey) {
      if (this.focusedOptionIndex() !== -1) {
        this.onOptionSelect(event, this.visibleOptions()[this.focusedOptionIndex()]);
      }
      this.overlayVisible() && this.hide();
      event.preventDefault();
    } else {
      const optionIndex = this.focusedOptionIndex() !== -1 ? this.findPrevOptionIndex(this.focusedOptionIndex()) : this.findLastFocusedOptionIndex();
      this.changeFocusedOptionIndex(event, optionIndex);
      event.preventDefault();
      event.stopPropagation();
    }
  }
  onArrowLeftKey(event) {
    const target = event.currentTarget;
    this.focusedOptionIndex.set(-1);
    if (this.multiple()) {
      if (p(target.value) && this.hasSelectedOption()) {
        Ht(this.multiContainerEL()?.nativeElement);
        this.focusedMultipleOptionIndex.set(this.modelValue().length);
      } else {
        event.stopPropagation();
      }
    }
  }
  onArrowRightKey(event) {
    this.focusedOptionIndex.set(-1);
    this.multiple() && event.stopPropagation();
  }
  onHomeKey(event) {
    const currentTarget = event.currentTarget;
    const len = currentTarget.value.length;
    currentTarget.setSelectionRange(0, event.shiftKey ? len : 0);
    this.focusedOptionIndex.set(-1);
    event.preventDefault();
  }
  onEndKey(event) {
    const currentTarget = event.currentTarget;
    const len = currentTarget.value.length;
    currentTarget.setSelectionRange(event.shiftKey ? 0 : len, len);
    this.focusedOptionIndex.set(-1);
    event.preventDefault();
  }
  onPageDownKey(event) {
    this.scrollInView(this.visibleOptions().length - 1);
    event.preventDefault();
  }
  onPageUpKey(event) {
    this.scrollInView(0);
    event.preventDefault();
  }
  onEnterKey(event) {
    if (!this.typeahead() && !this.forceSelection()) {
      if (this.multiple()) {
        const inputValue = event.target.value?.trim();
        if (inputValue && !this.isSelected(inputValue)) {
          this.updateModel([...this.modelValue() || [], inputValue]);
          this.onAdd.emit({
            originalEvent: event,
            value: inputValue
          });
          this.inputEL()?.nativeElement && (this.inputEL().nativeElement.value = "");
        }
      }
    }
    if (!this.overlayVisible()) {
      return;
    } else {
      if (this.focusedOptionIndex() !== -1) {
        this.onOptionSelect(event, this.visibleOptions()[this.focusedOptionIndex()]);
      }
      this.hide();
    }
    event.preventDefault();
  }
  onEscapeKey(event) {
    this.overlayVisible() && this.hide(true);
    event.preventDefault();
  }
  onTabKey(event) {
    if (this.focusedOptionIndex() !== -1) {
      this.onOptionSelect(event, this.visibleOptions()[this.focusedOptionIndex()]);
      return;
    }
    if (this.multiple() && !this.typeahead()) {
      const inputValue = (this.multiInputEl()?.nativeElement?.value || this.inputEL()?.nativeElement?.value || "").trim();
      if (this.addOnTab()) {
        if (inputValue && !this.isSelected(inputValue)) {
          this.updateModel([...this.modelValue() || [], inputValue]);
          this.onAdd.emit({
            originalEvent: event,
            value: inputValue
          });
          if (this.multiInputEl()?.nativeElement) {
            this.multiInputEl().nativeElement.value = "";
          } else if (this.inputEL()?.nativeElement) {
            this.inputEL().nativeElement.value = "";
          }
          this.updateInputValue();
          event.preventDefault();
          this.overlayVisible() && this.hide();
          return;
        }
      }
    }
    this.overlayVisible() && this.hide();
  }
  onBackspaceKey(event) {
    if (this.multiple()) {
      if (l(this.modelValue()) && !this.inputEL()?.nativeElement?.value) {
        const removedValue = this.modelValue()[this.modelValue().length - 1];
        const newValue = this.modelValue().slice(0, -1);
        this.updateModel(newValue);
        this.onUnselect.emit({
          originalEvent: event,
          value: removedValue
        });
      }
      event.stopPropagation();
    }
  }
  onArrowLeftKeyOnMultiple(event) {
    const optionIndex = this.focusedMultipleOptionIndex() < 1 ? 0 : this.focusedMultipleOptionIndex() - 1;
    this.focusedMultipleOptionIndex.set(optionIndex);
  }
  onArrowRightKeyOnMultiple(event) {
    let optionIndex = this.focusedMultipleOptionIndex();
    optionIndex++;
    this.focusedMultipleOptionIndex.set(optionIndex);
    if (optionIndex > this.modelValue().length - 1) {
      this.focusedMultipleOptionIndex.set(-1);
      Ht(this.inputEL()?.nativeElement);
    }
  }
  onBackspaceKeyOnMultiple(event) {
    if (this.focusedMultipleOptionIndex() !== -1) {
      this.removeOption(event, this.focusedMultipleOptionIndex());
    }
  }
  onOptionSelect(event, option, isHide = true) {
    if (this.multiple()) {
      this.inputEL()?.nativeElement && (this.inputEL().nativeElement.value = "");
      if (!this.isSelected(option)) {
        this.updateModel([...this.modelValue() || [], option]);
      }
    } else {
      this.updateModel(option);
    }
    if (event) {
      this.onSelect.emit({
        originalEvent: event,
        value: option
      });
    }
    isHide && this.hide(true);
  }
  onOptionMouseEnter(event, index) {
    if (this.focusOnHover()) {
      this.changeFocusedOptionIndex(event, index);
    }
  }
  search(event, query, source) {
    if (query === void 0 || query === null) {
      return;
    }
    if (source === "input" && query.trim().length === 0) {
      return;
    }
    this.loading.set(true);
    this.completeMethod.emit({
      originalEvent: event,
      query
    });
  }
  removeOption(event, index) {
    event.stopPropagation();
    const removedOption = this.modelValue()[index];
    const value = this.modelValue().filter((_, i) => i !== index);
    this.updateModel(value);
    this.onUnselect.emit({
      originalEvent: event,
      value: removedOption
    });
    Ht(this.inputEL()?.nativeElement);
  }
  updateModel(options) {
    let value = null;
    if (options) {
      value = this.multiple() ? options.map((option) => this.getOptionValue(option)) : this.getOptionValue(options);
    }
    this.value = value;
    this.writeModelValue(options);
    this.onModelChange(value);
    this.updateInputValue();
  }
  updateInputValue() {
    const inputEl = this.inputEL();
    if (inputEl && inputEl.nativeElement) {
      if (!this.multiple()) {
        inputEl.nativeElement.value = this.inputValue();
      } else {
        inputEl.nativeElement.value = "";
      }
    }
  }
  updateInputWithForceSelection(event) {
    const input2 = this.inputEL()?.nativeElement;
    const inputCleared = !input2?.value && l(this.modelValue());
    if (!this.forceSelection() || this.overlayVisible() || !input2?.value && !inputCleared) {
      return;
    }
    const _minLength = this.minQueryLength();
    if (!inputCleared && _minLength != null && input2.value.length < _minLength) {
      return;
    }
    const matchedOption = this.visibleOptions()?.find((option) => this.isOptionMatched(option, input2.value));
    if (!matchedOption) {
      input2.value = "";
      if (!this.multiple()) {
        this.clear();
      }
      return;
    }
    if (matchedOption && !this.isSelected(matchedOption)) {
      this.onOptionSelect(event, matchedOption);
    }
  }
  autoUpdateModel() {
    if ((this.selectOnFocus() || this.autoHighlight()) && this.autoOptionFocus() && !this.hasSelectedOption()) {
      const focusedOptionIndex = this.findFirstFocusedOptionIndex();
      this.focusedOptionIndex.set(focusedOptionIndex);
      this.onOptionSelect(null, this.visibleOptions()[this.focusedOptionIndex()], false);
    }
  }
  scrollInView(index = -1) {
    const id = index !== -1 ? `${this.$id()}_${index}` : this.focusedOptionId;
    const itemsViewChild = this.itemsViewChild();
    if (itemsViewChild && itemsViewChild.nativeElement) {
      const element = et(itemsViewChild.nativeElement, `li[id="${id}"]`);
      if (element) {
        element.scrollIntoView && element.scrollIntoView({
          block: "nearest",
          inline: "nearest"
        });
      } else if (!this.virtualScrollerDisabled) {
        setTimeout(() => {
          this.virtualScroll() && this.scroller()?.scrollToIndex(index !== -1 ? index : this.focusedOptionIndex());
        }, 0);
      }
    }
  }
  changeFocusedOptionIndex(event, index) {
    if (this.focusedOptionIndex() !== index) {
      this.focusedOptionIndex.set(index);
      this.scrollInView();
      if (this.selectOnFocus()) {
        this.onOptionSelect(event, this.visibleOptions()[index], false);
      }
    }
  }
  show(isFocus = false) {
    this.dirty = true;
    this.overlayVisible.set(true);
    const focusedOptionIndex = this.focusedOptionIndex() !== -1 ? this.focusedOptionIndex() : this.autoOptionFocus() ? this.findFirstFocusedOptionIndex() : -1;
    this.focusedOptionIndex.set(focusedOptionIndex);
    isFocus && Ht(this.inputEL()?.nativeElement);
    if (isFocus) {
      Ht(this.inputEL()?.nativeElement);
    }
    this.onShow.emit();
  }
  hide(isFocus = false) {
    const _hide = () => {
      this.dirty = isFocus;
      this.overlayVisible.set(false);
      this.focusedOptionIndex.set(-1);
      isFocus && Ht(this.inputEL()?.nativeElement);
      this.onHide.emit();
      this.updateInputWithForceSelection(null);
    };
    if (this.timeout) {
      clearTimeout(this.timeout);
    }
    this.timeout = setTimeout(() => {
      _hide();
    }, 0);
  }
  clear() {
    this.updateModel(null);
    this.inputEL()?.nativeElement && (this.inputEL().nativeElement.value = "");
    this.onClear.emit();
    this.overlayVisible() && this.hide();
  }
  hasSelectedOption() {
    return l(this.modelValue());
  }
  getAriaPosInset(index) {
    return (this.optionGroupLabel() ? index - this.visibleOptions().slice(0, index).filter((option) => this.isOptionGroup(option)).length : index) + 1;
  }
  getOptionLabel(option) {
    const optionLabel = this.optionLabel();
    return optionLabel ? d(option, optionLabel) : option && option.label != void 0 ? option.label : option;
  }
  getOptionValue(option) {
    const optionValue = this.optionValue();
    return optionValue ? d(option, optionValue) : option && option.value != void 0 ? option.value : option;
  }
  getChipTrackKey(option) {
    return option != null && typeof option === "object" ? this.getOptionValue(option) : option;
  }
  getOptionIndex(index, scrollerOptions) {
    return this.virtualScrollerDisabled ? index : scrollerOptions && scrollerOptions.getItemOptions(index)["index"];
  }
  getOptionGroupLabel(optionGroup) {
    const optionGroupLabel = this.optionGroupLabel();
    return optionGroupLabel ? d(optionGroup, optionGroupLabel) : optionGroup && optionGroup.label != void 0 ? optionGroup.label : optionGroup;
  }
  getOptionGroupChildren(optionGroup) {
    const optionGroupChildren = this.optionGroupChildren();
    return optionGroupChildren ? d(optionGroup, optionGroupChildren) : optionGroup.items;
  }
  getPTOptions(option, scrollerOptions, index, key) {
    return this.ptm(key, {
      context: {
        option,
        index: this.getOptionIndex(index, scrollerOptions),
        selected: this.isSelected(option),
        focused: this.focusedOptionIndex() === this.getOptionIndex(index, scrollerOptions),
        disabled: this.isOptionDisabled(option)
      }
    });
  }
  getSelectedItemContext(option) {
    return {
      $implicit: option
    };
  }
  getRemoveIconContext(index) {
    return {
      removeCallback: this.removeOption.bind(this),
      index,
      class: this.cx("chipIcon")
    };
  }
  getGroupContext(optionGroup) {
    return {
      $implicit: optionGroup
    };
  }
  getItemContext(option, index, scrollerOptions) {
    return {
      $implicit: option,
      index: scrollerOptions.getOptions ? scrollerOptions.getOptions(index) : index
    };
  }
  getLoaderContext(scrollerOptions) {
    return {
      options: scrollerOptions
    };
  }
  getBuildInItemsContext(items, scrollerOptions) {
    return {
      $implicit: items,
      options: scrollerOptions
    };
  }
  shouldShowEmptyMessage(items) {
    return !items || items.length === 0 && this.showEmptyMessage();
  }
  getItemStyle(scrollerOptions) {
    return {
      height: scrollerOptions.itemSize + "px"
    };
  }
  getChipLabel(option) {
    return !this.selectedItemTemplate() && this.getOptionLabel(option);
  }
  getMultipleOptionId(index) {
    return this.$id() + "_multiple_option_" + index;
  }
  getOptionElementId(index, scrollerOptions) {
    return this.$id() + "_" + this.getOptionIndex(index, scrollerOptions);
  }
  isOptionFocused(index, scrollerOptions) {
    return this.focusedOptionIndex() === this.getOptionIndex(index, scrollerOptions);
  }
  getAriaControls() {
    return this.overlayVisible() ? this.$listId() : null;
  }
  getAriaActiveDescendant() {
    return this.focused() ? this.focusedOptionId : void 0;
  }
  getMultipleAriaActiveDescendant() {
    return this.focused() ? this.focusedMultipleOptionId : void 0;
  }
  getMultiplePlaceholder() {
    return !this.$filled() ? this.placeholder() : null;
  }
  getListContainerMaxHeight() {
    return this.virtualScroll() ? "auto" : this.scrollHeight();
  }
  onOverlayBeforeEnter() {
    this.itemsWrapper = et(this.overlayViewChild()?.overlayViewChild()?.nativeElement, this.virtualScroll() ? '[data-pc-name="virtualscroller"]' : '[data-pc-name="pcoverlay"]');
    if (this.virtualScroll()) {
      this.scroller()?.setContentEl(this.itemsViewChild()?.nativeElement);
      this.scroller()?.viewInit();
    }
    if (this.visibleOptions() && this.visibleOptions().length) {
      if (this.virtualScroll()) {
        const selectedIndex = this.modelValue() ? this.focusedOptionIndex() : -1;
        if (selectedIndex !== -1) {
          this.scroller()?.scrollToIndex(selectedIndex);
        }
      } else {
        let selectedListItem = et(this.itemsWrapper, '[data-pc-section="option"][data-p-selected="true"]');
        if (selectedListItem) {
          selectedListItem.scrollIntoView({
            block: "nearest",
            inline: "center"
          });
        }
      }
    }
  }
  get containerDataP() {
    return this.cn({
      fluid: this.hasFluid
    });
  }
  get overlayDataP() {
    return this.cn({
      [`overlay-${this.$appendTo()}`]: true
    });
  }
  get inputMultipleDataP() {
    return this.cn({
      invalid: this.invalid(),
      disabled: this.$disabled(),
      focus: this.focused(),
      fluid: this.hasFluid,
      filled: this.$variant() === "filled",
      empty: !this.$filled(),
      [this.size()]: this.size()
    });
  }
  /**
   * @override
   *
   * @see {@link BaseEditableHolder.writeControlValue}
   * Writes the value to the control.
   */
  writeControlValue(value, setModelValue) {
    if (this.multiple()) {
      const resolved = (value || []).map((val) => {
        const match = this.visibleOptions().find((option) => b(val, option, this.equalityKey()));
        return match ?? val;
      });
      setModelValue(p(value) ? value : resolved);
    } else {
      const option = this.visibleOptions().find((option2) => b(value, option2, this.equalityKey()));
      setModelValue(p(option) ? value : option);
    }
    this.value = value;
    this.updateInputValue();
  }
  onDestroy() {
    if (this.searchTimeout) {
      clearTimeout(this.searchTimeout);
    }
    if (this.timeout) {
      clearTimeout(this.timeout);
    }
    if (this.scrollHandler) {
      this.scrollHandler.destroy();
      this.scrollHandler = null;
    }
  }
  static ɵfac = function AutoComplete_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _AutoComplete)();
  };
  static ɵcmp = ɵɵdefineComponent({
    type: _AutoComplete,
    selectors: [["p-autocomplete"], ["p-auto-complete"]],
    contentQueries: function AutoComplete_ContentQueries(rf, ctx, dirIndex) {
      if (rf & 1) {
        ɵɵcontentQuerySignal(dirIndex, ctx.itemTemplate, _c04, 4)(dirIndex, ctx.emptyTemplate, _c14, 4)(dirIndex, ctx.headerTemplate, _c23, 4)(dirIndex, ctx.footerTemplate, _c33, 4)(dirIndex, ctx.selectedItemTemplate, _c43, 4)(dirIndex, ctx.groupTemplate, _c53, 4)(dirIndex, ctx.loaderTemplate, _c6, 4)(dirIndex, ctx.removeIconTemplate, _c7, 4)(dirIndex, ctx.loadingIconTemplate, _c8, 4)(dirIndex, ctx.clearIconTemplate, _c9, 4)(dirIndex, ctx.dropdownIconTemplate, _c10, 4);
      }
      if (rf & 2) {
        ɵɵqueryAdvance(11);
      }
    },
    viewQuery: function AutoComplete_Query(rf, ctx) {
      if (rf & 1) {
        ɵɵviewQuerySignal(ctx.inputEL, _c11, 5)(ctx.multiInputEl, _c122, 5)(ctx.multiContainerEL, _c132, 5)(ctx.dropdownButton, _c142, 5)(ctx.itemsViewChild, _c15, 5)(ctx.scroller, _c16, 5)(ctx.overlayViewChild, _c17, 5);
      }
      if (rf & 2) {
        ɵɵqueryAdvance(7);
      }
    },
    hostVars: 5,
    hostBindings: function AutoComplete_HostBindings(rf, ctx) {
      if (rf & 1) {
        ɵɵlistener("click", function AutoComplete_click_HostBindingHandler($event) {
          return ctx.onHostClick($event);
        });
      }
      if (rf & 2) {
        ɵɵattribute("data-p", ctx.containerDataP);
        ɵɵstyleMap(ctx.sx("root"));
        ɵɵclassMap(ctx.cx("root"));
      }
    },
    inputs: {
      minQueryLength: [1, "minQueryLength"],
      delay: [1, "delay"],
      panelStyle: [1, "panelStyle"],
      panelStyleClass: [1, "panelStyleClass"],
      inputStyle: [1, "inputStyle"],
      inputId: [1, "inputId"],
      inputStyleClass: [1, "inputStyleClass"],
      placeholder: [1, "placeholder"],
      readonly: [1, "readonly"],
      scrollHeight: [1, "scrollHeight"],
      lazy: [1, "lazy"],
      virtualScroll: [1, "virtualScroll"],
      virtualScrollItemSize: [1, "virtualScrollItemSize"],
      virtualScrollOptions: [1, "virtualScrollOptions"],
      autoHighlight: [1, "autoHighlight"],
      forceSelection: [1, "forceSelection"],
      type: [1, "type"],
      autoZIndex: [1, "autoZIndex"],
      baseZIndex: [1, "baseZIndex"],
      ariaLabel: [1, "ariaLabel"],
      dropdownAriaLabel: [1, "dropdownAriaLabel"],
      ariaLabelledBy: [1, "ariaLabelledBy"],
      dropdownIcon: [1, "dropdownIcon"],
      unique: [1, "unique"],
      group: [1, "group"],
      completeOnFocus: [1, "completeOnFocus"],
      showClear: [1, "showClear"],
      dropdown: [1, "dropdown"],
      showEmptyMessage: [1, "showEmptyMessage"],
      dropdownMode: [1, "dropdownMode"],
      multiple: [1, "multiple"],
      addOnTab: [1, "addOnTab"],
      tabindex: [1, "tabindex"],
      dataKey: [1, "dataKey"],
      emptyMessage: [1, "emptyMessage"],
      autofocus: [1, "autofocus"],
      autocomplete: [1, "autocomplete"],
      optionGroupChildren: [1, "optionGroupChildren"],
      optionGroupLabel: [1, "optionGroupLabel"],
      overlayOptions: [1, "overlayOptions"],
      suggestions: [1, "suggestions"],
      optionLabel: [1, "optionLabel"],
      optionValue: [1, "optionValue"],
      id: [1, "id"],
      searchMessage: [1, "searchMessage"],
      emptySelectionMessage: [1, "emptySelectionMessage"],
      selectionMessage: [1, "selectionMessage"],
      autoOptionFocus: [1, "autoOptionFocus"],
      selectOnFocus: [1, "selectOnFocus"],
      searchLocale: [1, "searchLocale"],
      optionDisabled: [1, "optionDisabled"],
      focusOnHover: [1, "focusOnHover"],
      typeahead: [1, "typeahead"],
      addOnBlur: [1, "addOnBlur"],
      separator: [1, "separator"],
      appendTo: [1, "appendTo"],
      motionOptions: [1, "motionOptions"]
    },
    outputs: {
      completeMethod: "completeMethod",
      onSelect: "onSelect",
      onUnselect: "onUnselect",
      onAdd: "onAdd",
      onFocus: "onFocus",
      onBlur: "onBlur",
      onDropdownClick: "onDropdownClick",
      onClear: "onClear",
      onInputKeydown: "onInputKeydown",
      onKeyUp: "onKeyUp",
      onShow: "onShow",
      onHide: "onHide",
      onLazyLoad: "onLazyLoad"
    },
    features: [ɵɵProvidersFeature([AUTOCOMPLETE_VALUE_ACCESSOR, AutoCompleteStyle, {
      provide: AUTOCOMPLETE_INSTANCE,
      useExisting: _AutoComplete
    }, {
      provide: PARENT_INSTANCE,
      useExisting: _AutoComplete
    }]), ɵɵHostDirectivesFeature([Bind]), ɵɵInheritDefinitionFeature],
    decls: 9,
    vars: 14,
    consts: [["overlay", ""], ["content", ""], ["focusInput", ""], ["multiContainer", ""], ["focusInput", "", "multiIn", ""], ["token", ""], ["removeicon", ""], ["ddBtn", ""], ["buildInItems", ""], ["scroller", ""], ["loader", ""], ["items", ""], ["pInputText", "", "aria-autocomplete", "list", "role", "combobox", 3, "pAutoFocus", "pt", "class", "style", "variant", "invalid", "pSize", "fluid", "pInputTextUnstyled"], ["role", "listbox", 3, "pBind", "class", "tabindex"], ["type", "button", "pRipple", "", 3, "pBind", "class", "disabled"], [3, "visibleChange", "onBeforeEnter", "onHide", "hostAttrSelector", "visible", "options", "target", "appendTo", "unstyled", "pt", "motionOptions"], ["pInputText", "", "aria-autocomplete", "list", "role", "combobox", 3, "input", "keydown", "change", "focus", "blur", "paste", "keyup", "pAutoFocus", "pt", "variant", "invalid", "pSize", "fluid", "pInputTextUnstyled"], ["data-p-icon", "times", 3, "pBind", "class"], [3, "pBind", "class"], ["data-p-icon", "times", 3, "click", "pBind"], [3, "click", "pBind"], [3, "ngTemplateOutlet"], ["role", "listbox", 3, "focus", "blur", "keydown", "pBind", "tabindex"], ["role", "option", 3, "pBind", "class"], ["role", "option", 3, "pBind"], ["role", "combobox", "aria-autocomplete", "list", 3, "input", "keydown", "change", "focus", "blur", "paste", "keyup", "pAutoFocus", "pBind"], [3, "onRemove", "pt", "label", "disabled", "removable", "unstyled"], [3, "ngTemplateOutlet", "ngTemplateOutletContext"], [3, "pBind"], ["data-p-icon", "times-circle"], ["data-p-icon", "spinner", 3, "pBind", "class"], ["data-p-icon", "spinner", 3, "pBind"], ["type", "button", "pRipple", "", 3, "click", "pBind", "disabled"], [3, "class"], ["data-p-icon", "chevron-down", 3, "pBind"], [3, "pBind", "tabindex"], [3, "tabindex", "pt", "items", "style", "itemSize", "autoSize", "lazy", "options"], ["role", "status", "aria-live", "polite", 1, "p-hidden-accessible"], [3, "onLazyLoad", "tabindex", "pt", "items", "itemSize", "autoSize", "lazy", "options"], ["role", "listbox", 3, "pBind"], ["role", "option", 3, "pBind", "class", "style"], ["pRipple", "", "role", "option", 3, "pBind", "style", "class"], ["pRipple", "", "role", "option", 3, "click", "mouseenter", "pBind"]],
    template: function AutoComplete_Template(rf, ctx) {
      if (rf & 1) {
        ɵɵconditionalCreate(0, AutoComplete_Conditional_0_Template, 2, 33, "input", 12);
        ɵɵconditionalCreate(1, AutoComplete_Conditional_1_Template, 2, 1);
        ɵɵconditionalCreate(2, AutoComplete_Conditional_2_Template, 8, 37, "ul", 13);
        ɵɵconditionalCreate(3, AutoComplete_Conditional_3_Template, 2, 1);
        ɵɵconditionalCreate(4, AutoComplete_Conditional_4_Template, 4, 7, "button", 14);
        ɵɵelementStart(5, "p-overlay", 15, 0);
        ɵɵlistener("visibleChange", function AutoComplete_Template_p_overlay_visibleChange_5_listener($event) {
          return ctx.overlayVisible.set($event);
        })("onBeforeEnter", function AutoComplete_Template_p_overlay_onBeforeEnter_5_listener() {
          return ctx.onOverlayBeforeEnter();
        })("onHide", function AutoComplete_Template_p_overlay_onHide_5_listener() {
          return ctx.hide();
        });
        ɵɵtemplate(7, AutoComplete_ng_template_7_Template, 10, 15, "ng-template", null, 1, ɵɵtemplateRefExtractor);
        ɵɵelementEnd();
      }
      if (rf & 2) {
        ɵɵconditional(!ctx.multiple() ? 0 : -1);
        ɵɵadvance();
        ɵɵconditional(ctx.$showClear() ? 1 : -1);
        ɵɵadvance();
        ɵɵconditional(ctx.multiple() ? 2 : -1);
        ɵɵadvance();
        ɵɵconditional(ctx.loading() ? 3 : -1);
        ɵɵadvance();
        ɵɵconditional(ctx.dropdown() ? 4 : -1);
        ɵɵadvance();
        ɵɵproperty("hostAttrSelector", ctx.$attrSelector)("visible", ctx.overlayVisible())("options", ctx.overlayOptions())("target", ctx.$overlayTarget)("appendTo", ctx.$appendTo())("unstyled", ctx.unstyled())("pt", ctx.ptm("pcOverlay"))("motionOptions", ctx.motionOptions());
        ɵɵattribute("data-p", ctx.overlayDataP);
      }
    },
    dependencies: [NgTemplateOutlet, Overlay, InputText, Ripple, Scroller, AutoFocus, TimesCircle, Spinner, ChevronDown, Chip, SharedModule, Times, BindModule, Bind],
    encapsulation: 2
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(AutoComplete, [{
    type: Component,
    args: [{
      selector: "p-autocomplete, p-auto-complete",
      standalone: true,
      imports: [NgTemplateOutlet, Overlay, InputText, Ripple, Scroller, AutoFocus, TimesCircle, Spinner, ChevronDown, Chip, SharedModule, Times, BindModule],
      template: `
        @if (!multiple()) {
            <input
                #focusInput
                [pAutoFocus]="autofocus()"
                pInputText
                [pt]="ptm('pcInputText')"
                [class]="cn(cx('pcInputText'), inputStyleClass())"
                [style]="inputStyle()"
                [attr.type]="type()"
                [attr.value]="inputValue()"
                [variant]="$variant()"
                [invalid]="invalid()"
                [attr.id]="inputId()"
                [attr.autocomplete]="autocomplete()"
                aria-autocomplete="list"
                role="combobox"
                [attr.placeholder]="placeholder()"
                [attr.name]="name()"
                [attr.minlength]="minlength()"
                [pSize]="size()"
                [attr.min]="min()"
                [attr.max]="max()"
                [attr.pattern]="$pattern()"
                [attr.size]="inputSize()"
                [attr.maxlength]="maxlength()"
                [attr.tabindex]="$tabindex()"
                [attr.required]="requiredAttr()"
                [attr.readonly]="readonlyAttr()"
                [attr.disabled]="disabledAttr()"
                [attr.aria-label]="ariaLabel()"
                [attr.aria-labelledby]="ariaLabelledBy()"
                [attr.aria-required]="required()"
                [attr.aria-expanded]="overlayVisible()"
                [attr.aria-controls]="getAriaControls()"
                [attr.aria-activedescendant]="getAriaActiveDescendant()"
                (input)="onInput($event)"
                (keydown)="onKeyDown($event)"
                (change)="onInputChange($event)"
                (focus)="onInputFocus($event)"
                (blur)="onInputBlur($event)"
                (paste)="onInputPaste($event)"
                (keyup)="onInputKeyUp($event)"
                [fluid]="hasFluid"
                [pInputTextUnstyled]="unstyled()"
            />
        }
        @if ($showClear()) {
            @if (!clearIconTemplate()) {
                <svg data-p-icon="times" [pBind]="ptm('clearIcon')" [class]="cx('clearIcon')" (click)="clear()" [attr.aria-hidden]="true" />
            } @else {
                <span [pBind]="ptm('clearIcon')" [class]="cx('clearIcon')" (click)="clear()">
                    <ng-container [ngTemplateOutlet]="clearIconTemplate()!"></ng-container>
                </span>
            }
        }

        @if (multiple()) {
            <ul
                #multiContainer
                [pBind]="ptm('inputMultiple')"
                [class]="cx('inputMultiple')"
                [attr.data-p]="inputMultipleDataP"
                [tabindex]="-1"
                role="listbox"
                [attr.aria-orientation]="'horizontal'"
                [attr.aria-activedescendant]="getMultipleAriaActiveDescendant()"
                (focus)="onMultipleContainerFocus($event)"
                (blur)="onMultipleContainerBlur($event)"
                (keydown)="onMultipleContainerKeyDown($event)"
            >
                @for (option of modelValue(); track getChipTrackKey(option); let i = $index) {
                    <li
                        #token
                        [pBind]="ptm('chipItem')"
                        [class]="cx('chipItem', { i })"
                        [attr.id]="getMultipleOptionId(i)"
                        role="option"
                        [attr.aria-label]="getOptionLabel(option)"
                        [attr.aria-setsize]="modelValue().length"
                        [attr.aria-posinset]="i + 1"
                        [attr.aria-selected]="true"
                    >
                        <p-chip [pt]="ptm('pcChip')" [class]="cx('pcChip')" [label]="getChipLabel(option)" [disabled]="$disabled()" [removable]="true" (onRemove)="!readonly() ? removeOption($event, i) : ''" [unstyled]="unstyled()">
                            @if (selectedItemTemplate()) {
                                <ng-container [ngTemplateOutlet]="selectedItemTemplate()!" [ngTemplateOutletContext]="getSelectedItemContext(option)"></ng-container>
                            }
                            <ng-template #removeicon>
                                @if (!removeIconTemplate()) {
                                    <span [pBind]="ptm('chipIcon')" [class]="cx('chipIcon')" (click)="!readonly() && !$disabled() ? removeOption($event, i) : ''">
                                        <svg data-p-icon="times-circle" [class]="cx('chipIcon')" [attr.aria-hidden]="true" />
                                    </span>
                                } @else {
                                    <span [pBind]="ptm('chipIcon')" [attr.aria-hidden]="true">
                                        <ng-container [ngTemplateOutlet]="removeIconTemplate()!" [ngTemplateOutletContext]="getRemoveIconContext(i)"></ng-container>
                                    </span>
                                }
                            </ng-template>
                        </p-chip>
                    </li>
                }
                <li [pBind]="ptm('inputChip')" [class]="cx('inputChip')" role="option">
                    <input
                        #focusInput
                        #multiIn
                        [pAutoFocus]="autofocus()"
                        [pBind]="ptm('input')"
                        [class]="cx('pcInputText')"
                        [style]="inputStyle()"
                        [attr.type]="type()"
                        [attr.id]="inputId()"
                        [attr.autocomplete]="autocomplete()"
                        [attr.name]="name()"
                        [attr.minlength]="minlength()"
                        [attr.maxlength]="maxlength()"
                        [attr.size]="size()"
                        [attr.min]="min()"
                        [attr.max]="max()"
                        [attr.pattern]="$pattern()"
                        role="combobox"
                        [attr.placeholder]="getMultiplePlaceholder()"
                        aria-autocomplete="list"
                        [attr.tabindex]="$tabindex()"
                        [attr.required]="requiredAttr()"
                        [attr.readonly]="readonlyAttr()"
                        [attr.disabled]="disabledAttr()"
                        [attr.aria-label]="ariaLabel()"
                        [attr.aria-labelledby]="ariaLabelledBy()"
                        [attr.aria-required]="required()"
                        [attr.aria-expanded]="overlayVisible()"
                        [attr.aria-controls]="getAriaControls()"
                        [attr.aria-activedescendant]="getAriaActiveDescendant()"
                        (input)="onInput($event)"
                        (keydown)="onKeyDown($event)"
                        (change)="onInputChange($event)"
                        (focus)="onInputFocus($event)"
                        (blur)="onInputBlur($event)"
                        (paste)="onInputPaste($event)"
                        (keyup)="onInputKeyUp($event)"
                    />
                </li>
            </ul>
        }
        @if (loading()) {
            @if (!loadingIconTemplate()) {
                <svg data-p-icon="spinner" [pBind]="ptm('loader')" [class]="cn(cx('loader'))" [attr.aria-hidden]="true" />
            } @else {
                <span [pBind]="ptm('loader')" [class]="cx('loader')" [attr.aria-hidden]="true">
                    <ng-container [ngTemplateOutlet]="loadingIconTemplate()!"></ng-container>
                </span>
            }
        }
        @if (dropdown()) {
            <button #ddBtn type="button" [pBind]="ptm('dropdown')" [attr.aria-label]="dropdownAriaLabel()" [class]="cx('dropdown')" [disabled]="$disabled()" pRipple (click)="handleDropdownClick($event)" [attr.tabindex]="tabindex()">
                @if (dropdownIcon()) {
                    <span [class]="dropdownIcon()" [attr.aria-hidden]="true"></span>
                } @else {
                    @if (!dropdownIconTemplate()) {
                        <svg data-p-icon="chevron-down" [pBind]="ptm('dropdown')" />
                    } @else {
                        <ng-container [ngTemplateOutlet]="dropdownIconTemplate()!"></ng-container>
                    }
                }
            </button>
        }
        <p-overlay
            #overlay
            [hostAttrSelector]="$attrSelector"
            [visible]="overlayVisible()"
            (visibleChange)="overlayVisible.set($event)"
            [options]="overlayOptions()"
            [target]="$overlayTarget"
            [appendTo]="$appendTo()"
            [unstyled]="unstyled()"
            [pt]="ptm('pcOverlay')"
            [motionOptions]="motionOptions()"
            (onBeforeEnter)="onOverlayBeforeEnter()"
            (onHide)="hide()"
            [attr.data-p]="overlayDataP"
        >
            <ng-template #content>
                <div [pBind]="ptm('overlay')" [class]="cn(cx('overlay'), panelStyleClass())" [style]="panelStyle()">
                    @if (headerTemplate()) {
                        <ng-container [ngTemplateOutlet]="headerTemplate()!"></ng-container>
                    }
                    <div [pBind]="ptm('listContainer')" [class]="cx('listContainer')" [style.max-height]="getListContainerMaxHeight()" [tabindex]="-1">
                        @if (virtualScroll()) {
                            <p-scroller
                                #scroller
                                [tabindex]="-1"
                                [pt]="ptm('virtualScroller')"
                                [items]="visibleOptions()"
                                [style]="scrollerStyle()"
                                [itemSize]="virtualScrollItemSize()"
                                [autoSize]="true"
                                [lazy]="lazy()"
                                (onLazyLoad)="onLazyLoad.emit($event)"
                                [options]="virtualScrollOptions()"
                            >
                                <ng-template #content let-items let-scrollerOptions="options">
                                    <ng-container [ngTemplateOutlet]="buildInItems" [ngTemplateOutletContext]="getBuildInItemsContext(items, scrollerOptions)"></ng-container>
                                </ng-template>
                                @if (loaderTemplate()) {
                                    <ng-template #loader let-scrollerOptions="options">
                                        <ng-container [ngTemplateOutlet]="loaderTemplate()!" [ngTemplateOutletContext]="getLoaderContext(scrollerOptions)"></ng-container>
                                    </ng-template>
                                }
                            </p-scroller>
                        } @else {
                            <ng-container [ngTemplateOutlet]="buildInItems" [ngTemplateOutletContext]="getBuildInItemsContext(visibleOptions(), {})"></ng-container>
                        }
                    </div>

                    <ng-template #buildInItems let-items let-scrollerOptions="options">
                        <ul #items [pBind]="ptm('list')" [class]="cn(cx('list'), scrollerOptions.contentStyleClass)" [style]="scrollerOptions.contentStyle" role="listbox" [attr.id]="$listId()" [attr.aria-label]="listLabel">
                            @for (option of items; track getOptionValue(option); let i = $index) {
                                @if (isOptionGroup(option)) {
                                    <li [pBind]="ptm('optionGroup')" [attr.id]="getOptionElementId(i, scrollerOptions)" [class]="cx('optionGroup')" [style]="getItemStyle(scrollerOptions)" role="option">
                                        @if (!groupTemplate()) {
                                            <span>{{ getOptionGroupLabel(option.optionGroup) }}</span>
                                        }
                                        @if (groupTemplate()) {
                                            <ng-container [ngTemplateOutlet]="groupTemplate()!" [ngTemplateOutletContext]="getGroupContext(option.optionGroup)"></ng-container>
                                        }
                                    </li>
                                } @else {
                                    <li
                                        pRipple
                                        [pBind]="getPTOptions(option, scrollerOptions, i, 'option')"
                                        [style]="getItemStyle(scrollerOptions)"
                                        [class]="cx('option', { option, i, scrollerOptions })"
                                        [attr.id]="getOptionElementId(i, scrollerOptions)"
                                        role="option"
                                        [attr.aria-label]="getOptionLabel(option)"
                                        [attr.aria-selected]="isSelected(option)"
                                        [attr.data-p-selected]="isSelected(option)"
                                        [attr.aria-disabled]="isOptionDisabled(option)"
                                        [attr.data-p-focused]="isOptionFocused(i, scrollerOptions)"
                                        [attr.aria-setsize]="ariaSetSize"
                                        [attr.aria-posinset]="getAriaPosInset(getOptionIndex(i, scrollerOptions))"
                                        (click)="onOptionSelect($event, option)"
                                        (mouseenter)="onOptionMouseEnter($event, getOptionIndex(i, scrollerOptions))"
                                    >
                                        @if (!itemTemplate()) {
                                            <span>{{ getOptionLabel(option) }}</span>
                                        } @else {
                                            <ng-container [ngTemplateOutlet]="itemTemplate()!" [ngTemplateOutletContext]="getItemContext(option, i, scrollerOptions)"></ng-container>
                                        }
                                    </li>
                                }
                            }
                            @if (shouldShowEmptyMessage(items)) {
                                <li [pBind]="ptm('emptyMessage')" [class]="cx('emptyMessage')" [style]="getItemStyle(scrollerOptions)" role="option">
                                    @if (!emptyTemplate()) {
                                        {{ searchResultMessageText }}
                                    } @else {
                                        <ng-container [ngTemplateOutlet]="emptyTemplate()!"></ng-container>
                                    }
                                </li>
                            }
                        </ul>
                    </ng-template>
                    @if (footerTemplate()) {
                        <ng-container [ngTemplateOutlet]="footerTemplate()!"></ng-container>
                    }
                </div>
                <span role="status" aria-live="polite" class="p-hidden-accessible">
                    {{ selectedMessageText }}
                </span>
            </ng-template>
        </p-overlay>
    `,
      providers: [AUTOCOMPLETE_VALUE_ACCESSOR, AutoCompleteStyle, {
        provide: AUTOCOMPLETE_INSTANCE,
        useExisting: AutoComplete
      }, {
        provide: PARENT_INSTANCE,
        useExisting: AutoComplete
      }],
      changeDetection: ChangeDetectionStrategy.OnPush,
      encapsulation: ViewEncapsulation.None,
      host: {
        "[class]": "cx('root')",
        "[style]": "sx('root')",
        "[attr.data-p]": "containerDataP"
      },
      hostDirectives: [Bind]
    }]
  }], () => [], {
    minQueryLength: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "minQueryLength",
        required: false
      }]
    }],
    delay: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "delay",
        required: false
      }]
    }],
    panelStyle: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "panelStyle",
        required: false
      }]
    }],
    panelStyleClass: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "panelStyleClass",
        required: false
      }]
    }],
    inputStyle: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "inputStyle",
        required: false
      }]
    }],
    inputId: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "inputId",
        required: false
      }]
    }],
    inputStyleClass: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "inputStyleClass",
        required: false
      }]
    }],
    placeholder: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "placeholder",
        required: false
      }]
    }],
    readonly: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "readonly",
        required: false
      }]
    }],
    scrollHeight: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "scrollHeight",
        required: false
      }]
    }],
    lazy: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "lazy",
        required: false
      }]
    }],
    virtualScroll: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "virtualScroll",
        required: false
      }]
    }],
    virtualScrollItemSize: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "virtualScrollItemSize",
        required: false
      }]
    }],
    virtualScrollOptions: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "virtualScrollOptions",
        required: false
      }]
    }],
    autoHighlight: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "autoHighlight",
        required: false
      }]
    }],
    forceSelection: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "forceSelection",
        required: false
      }]
    }],
    type: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "type",
        required: false
      }]
    }],
    autoZIndex: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "autoZIndex",
        required: false
      }]
    }],
    baseZIndex: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "baseZIndex",
        required: false
      }]
    }],
    ariaLabel: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "ariaLabel",
        required: false
      }]
    }],
    dropdownAriaLabel: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "dropdownAriaLabel",
        required: false
      }]
    }],
    ariaLabelledBy: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "ariaLabelledBy",
        required: false
      }]
    }],
    dropdownIcon: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "dropdownIcon",
        required: false
      }]
    }],
    unique: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "unique",
        required: false
      }]
    }],
    group: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "group",
        required: false
      }]
    }],
    completeOnFocus: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "completeOnFocus",
        required: false
      }]
    }],
    showClear: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "showClear",
        required: false
      }]
    }],
    dropdown: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "dropdown",
        required: false
      }]
    }],
    showEmptyMessage: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "showEmptyMessage",
        required: false
      }]
    }],
    dropdownMode: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "dropdownMode",
        required: false
      }]
    }],
    multiple: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "multiple",
        required: false
      }]
    }],
    addOnTab: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "addOnTab",
        required: false
      }]
    }],
    tabindex: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "tabindex",
        required: false
      }]
    }],
    dataKey: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "dataKey",
        required: false
      }]
    }],
    emptyMessage: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "emptyMessage",
        required: false
      }]
    }],
    autofocus: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "autofocus",
        required: false
      }]
    }],
    autocomplete: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "autocomplete",
        required: false
      }]
    }],
    optionGroupChildren: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "optionGroupChildren",
        required: false
      }]
    }],
    optionGroupLabel: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "optionGroupLabel",
        required: false
      }]
    }],
    overlayOptions: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "overlayOptions",
        required: false
      }]
    }],
    suggestions: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "suggestions",
        required: false
      }]
    }],
    optionLabel: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "optionLabel",
        required: false
      }]
    }],
    optionValue: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "optionValue",
        required: false
      }]
    }],
    id: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "id",
        required: false
      }]
    }],
    searchMessage: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "searchMessage",
        required: false
      }]
    }],
    emptySelectionMessage: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "emptySelectionMessage",
        required: false
      }]
    }],
    selectionMessage: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "selectionMessage",
        required: false
      }]
    }],
    autoOptionFocus: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "autoOptionFocus",
        required: false
      }]
    }],
    selectOnFocus: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "selectOnFocus",
        required: false
      }]
    }],
    searchLocale: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "searchLocale",
        required: false
      }]
    }],
    optionDisabled: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "optionDisabled",
        required: false
      }]
    }],
    focusOnHover: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "focusOnHover",
        required: false
      }]
    }],
    typeahead: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "typeahead",
        required: false
      }]
    }],
    addOnBlur: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "addOnBlur",
        required: false
      }]
    }],
    separator: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "separator",
        required: false
      }]
    }],
    appendTo: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "appendTo",
        required: false
      }]
    }],
    motionOptions: [{
      type: Input,
      args: [{
        isSignal: true,
        alias: "motionOptions",
        required: false
      }]
    }],
    completeMethod: [{
      type: Output,
      args: ["completeMethod"]
    }],
    onSelect: [{
      type: Output,
      args: ["onSelect"]
    }],
    onUnselect: [{
      type: Output,
      args: ["onUnselect"]
    }],
    onAdd: [{
      type: Output,
      args: ["onAdd"]
    }],
    onFocus: [{
      type: Output,
      args: ["onFocus"]
    }],
    onBlur: [{
      type: Output,
      args: ["onBlur"]
    }],
    onDropdownClick: [{
      type: Output,
      args: ["onDropdownClick"]
    }],
    onClear: [{
      type: Output,
      args: ["onClear"]
    }],
    onInputKeydown: [{
      type: Output,
      args: ["onInputKeydown"]
    }],
    onKeyUp: [{
      type: Output,
      args: ["onKeyUp"]
    }],
    onShow: [{
      type: Output,
      args: ["onShow"]
    }],
    onHide: [{
      type: Output,
      args: ["onHide"]
    }],
    onLazyLoad: [{
      type: Output,
      args: ["onLazyLoad"]
    }],
    inputEL: [{
      type: ViewChild,
      args: ["focusInput", {
        isSignal: true
      }]
    }],
    multiInputEl: [{
      type: ViewChild,
      args: ["multiIn", {
        isSignal: true
      }]
    }],
    multiContainerEL: [{
      type: ViewChild,
      args: ["multiContainer", {
        isSignal: true
      }]
    }],
    dropdownButton: [{
      type: ViewChild,
      args: ["ddBtn", {
        isSignal: true
      }]
    }],
    itemsViewChild: [{
      type: ViewChild,
      args: ["items", {
        isSignal: true
      }]
    }],
    scroller: [{
      type: ViewChild,
      args: ["scroller", {
        isSignal: true
      }]
    }],
    overlayViewChild: [{
      type: ViewChild,
      args: ["overlay", {
        isSignal: true
      }]
    }],
    itemTemplate: [{
      type: ContentChild,
      args: ["item", __spreadProps(__spreadValues({}, {
        descendants: false
      }), {
        isSignal: true
      })]
    }],
    emptyTemplate: [{
      type: ContentChild,
      args: ["empty", __spreadProps(__spreadValues({}, {
        descendants: false
      }), {
        isSignal: true
      })]
    }],
    headerTemplate: [{
      type: ContentChild,
      args: ["header", __spreadProps(__spreadValues({}, {
        descendants: false
      }), {
        isSignal: true
      })]
    }],
    footerTemplate: [{
      type: ContentChild,
      args: ["footer", __spreadProps(__spreadValues({}, {
        descendants: false
      }), {
        isSignal: true
      })]
    }],
    selectedItemTemplate: [{
      type: ContentChild,
      args: ["selecteditem", __spreadProps(__spreadValues({}, {
        descendants: false
      }), {
        isSignal: true
      })]
    }],
    groupTemplate: [{
      type: ContentChild,
      args: ["group", __spreadProps(__spreadValues({}, {
        descendants: false
      }), {
        isSignal: true
      })]
    }],
    loaderTemplate: [{
      type: ContentChild,
      args: ["loader", __spreadProps(__spreadValues({}, {
        descendants: false
      }), {
        isSignal: true
      })]
    }],
    removeIconTemplate: [{
      type: ContentChild,
      args: ["removeicon", __spreadProps(__spreadValues({}, {
        descendants: false
      }), {
        isSignal: true
      })]
    }],
    loadingIconTemplate: [{
      type: ContentChild,
      args: ["loadingicon", __spreadProps(__spreadValues({}, {
        descendants: false
      }), {
        isSignal: true
      })]
    }],
    clearIconTemplate: [{
      type: ContentChild,
      args: ["clearicon", __spreadProps(__spreadValues({}, {
        descendants: false
      }), {
        isSignal: true
      })]
    }],
    dropdownIconTemplate: [{
      type: ContentChild,
      args: ["dropdownicon", __spreadProps(__spreadValues({}, {
        descendants: false
      }), {
        isSignal: true
      })]
    }],
    onHostClick: [{
      type: HostListener,
      args: ["click", ["$event"]]
    }]
  });
})();
var AutoCompleteModule = class _AutoCompleteModule {
  static ɵfac = function AutoCompleteModule_Factory(__ngFactoryType__) {
    return new (__ngFactoryType__ || _AutoCompleteModule)();
  };
  static ɵmod = ɵɵdefineNgModule({
    type: _AutoCompleteModule,
    imports: [AutoComplete, SharedModule],
    exports: [AutoComplete, SharedModule]
  });
  static ɵinj = ɵɵdefineInjector({
    imports: [AutoComplete, SharedModule, SharedModule]
  });
};
(() => {
  (typeof ngDevMode === "undefined" || ngDevMode) && setClassMetadata(AutoCompleteModule, [{
    type: NgModule,
    args: [{
      imports: [AutoComplete, SharedModule],
      exports: [AutoComplete, SharedModule]
    }]
  }], null, null);
})();
export {
  AUTOCOMPLETE_VALUE_ACCESSOR,
  AutoComplete,
  AutoCompleteClasses,
  AutoCompleteModule,
  AutoCompleteStyle
};
//# sourceMappingURL=primeng_autocomplete.js.map
